import { INestNode } from '../components/pivotTable/inteface';
import { IViewField, IRow, IManualSortValue } from '../interfaces';
export declare function buildPivotTable(dimsInRow: IViewField[], dimsInColumn: IViewField[], measures: IViewField[], allData: IRow[], aggData: IRow[], collapsedKeyList: string[], showTableSummary: boolean, sort?: {
    fid: string;
    type: 'ascending' | 'descending';
    mode: 'row' | 'column';
}, manualSortConfig?: Record<string, IManualSortValue[]>, alphabeticalSortConfig?: Record<string, 'ascending' | 'descending'>): {
    lt: INestNode;
    tt: INestNode;
    metric: (IRow | null)[][];
};
