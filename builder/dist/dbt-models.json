{"models":[{"name":"int_accounts","alias":"int_accounts","relation_name":"`project-for-method-dw`.`revenue`.`int_accounts`","description":"One row per Method account (account grain, key = account_record_id).\nCurrent-state account attributes for operational lookup: SaaS run-rate,\nuser licenses, health score, pay type, active flag. entity_record_id\nbridges to the entity-grain revenue models (int_customers,\nint_customer_mrr) which key on EntityRecordID. Source: revenue.Account\n(Alocet nightly mirror). Distinct grain from int_customers, which is\nentity/customer grain and a monthly time series — this model is current\naccount state, one row per account.\n","original_file_path":"models/intermediate/int_accounts.sql","refs":[],"sources":["Account"],"columns":[{"name":"account_record_id","description":"Account-level primary key. Equals Alocet CustomerMethodAccount.RecordID and call_prep.snapshots.account_record_id — the join key for the call-prep brief.\n"},{"name":"entity_record_id","description":"Customer/entity key. Bridge to int_customers / int_customer_mrr, which key on EntityRecordID. This was the missing link that blocked joining account-grain call-prep data to the entity-grain revenue models.\n"},{"name":"company_account","description":"Subdomain-style account name (e.g. \"wavedistro1\")."},{"name":"mrr_run_rate","description":"Monthly SaaS run-rate proxy from Account.Custdatlastsaasamount (nightly snapshot). Excludes prepay discount, portals, and overages. Directional only — NOT recognized revenue and NOT board-grade MRR (canonical recognized SaaS is TransLineFlattened.SaaSAmount). Use for operational context on the account brief, not for revenue reporting.\n"},{"name":"user_licenses","description":"Paid user license count (Account.LicenseCount). Source contains negatives (min observed -2) from credits/adjustments — guard with NULLIF(GREATEST(user_licenses,0),0) before using as a ratio denominator.\n"},{"name":"health_score","description":"Customer health score 0–100 (Account.HealthScore). ~61% null across all accounts, populated mainly on active accounts. Nullable by design — no not_null test.\n"},{"name":"is_active","description":"Whether the account is active (has not cancelled)."},{"name":"saas_pay_type","description":"Billing type, e.g. Prepay or Monthly (Account.SaaSPayType)."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Intermediate model: one row per Method account (account grain), keyed on\n-- account_record_id. Current-state account attributes for operational lookup\n-- (call prep, account views): SaaS run-rate, licenses, health, pay type.\n--\n-- Grain: account (RecordID). Distinct from int_customers, which is\n-- entity/customer grain (EntityRecordID) and a monthly time series. Different\n-- key, different purpose -> separate model, not an extension of int_customers.\n--\n-- Source: revenue.Account (Alocet nightly mirror, one row per account —\n-- 146,398 rows, RecordID unique, verified). entity_record_id is retained as\n-- the bridge to entity-grain models (int_customers, int_customer_mrr) which\n-- key on EntityRecordID.\n--\n-- Caveats baked into column choices (see _int_accounts.yml for detail):\n--   * mrr_run_rate = Account.Custdatlastsaasamount, a nightly snapshot\n--     run-rate proxy (excludes prepay discount, portals, overages). NOT\n--     recognized revenue — do not use for board MRR. Directional only.\n--   * user_licenses can be negative in source (credits/adjustments); any\n--     utilization ratio must guard with NULLIF(GREATEST(user_licenses,0),0).\n--   * health_score is ~61% null (populated mainly on active accounts).\n--\n-- PENDING: utilization (active_users_30d / user_licenses) is not built yet.\n-- The numerator (Alocet CustDatCountUsersActiveLast30days) is not in the\n-- revenue.Account mirror. Once that column is added to the nightly load, add\n-- active_users_30d here and the ratio becomes a one-line derivation.\n\nSELECT\n  RecordID              AS account_record_id,\n  CompanyAccount        AS company_account,\n  EntityRecordID        AS entity_record_id,\n  IsActive              AS is_active,\n  SaaSPayType           AS saas_pay_type,\n  Custdatlastsaasamount AS mrr_run_rate,\n  LicenseCount          AS user_licenses,\n  HealthScore           AS health_score\nFROM {{ source('revenue', 'Account') }}\n","tests":["not_null(account_record_id)","unique(account_record_id)"]},{"name":"int_annual_mrr_movement_decomposed","alias":"int_annual_mrr_movement_decomposed","relation_name":"`project-for-method-dw`.`revenue`.`int_annual_mrr_movement_decomposed`","description":"Annual-cohort sibling of int_mrr_movement_decomposed: per (month, entity) change comparing the customer's book at month M vs M-12, split into app_mrr / seat_mrr / price_mrr via the same price–volume–mix decomposition, plus a movement_kind label (downgrade / expansion / cancellation / new / flat). Validated 2026-06-04: identity holds within $0.01 across all rows (scripts/parity_annual_decomposition_identity.py); reconciles to v_metric__annual_downgrades / expansions / cancellations at $0.00 across all overlapping periods (scripts/parity_annual_decomposition_vs_metrics.py).\n","original_file_path":"models/intermediate/int_annual_mrr_movement_decomposed.sql","refs":["int_customer_mrr_lines"],"sources":["TransLineFlattened"],"columns":[{"name":"month","description":"First of the calendar month (window-end; compared against M-12)."},{"name":"entity_record_id","description":"Stable numeric entity id (join key; survives company renames)."},{"name":"movement_kind","description":"downgrade | expansion | cancellation | new | flat (by net YoY change)."},{"name":"seat_mrr","description":"Portion of the change from seat/user (Qty) movement."},{"name":"app_mrr","description":"Portion from modules (ItemFullName) added or dropped."},{"name":"price_mrr","description":"Portion from unit-rate / discount changes."}],"compiled_sql":"-- models/intermediate/int_annual_mrr_movement_decomposed.sql\n--\n-- Annual-cohort decomposition of each customer's MRR movement into APP / SEAT / PRICE.\n-- Identical to int_mrr_movement_decomposed but pairs month M against month M-12\n-- (annual cohort), matching int_customer_annual_mrr. Grain: one row per (month, entity)\n-- where `month` is the END of the 12-month window. Output from 2023-01.\n--   app   = a module (Service ItemFullName) added or dropped entirely over the year\n--   seat  = same module, change in Qty (users)        -> Δqty * prior unit-rate\n--   price = same module, change in unit-rate (residual) + any Discount-line change\n-- Validation: scripts/parity_annual_decomposition_identity.py + _vs_metrics.py.\n\n{{ config(materialized='table') }}\n\nwith lines as (\n    select * from {{ ref('int_customer_mrr_lines') }}\n),\n\npe_entity_monthly as (\n    select\n        date_trunc(TxnDate, month) as month,\n        EntityRecordID             as entity_record_id,\n        countif(SaaSAmount != 0)   as saas_lines,\n        countif(SaaSAmount != 0 and AccountFullName like '%Prepay Expiry Income%') as expiry_lines\n    from {{ source('revenue', 'TransLineFlattened') }}\n    where TxnDate >= '2021-12-01'\n      and format_date('%Y-%m', TxnDate) < format_date('%Y-%m', current_date())\n      and CompanyAccount not like 'm11%'\n      and CompanyAccount not like 'm18%'\n    group by 1, 2\n),\n\npe_flag as (\n    select month, entity_record_id,\n        (expiry_lines > 0 and expiry_lines = saas_lines) as is_all_pe\n    from pe_entity_monthly\n),\n\nem as (\n    select month, entity_record_id, sum(saas) as cur\n    from lines group by 1, 2\n),\n\ntot as (\n    select * from (\n        select\n            coalesce(a.month, date_add(b.month, interval 12 month)) as month,\n            coalesce(a.entity_record_id, b.entity_record_id)        as entity_record_id,\n            ifnull(a.cur, 0) as cur,\n            b.cur            as prv\n        from em a\n        full outer join em b\n          on a.entity_record_id = b.entity_record_id\n         and b.month = date_sub(a.month, interval 12 month)\n    )\n    where month < date_trunc(current_date(), month)\n      and month >= '2023-01-01'\n),\n\npaired as (\n    select\n        coalesce(c.entity_record_id, p.entity_record_id)         as entity_record_id,\n        coalesce(c.month, date_add(p.month, interval 12 month))  as month,\n        coalesce(c.is_discount, p.is_discount)                   as is_discount,\n        ifnull(c.qty, 0)  as cq, ifnull(p.qty, 0)  as pq,\n        ifnull(c.saas, 0) as cs, ifnull(p.saas, 0) as ps\n    from lines c\n    full outer join lines p\n      on  c.entity_record_id = p.entity_record_id\n      and c.item  = p.item\n      and c.month = date_add(p.month, interval 12 month)\n),\n\neff as (\n    select entity_record_id, month,\n        case when not is_discount and (cs = 0 or ps = 0)\n             then cs - ps else 0 end as app,\n        case when not is_discount and cs <> 0 and ps <> 0\n             then (cq - pq) * safe_divide(ps, pq) else 0 end as seat,\n        case when not is_discount and cs <> 0 and ps <> 0\n             then (cs - ps) - (cq - pq) * safe_divide(ps, pq)\n             when is_discount then cs - ps\n             else 0 end as price\n    from paired\n)\n\nselect\n    t.month,\n    t.entity_record_id,\n    t.prv as p1_saas,\n    t.cur as p2_saas,\n    case\n        when t.prv > 0 and t.cur > 0 and t.cur < t.prv then 'downgrade'\n        when t.prv > 0 and t.cur > 0 and t.cur > t.prv then 'expansion'\n        when t.prv > 0 and t.cur = 0 and coalesce(pe.is_all_pe, false) then 'flat'\n        when t.prv > 0 and t.cur = 0                   then 'cancellation'\n        when (t.prv is null or t.prv = 0) and t.cur > 0 then 'new'\n        else 'flat'\n    end as movement_kind,\n    round(sum(e.app),   2) as app_mrr,\n    round(sum(e.seat),  2) as seat_mrr,\n    round(sum(e.price), 2) as price_mrr\nfrom tot t\njoin eff e\n  on e.entity_record_id = t.entity_record_id and e.month = t.month\nleft join pe_flag pe\n  on pe.entity_record_id = t.entity_record_id\n and pe.month = date_sub(t.month, interval 12 month)\ngroup by 1, 2, 3, 4, 5\n","tests":["accepted_values(movement_kind)","not_null(entity_record_id)","not_null(month)","not_null(movement_kind)"]},{"name":"int_attribution_fractional","alias":"int_attribution_fractional","relation_name":"`project-for-method-dw`.`revenue`.`int_attribution_fractional`","description":"PRIMITIVE — real multi-touch (fractional) channel attribution. One row per\n(account × channel) with the fractional attribution weight; each account's\nweights sum to exactly 1.0 across channels (one customer's credit spread\nacross the channels that touched them). Distinct from the single-touch\n`AttributionChannel` dimension (which collapses each account to one channel\nand buckets the rest as 'Unknown'). Carries signup/first-invoice dates,\nrun-rate plan amount (Custdatlastsaasamount), first-invoice net SaaS, and\nUS/non-US region, so any fractional \"X by channel\" measure is\nSUM(<measure> * attribution_weight) GROUP BY channel. Excludes test accounts\nand internal Method Integration partner rows.\n","original_file_path":"models/marketing/int_attribution_fractional.sql","refs":[],"sources":["Account","TransLineFlattened"],"columns":[{"name":"CompanyAccount","description":"Method account (foreign-key grain — a customer can have multiple accounts)."},{"name":"EntityRecordID","description":"Stable customer-level id."},{"name":"channel","description":"Attribution channel the weight applies to (SEO, PPC, Direct, OPN, Partners, Email, Social, etc.)."},{"name":"attribution_weight","description":"Fractional multi-touch credit for this (account, channel). Sums to 1.0 per account across channels."},{"name":"SignupDate","description":"Trial signup date (for signup-cohort fractional breakdowns)."},{"name":"FirstSaaSInvoiceTxnDate","description":"First SaaS invoice date (conversion date; for new-customer-cohort fractional breakdowns)."},{"name":"plan_rate","description":"Custdatlastsaasamount — current monthly run-rate snapshot. DIRECTIONAL, not invoiced. Used for run-rate ARR."},{"name":"first_invoice_revenue","description":"Net SaaS (SaaSAmount+SaaSExpense) on the account's first-invoice date. Invoiced basis."},{"name":"is_us","description":"TRUE if the account's most-recent AR account is US (gets the USD->CAD multiplier downstream)."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- PRIMITIVE: fractional (real multi-touch) channel attribution.\n--\n-- One row per (account × channel) where the account has a nonzero attribution\n-- weight for that channel. Each account's weights sum to EXACTLY 1.0 across\n-- channels (verified 2026-06-01: 88/88 May accounts; 82 single-channel, 6 split\n-- across 2-3) — i.e. one customer's credit distributed across the channels that\n-- actually touched them. This is the REAL multi-touch model.\n--\n-- Distinct from the single-touch `AttributionChannel` dimension on\n-- int_trials / int_conversions, which collapses each account to ONE channel\n-- (first Att_*=1 wins) and dumps fractional-only accounts into 'Unknown'. Use\n-- THIS view for attribution-credited \"X by channel\" measures (e.g. Channel ARR);\n-- use the single-touch dimension for \"which one channel gets the whole account\".\n--\n-- Carries the account attributes downstream metrics need (signup / first-invoice\n-- dates, run-rate plan amount, first-invoice net SaaS, US/non-US region) so any\n-- fractional \"X by channel\" metric is just SUM(<measure> * attribution_weight)\n-- GROUP BY channel.\n\nWITH accounts AS (\n  SELECT\n    CompanyAccount, EntityRecordID,\n    SignupDate, FirstSaaSInvoiceTxnDate,\n    Custdatlastsaasamount AS plan_rate,\n    Att_Backlinks, Att_Banner_Ads, Att_Content, Att_Direct, Att_Email,\n    Att_Help_Center, Att_None, Att_Online_Chat_Tool,\n    Att_OPN_Other_Peoples_Networks, Att_Other, Att_Partners, Att_Pay_Per_Click,\n    Att_Referral_Link, Att_Referral_Program, Att_Remarketing,\n    Att_Seminar_Conference, Att_SEO, Att_Social\n  FROM {{ source('revenue', 'Account') }}\n  WHERE IsConversionException = FALSE\n    AND Partner != 'Method Integration'\n),\n\nfirst_invoice AS (\n  SELECT\n    CompanyAccount,\n    SUM(CASE WHEN TxnDate = FirstSaaSInvoiceTxnDate\n             THEN SaaSAmount + SaaSExpense ELSE 0 END) AS first_invoice_revenue\n  FROM {{ source('revenue', 'TransLineFlattened') }}\n  GROUP BY CompanyAccount\n),\n\nregion AS (  -- most-recent-transaction AR account → US / CAN / Other\n  SELECT CompanyAccount,\n    CASE WHEN AccountFullName LIKE '%US%'  THEN 'US'\n         WHEN AccountFullName LIKE '%CAN%' THEN 'CAN'\n         ELSE 'Other' END AS region\n  FROM (\n    SELECT CompanyAccount, AccountFullName,\n      ROW_NUMBER() OVER (PARTITION BY CompanyAccount ORDER BY TransRecordID DESC) AS rn\n    FROM {{ source('revenue', 'TransLineFlattened') }}\n    WHERE AccountFullName IS NOT NULL\n  )\n  WHERE rn = 1\n),\n\njoined AS (\n  SELECT\n    a.CompanyAccount, a.EntityRecordID, a.SignupDate, a.FirstSaaSInvoiceTxnDate,\n    a.plan_rate,\n    COALESCE(fi.first_invoice_revenue, 0) AS first_invoice_revenue,\n    COALESCE(r.region, 'Other') = 'US' AS is_us,\n    a.Att_Backlinks, a.Att_Banner_Ads, a.Att_Content, a.Att_Direct, a.Att_Email,\n    a.Att_Help_Center, a.Att_None, a.Att_Online_Chat_Tool,\n    a.Att_OPN_Other_Peoples_Networks, a.Att_Other, a.Att_Partners, a.Att_Pay_Per_Click,\n    a.Att_Referral_Link, a.Att_Referral_Program, a.Att_Remarketing,\n    a.Att_Seminar_Conference, a.Att_SEO, a.Att_Social\n  FROM accounts a\n  LEFT JOIN first_invoice fi USING (CompanyAccount)\n  LEFT JOIN region r USING (CompanyAccount)\n)\n\nSELECT\n  CompanyAccount, EntityRecordID, SignupDate, FirstSaaSInvoiceTxnDate,\n  plan_rate, first_invoice_revenue, is_us,\n  channel, attribution_weight\nFROM joined\nUNPIVOT (attribution_weight FOR channel IN (\n  Att_Backlinks AS 'Backlinks', Att_Banner_Ads AS 'Banner_Ads',\n  Att_Content AS 'Content', Att_Direct AS 'Direct', Att_Email AS 'Email',\n  Att_Help_Center AS 'Help_Center', Att_None AS 'None',\n  Att_Online_Chat_Tool AS 'Online_Chat_Tool',\n  Att_OPN_Other_Peoples_Networks AS 'OPN', Att_Other AS 'Other',\n  Att_Partners AS 'Partners', Att_Pay_Per_Click AS 'PPC',\n  Att_Referral_Link AS 'Referral_Link', Att_Referral_Program AS 'Referral_Program',\n  Att_Remarketing AS 'Remarketing', Att_Seminar_Conference AS 'Seminar_Conference',\n  Att_SEO AS 'SEO', Att_Social AS 'Social'))\nWHERE attribution_weight <> 0\n","tests":[]},{"name":"int_channel_cac","alias":"int_channel_cac","relation_name":"`project-for-method-dw`.`revenue`.`int_channel_cac`","description":"CAC per canonical acquisition channel per month. Numerator = sheet spend (int_marketing_spend); denominator = canonical new-customer counts (int_customers.IsNew by AttributionChannel), NOT the sheet's own count. Directional / marketing-owned input — not a verified revenue_metrics view. Complete months only.\n","original_file_path":"models/marketing/int_channel_cac.sql","refs":["int_customers","int_marketing_spend"],"sources":[],"columns":[{"name":"channel","description":"One of PPC, SEO, OPN, Partners, Email (mutually exclusive; matches AttributionChannel)."},{"name":"month","description":"Calendar month (first of month), complete months only."},{"name":"bq_new_customers","description":"COUNT(DISTINCT EntityRecordID) of int_customers.IsNew for the channel-month. The canonical denominator."},{"name":"cac_media","description":"media_spend / bq_new_customers. Media-only CAC — the default number for cross-channel comparison."},{"name":"cac_fully_loaded","description":"total_spend / bq_new_customers. Includes team + mktg/sales people cost as allocated by the sheet. Label it as fully loaded wherever shown."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- CAC per acquisition channel per month. Spend numerator comes from the\n-- marketing tracker sheet (int_marketing_spend); the customer denominator is\n-- canonical — int_customers.IsNew — NOT the sheet's own customer count. The\n-- two disagree by 10–25% on SEO/Partners (different attribution timing);\n-- sheet_customers is carried alongside so the gap stays visible instead of\n-- silently inherited.\n--\n-- Channels are the mutually-exclusive canonical set (matches\n-- AttributionChannel values). TOTAL and sub-split blocks are excluded here;\n-- query int_marketing_spend directly for those.\n--\n-- cac_media    = media dollars only (the number to compare across channels)\n-- cac_fully_loaded = sheet's Total Spend incl. team + mktg/sales cost\n\nWITH new_customers AS (\n  SELECT AttributionChannel AS channel, Month AS month,\n         COUNT(DISTINCT EntityRecordID) AS bq_new_customers\n  FROM {{ ref('int_customers') }}\n  WHERE IsNew\n  GROUP BY 1, 2\n)\n\nSELECT\n  s.channel,\n  s.month,\n  s.media_spend,\n  s.team_spend,\n  s.mktg_sales_cost,\n  s.total_spend,\n  n.bq_new_customers,\n  s.sheet_customers,\n  SAFE_DIVIDE(s.media_spend, n.bq_new_customers) AS cac_media,\n  SAFE_DIVIDE(s.total_spend, n.bq_new_customers) AS cac_fully_loaded,\n  s.sheet_arr,\n  s.sheet_cac_arr\nFROM {{ ref('int_marketing_spend') }} s\nLEFT JOIN new_customers n USING (channel, month)\nWHERE s.channel IN ('PPC', 'SEO', 'OPN', 'Partners', 'Email')\n  AND s.month < DATE_TRUNC(CURRENT_DATE(), MONTH)\n","tests":[]},{"name":"int_channel_forecast","alias":"int_channel_forecast","relation_name":"`project-for-method-dw`.`revenue`.`int_channel_forecast`","description":"Per-channel monthly forecast for trials and syncs, materialized as a native BQ\ntable. One row per (metric, forecast_date, channel). A pass-through UNION of the\ntwo Sheets-federated forecast views (v_trials_forecast_channel +\nv_syncs_forecast_channel).\n\nWhy materialized: the forecast views federate over Google Sheets (Drive), and\nreading them from the browser frontend requires a Drive OAuth scope the app does\nnot request — which 403'd users. dbt reads the Sheet once at build time (its\nservice account has Drive access) and lands this plain table the frontend can\njoin scope-free. Refreshed on the normal dbt run cadence.\n\n⚠️ Pass-through: the numbers are only as good as the upstream forecast Sheet — if\nthe Sheet is wrong or stale, this table is wrong. Values reflect the last dbt\nbuild, not live edits to the Sheet.\n","original_file_path":"models/marketing/int_channel_forecast.sql","refs":[],"sources":["v_trials_forecast_channel","v_syncs_forecast_channel"],"columns":[{"name":"metric","description":"Which forecast: trials | syncs."},{"name":"forecast_date","description":"First day of the forecast month (from the source view's forecast_date)."},{"name":"channel","description":"Attribution channel (from the source view's AttributionChannel)."},{"name":"forecast_value","description":"Forecasted count for the (metric, month, channel), sourced from the forecast Sheet."}],"compiled_sql":"{{ config(materialized='table') }}\n\n-- Per-channel monthly forecast, MATERIALIZED as a native table so browser\n-- queries never touch the Google-Sheets-federated source (looker_inputs).\n-- The forecast views (v_trials/v_syncs_forecast_channel) federate over Drive;\n-- reading them from the frontend requires Drive scope the app's OAuth doesn't\n-- request, which 403'd users. dbt reads the Sheet here once at build time (its\n-- service account has Drive access) and lands a plain table the frontend can\n-- join scope-free. Refreshed on the normal dbt run cadence.\n\nSELECT 'trials' AS metric, forecast_date, AttributionChannel AS channel, forecast_value\nFROM {{ source('revenue', 'v_trials_forecast_channel') }}\nUNION ALL\nSELECT 'syncs' AS metric, forecast_date, AttributionChannel AS channel, forecast_value\nFROM {{ source('revenue', 'v_syncs_forecast_channel') }}\n","tests":["accepted_values(metric)"]},{"name":"int_channel_funnel_daily","alias":"int_channel_funnel_daily","relation_name":"`project-for-method-dw`.`revenue`.`int_channel_funnel_daily`","description":"Daily building block for the Channel Trajectory scorecard's date-range filter. One row per (event_date, metric, channel) with the fractional attribution weight summed. The frontend windows this by the picked range to compute MTD / trajectory / prior-month / last-year. Trials from Account.SignupDate; syncs from Account where SyncTypeRegion != '', also dated by SignupDate. Fractional multi-touch attribution (Att_* summed directly) — weights can be non-integer. Supporting/building-block model; the month-level int_channel_funnel_trajectory documents the full method.\n\nBacklinks is kept as its OWN channel here. This is the building block, so the breakout stays recoverable; the Backlinks-into-SEO rollup the scorecard shows is applied downstream (int_channel_funnel_trajectory and the frontend windowed query in builder/src/lib/channelTrajectorySql.js).\n\nSyncs read Account directly rather than the revenue.Funnel view: Funnel's EventType='Sync' branch is the same filter re-dated to SignupDate, but its SELECT list omits Att_Email and so dropped every email-attributed sync (fixed 2026-08-10). Because a sync is dated by SignupDate but gated on SyncTypeRegion — which flips when the account eventually syncs — historical sync months keep growing retroactively. [REVIEW: is the daily grain meant to be a consumer-facing surface, or purely internal plumbing behind the month-level trajectory view?]\n","original_file_path":"models/marketing/int_channel_funnel_daily.sql","refs":[],"sources":["Account"],"columns":[{"name":"event_date","description":"Account.SignupDate for BOTH metrics. Not the date the sync happened — a sync row is a signed-up account that has since synced, dated by signup.\n"},{"name":"metric","description":"trials | syncs"},{"name":"channel","description":"Attribution channel (fractional multi-touch)."},{"name":"weight","description":"Fractional attribution weight summed for that day + channel."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Daily building block for the Channel Trajectory scorecard's date-range filter.\n-- One row per (event_date, metric, channel) with the fractional attribution\n-- weight summed for that day. The frontend windows this by the picked date range\n-- to compute MTD / trajectory / prior-month / last-year, and joins the per-channel\n-- forecast views. Attribution is fractional multi-touch (Att_* summed directly);\n-- trials = Account.SignupDate, syncs = Funnel EventType='Sync'. Keeping the\n-- parity-critical attribution here in BQ; only the date windowing lives frontend.\n-- The month-level int_channel_funnel_trajectory view documents the full method.\n--\n-- Backlinks stays its OWN channel here. This is the building block, so the\n-- breakout must remain recoverable; the Backlinks-into-SEO rollup the scorecard\n-- displays happens downstream (int_channel_funnel_trajectory + the frontend\n-- windowed query in builder/src/lib/channelTrajectorySql.js).\n--\n-- Caveat: a sync row is dated by SignupDate but gated on SyncTypeRegion, which\n-- flips when the account eventually syncs — so historical sync months keep\n-- growing retroactively. The same window returns different numbers on different\n-- days. Any \"matches Looker\" claim holds only at a single instant.\n\nWITH trial_rows AS (\n  -- NB: Account's native column is `SignUpDate` (capital U); BigQuery resolves\n  -- column names case-insensitively. Funnel's is `SignupDate`.\n  SELECT SignupDate AS event_date, 'trials' AS metric, channel, weight FROM (\n    SELECT SignupDate,\n      Att_SEO, Att_Pay_Per_Click, Att_OPN_Other_Peoples_Networks, Att_Direct,\n      Att_None, Att_Email, Att_Partners, Att_Content, Att_Social, Att_Other,\n      Att_Referral_Link, Att_Referral_Program, Att_Remarketing, Att_Backlinks,\n      Att_Banner_Ads, Att_Help_Center, Att_Online_Chat_Tool, Att_Seminar_Conference\n    FROM {{ source('revenue', 'Account') }}\n    WHERE IsConversionException = FALSE\n      AND Partner != 'Method Integration'\n      AND SignupDate != DATE('0001-01-01')\n  )\n  UNPIVOT (weight FOR channel IN (\n    Att_SEO AS 'SEO', Att_Pay_Per_Click AS 'PPC',\n    Att_OPN_Other_Peoples_Networks AS 'OPN', Att_Direct AS 'Direct',\n    Att_None AS 'None', Att_Email AS 'Email', Att_Partners AS 'Partners',\n    Att_Content AS 'Content', Att_Social AS 'Social', Att_Other AS 'Other',\n    Att_Referral_Link AS 'Referral', Att_Referral_Program AS 'Referral_Program',\n    Att_Remarketing AS 'Remarketing', Att_Backlinks AS 'Backlinks',\n    Att_Banner_Ads AS 'Banner_Ads', Att_Help_Center AS 'Help_Center',\n    Att_Online_Chat_Tool AS 'Online_Chat', Att_Seminar_Conference AS 'Seminar'))\n  WHERE weight <> 0\n),\n\nsync_rows AS (\n  -- Sourced from Account, NOT the revenue.Funnel view. Funnel's 'Sync' branch is\n  -- just `Account WHERE SyncTypeRegion != ''` re-dated to SignupDate, but its\n  -- SELECT list hand-enumerates the Att_* columns and omits Att_Email — so every\n  -- email-attributed sync silently lost its weight (5.6 of 230 rows in Jul 2026).\n  -- Reading Account directly restores Att_Email. Verified bit-identical to the\n  -- Funnel path on all 17 shared channels across 5,078 days / 214 months.\n  SELECT SignupDate AS event_date, 'syncs' AS metric, channel, weight FROM (\n    SELECT SignupDate,\n      Att_SEO, Att_Pay_Per_Click, Att_OPN_Other_Peoples_Networks, Att_Direct,\n      Att_None, Att_Email, Att_Partners, Att_Content, Att_Social, Att_Other,\n      Att_Referral_Link, Att_Referral_Program, Att_Remarketing, Att_Backlinks,\n      Att_Banner_Ads, Att_Help_Center, Att_Online_Chat_Tool, Att_Seminar_Conference\n    FROM {{ source('revenue', 'Account') }}\n    WHERE IsConversionException = FALSE\n      AND Partner != 'Method Integration'\n      AND SignupDate != DATE('0001-01-01')\n      AND SyncTypeRegion != ''\n  )\n  UNPIVOT (weight FOR channel IN (\n    Att_SEO AS 'SEO', Att_Pay_Per_Click AS 'PPC',\n    Att_OPN_Other_Peoples_Networks AS 'OPN', Att_Direct AS 'Direct',\n    Att_None AS 'None', Att_Email AS 'Email', Att_Partners AS 'Partners',\n    Att_Content AS 'Content', Att_Social AS 'Social', Att_Other AS 'Other',\n    Att_Referral_Link AS 'Referral', Att_Referral_Program AS 'Referral_Program',\n    Att_Remarketing AS 'Remarketing', Att_Backlinks AS 'Backlinks',\n    Att_Banner_Ads AS 'Banner_Ads', Att_Help_Center AS 'Help_Center',\n    Att_Online_Chat_Tool AS 'Online_Chat', Att_Seminar_Conference AS 'Seminar'))\n  WHERE weight <> 0\n)\n\nSELECT event_date, metric, channel, SUM(weight) AS weight\nFROM (SELECT * FROM trial_rows UNION ALL SELECT * FROM sync_rows)\nGROUP BY event_date, metric, channel\n","tests":["accepted_values(metric)"]},{"name":"int_channel_funnel_trajectory","alias":"int_channel_funnel_trajectory","relation_name":"`project-for-method-dw`.`revenue`.`int_channel_funnel_trajectory`","description":"Per channel × metric (trials/syncs/sync_rate): current-month MTD (excl today), calendar-day linear trajectory, prior-month full, last-year full, and YoY/MoM %. Fractional multi-touch attribution. Trials from Account (SignupDate); syncs from Account where SyncTypeRegion != '', also dated by SignupDate (deliberately NOT CustDatFirstSyncCompleted). Recomputes live against CURRENT_DATE(). Backs the Channel Trajectory scorecard (Labs); status pending, not flipped to live. Parity-verified against the Looker Marketing Dashboard PDF (Jul 1–8 2026).\n\n⚠️ Use with care:\n- Trajectory is a linear run-rate projection (mtd/days_elapsed*days_in_month)\n  — noisy early in the month when few days have elapsed.\n\n- Fractional multi-touch attribution means channel values can be non-integers\n  (an account touched by PPC+SEO contributes 0.5 to each; e.g. 14.5 syncs).\n\n- Backlinks is rolled INTO SEO (requested 2026-08-10 — backlinks are SEO work\n  and were never wanted as their own line). This is a deliberate deviation\n  from Looker, which keeps Backlinks separate: SEO here runs ~0.5–2.0 above\n  Looker's SEO in the ~9-in-24 months where Backlinks is nonzero. The\n  breakout is still available in int_channel_funnel_daily.\n\n- Syncs read Account directly rather than the revenue.Funnel view. Funnel's\n  EventType='Sync' branch is the same filter re-dated to SignupDate, but its\n  SELECT list omits Att_Email, so it silently dropped every email-attributed\n  sync (5.6 of 230 rows in Jul 2026). Fixed 2026-08-10; before that date,\n  Email syncs and Email sync_rate were null and the syncs Total was\n  understated. Verified bit-identical to the Funnel path on the 17 shared\n  channels across 5,078 days / 214 months.\n\n- A sync is dated by SignupDate but gated on SyncTypeRegion, which flips when\n  the account eventually syncs. Historical sync months therefore keep growing\n  retroactively — the same window returns different numbers on different\n  days, and any \"matches Looker\" claim holds only at a single instant.\n\n- Trajectory method is calendar-day linear, NOT the prior-month-shape method\n  used for Net SaaS.\n","original_file_path":"models/marketing/int_channel_funnel_trajectory.sql","refs":[],"sources":["Account","v_trials_forecast_channel","v_syncs_forecast_channel"],"columns":[{"name":"metric","description":"trials | syncs | sync_rate"},{"name":"channel","description":"Attribution channel (fractional multi-touch)."},{"name":"mtd_actual","description":"Month-to-date actual excluding today."},{"name":"trajectory","description":"Calendar-day linear projection of the full current month (mtd / days_elapsed * days_in_month)."},{"name":"prior_month_full","description":"Prior calendar month, full."},{"name":"last_year_full","description":"Same month last year, full."},{"name":"forecast","description":"Current-month per-channel forecast (from v_trials/v_syncs_forecast_channel; sync_rate = sync_fcst/trial_fcst). Matches Looker's per-channel Forecast."},{"name":"yoy_pct","description":"(trajectory - last_year_full) / last_year_full."},{"name":"mom_pct","description":"(trajectory - prior_month_full) / prior_month_full."},{"name":"fcst_pct","description":"(trajectory - forecast) / forecast — trajectory-vs-forecast attainment."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Channel Trajectory: per channel × metric (trials / syncs / sync_rate) —\n-- current-month MTD (excl today), calendar-day linear trajectory, prior-month\n-- full, last-year full, and YoY / MoM %.\n--\n-- Attribution is FRACTIONAL multi-touch: Att_* are already fractional weights,\n-- summed directly (do NOT normalize, do NOT collapse to one channel).\n-- Trials  = revenue.Account by SignupDate.\n-- Syncs   = revenue.Account where SyncTypeRegion != '', also by SignupDate (this\n--           is what revenue.Funnel's EventType='Sync' branch resolves to; we read\n--           Account directly because the Funnel view drops Att_Email). NOT\n--           CustDatFirstSyncCompleted. Because a sync is dated by SignupDate but\n--           gated on a field that flips later, historical sync months keep growing.\n-- Backlinks is rolled into SEO (see the events CTE).\n-- Window  = [DATE_TRUNC(CURRENT_DATE(),MONTH), CURRENT_DATE())  (MTD excl today).\n-- Trajectory = MTD / days_elapsed * days_in_month  (calendar-day linear run-rate).\n\nWITH cal AS (\n  SELECT\n    CURRENT_DATE() AS today,\n    DATE_TRUNC(CURRENT_DATE(), MONTH) AS m_start,\n    DATE_TRUNC(DATE_SUB(CURRENT_DATE(), INTERVAL 1 MONTH), MONTH) AS pm_start,\n    DATE_TRUNC(DATE_SUB(CURRENT_DATE(), INTERVAL 12 MONTH), MONTH) AS ly_start,\n    DATE_TRUNC(DATE_SUB(CURRENT_DATE(), INTERVAL 11 MONTH), MONTH) AS ly_next_start,\n    DATE_DIFF(CURRENT_DATE(), DATE_TRUNC(CURRENT_DATE(), MONTH), DAY) AS days_elapsed,\n    EXTRACT(DAY FROM LAST_DAY(CURRENT_DATE())) AS days_in_month\n),\n\ntrial_rows AS (\n  -- NB: Account's native column is `SignUpDate` (capital U); we rely on\n  -- BigQuery's case-insensitive column resolution. Funnel's is `SignupDate`.\n  SELECT SignupDate AS d, channel, weight FROM (\n    SELECT SignupDate,\n      Att_SEO, Att_Pay_Per_Click, Att_OPN_Other_Peoples_Networks, Att_Direct,\n      Att_None, Att_Email, Att_Partners, Att_Content, Att_Social, Att_Other,\n      Att_Referral_Link, Att_Referral_Program, Att_Remarketing, Att_Backlinks,\n      Att_Banner_Ads, Att_Help_Center, Att_Online_Chat_Tool, Att_Seminar_Conference\n    FROM {{ source('revenue', 'Account') }}\n    WHERE IsConversionException = FALSE\n      AND Partner != 'Method Integration'\n      AND SignupDate != DATE('0001-01-01')\n  )\n  UNPIVOT (weight FOR channel IN (\n    Att_SEO AS 'SEO', Att_Pay_Per_Click AS 'PPC',\n    Att_OPN_Other_Peoples_Networks AS 'OPN', Att_Direct AS 'Direct',\n    Att_None AS 'None', Att_Email AS 'Email', Att_Partners AS 'Partners',\n    Att_Content AS 'Content', Att_Social AS 'Social', Att_Other AS 'Other',\n    Att_Referral_Link AS 'Referral', Att_Referral_Program AS 'Referral_Program',\n    Att_Remarketing AS 'Remarketing', Att_Backlinks AS 'Backlinks',\n    Att_Banner_Ads AS 'Banner_Ads', Att_Help_Center AS 'Help_Center',\n    Att_Online_Chat_Tool AS 'Online_Chat', Att_Seminar_Conference AS 'Seminar'))\n  WHERE weight <> 0\n),\n\nsync_rows AS (\n  -- Sourced from Account, NOT the revenue.Funnel view — Funnel's 'Sync' branch is\n  -- this exact filter re-dated to SignupDate, but its SELECT list omits Att_Email\n  -- and so drops every email-attributed sync. See int_channel_funnel_daily.\n  SELECT SignupDate AS d, channel, weight FROM (\n    SELECT SignupDate,\n      Att_SEO, Att_Pay_Per_Click, Att_OPN_Other_Peoples_Networks, Att_Direct,\n      Att_None, Att_Email, Att_Partners, Att_Content, Att_Social, Att_Other,\n      Att_Referral_Link, Att_Referral_Program, Att_Remarketing, Att_Backlinks,\n      Att_Banner_Ads, Att_Help_Center, Att_Online_Chat_Tool, Att_Seminar_Conference\n    FROM {{ source('revenue', 'Account') }}\n    WHERE IsConversionException = FALSE\n      AND Partner != 'Method Integration'\n      AND SignupDate != DATE('0001-01-01')\n      AND SyncTypeRegion != ''\n  )\n  UNPIVOT (weight FOR channel IN (\n    Att_SEO AS 'SEO', Att_Pay_Per_Click AS 'PPC',\n    Att_OPN_Other_Peoples_Networks AS 'OPN', Att_Direct AS 'Direct',\n    Att_None AS 'None', Att_Email AS 'Email', Att_Partners AS 'Partners',\n    Att_Content AS 'Content', Att_Social AS 'Social', Att_Other AS 'Other',\n    Att_Referral_Link AS 'Referral', Att_Referral_Program AS 'Referral_Program',\n    Att_Remarketing AS 'Remarketing', Att_Backlinks AS 'Backlinks',\n    Att_Banner_Ads AS 'Banner_Ads', Att_Help_Center AS 'Help_Center',\n    Att_Online_Chat_Tool AS 'Online_Chat', Att_Seminar_Conference AS 'Seminar'))\n  WHERE weight <> 0\n),\n\n-- Backlinks rolls up INTO SEO for reporting. Requested 2026-08-10: backlinks are\n-- SEO work and were never wanted as their own line. Deliberate deviation from the\n-- Looker report, which keeps them separate — so this view's SEO runs ~0.5–2.0\n-- above Looker's SEO in the ~9-in-24 months where Backlinks is nonzero. The\n-- breakout survives in int_channel_funnel_daily if it's ever needed back.\nevents AS (\n  SELECT 'trials' AS metric, d, IF(channel = 'Backlinks', 'SEO', channel) AS channel, weight FROM trial_rows\n  UNION ALL\n  SELECT 'syncs' AS metric, d, IF(channel = 'Backlinks', 'SEO', channel) AS channel, weight FROM sync_rows\n),\n\nagg AS (\n  SELECT\n    e.metric, e.channel,\n    SUM(CASE WHEN e.d >= c.m_start  AND e.d < c.today        THEN e.weight END) AS mtd,\n    SUM(CASE WHEN e.d >= c.pm_start AND e.d < c.m_start       THEN e.weight END) AS prior_full,\n    SUM(CASE WHEN e.d >= c.ly_start AND e.d < c.ly_next_start THEN e.weight END) AS ly_full,\n    ANY_VALUE(c.days_elapsed)  AS days_elapsed,\n    ANY_VALUE(c.days_in_month) AS days_in_month\n  FROM events e CROSS JOIN cal c\n  GROUP BY e.metric, e.channel\n),\n\n-- Per-channel forecast for the CURRENT month (Looker parity). method_forecast\n-- itself is month-level only; these views carry the channel split.\nfcst AS (\n  SELECT 'trials' AS metric, f.AttributionChannel AS channel, f.forecast_value AS forecast\n  FROM {{ source('revenue', 'v_trials_forecast_channel') }} f, cal c\n  WHERE f.forecast_date = c.m_start\n  UNION ALL\n  SELECT 'syncs', f.AttributionChannel, f.forecast_value\n  FROM {{ source('revenue', 'v_syncs_forecast_channel') }} f, cal c\n  WHERE f.forecast_date = c.m_start\n),\n\nbase AS (\n  SELECT\n    a.metric, a.channel, a.mtd, a.prior_full, a.ly_full,\n    CASE WHEN a.days_elapsed > 0 THEN a.mtd / a.days_elapsed * a.days_in_month END AS trajectory,\n    f.forecast\n  FROM agg a\n  LEFT JOIN fcst f USING (metric, channel)\n),\n\nrate AS (\n  SELECT\n    'sync_rate' AS metric, t.channel,\n    SAFE_DIVIDE(s.mtd, t.mtd)               AS mtd,\n    SAFE_DIVIDE(s.prior_full, t.prior_full) AS prior_full,\n    SAFE_DIVIDE(s.ly_full, t.ly_full)       AS ly_full,\n    SAFE_DIVIDE(s.trajectory, t.trajectory) AS trajectory,\n    SAFE_DIVIDE(s.forecast, t.forecast)     AS forecast\n  FROM (SELECT * FROM base WHERE metric = 'trials') t\n  LEFT JOIN (SELECT * FROM base WHERE metric = 'syncs') s USING (channel)\n),\n\nunioned AS (\n  SELECT metric, channel, mtd, trajectory, prior_full, ly_full, forecast FROM base\n  UNION ALL\n  SELECT metric, channel, mtd, trajectory, prior_full, ly_full, forecast FROM rate\n)\n\nSELECT\n  metric,\n  channel,\n  mtd                                            AS mtd_actual,\n  trajectory,\n  prior_full                                     AS prior_month_full,\n  ly_full                                        AS last_year_full,\n  forecast,\n  SAFE_DIVIDE(trajectory - ly_full, ly_full)     AS yoy_pct,\n  SAFE_DIVIDE(trajectory - prior_full, prior_full) AS mom_pct,\n  SAFE_DIVIDE(trajectory - forecast, forecast)   AS fcst_pct\nFROM unioned\n","tests":["accepted_values(metric)"]},{"name":"int_consultant_work","alias":"int_consultant_work","relation_name":"`project-for-method-dw`.`revenue`.`int_consultant_work`","description":"Consultant-delivered work, one row per time entry. Answers \"who did what on which account, and for how long\" without the caller needing to know the TimeTracking vs Activity grain trap: EntityRecordID is the METHOD STAFF MEMBER on TimeTracking but the CUSTOMER on Activity, so joining the wrong one to Entity silently yields customer names in a consultant column. Excludes internal attendance rows (IsAttendenceEntry — clock-in/out, no account) and deleted rows. Entries with no account FK are kept with a NULL account so consultant hour totals still reconcile against raw TimeTracking. Grading fields (AdminRating, ValidatedBy, AdminComments) are NOT here — PSDEPChecklistEntry is not yet mirrored to BigQuery.\n","original_file_path":"models/intermediate/int_consultant_work.sql","refs":[],"sources":["TimeTracking","Entity","Account"],"columns":[{"name":"time_tracking_record_id","description":"PK. TimeTracking.RecordID."},{"name":"consultant_record_id","description":"Method staff member who logged the time. TimeTracking.EntityRecordID."},{"name":"consultant","description":"Staff member's display name, from Entity.EntityFullName."},{"name":"account_record_id","description":"Method account the work was billed to. TimeTracking.MethodCompanyAccountRecordID, joins Account.RecordID. NULL on ~5% of non-attendance entries.\n"},{"name":"company_account","description":"Account slug from Account.CompanyAccount. NULL when account_record_id is NULL."},{"name":"customer_record_id","description":"Customer entity the work was for. TimeTracking.CustomerRecordID."},{"name":"txn_date","description":"Date the work was performed."},{"name":"txn_month","description":"Month start of txn_date, for period rollups."},{"name":"hours","description":"DurationHours + DurationMinutes/60, rounded to 4 decimals. Excludes BreakMinutes."},{"name":"method_support_type","description":"Care model the entry was logged under (e.g. Dedicated)."},{"name":"billable_status","description":"Billing state of the entry (e.g. HasBeenBilled)."},{"name":"is_invoiced","description":"Whether the entry has been invoiced."},{"name":"is_demo","description":"Whether the entry was logged as a demo."},{"name":"case_record_id","description":"Case the time was logged against, when any."},{"name":"activity_record_id","description":"Activity the time was logged against, when any."},{"name":"notes","description":"Free-text note body. Richest qualitative record of what was actually done — consultants paste checklist bodies and session summaries here.\n"}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Consultant-delivered work, one row per time entry. Built for consultant grading /\n-- service-quality review, where the question is \"who did what on which account, and\n-- for how long\".\n--\n-- GRAIN TRAP this model exists to hide: TimeTracking.EntityRecordID is the METHOD\n-- STAFF MEMBER, while Activity.EntityRecordID is the CUSTOMER. Joining the wrong one\n-- to Entity silently returns customer names in a column labelled \"consultant\".\n--\n-- Excludes internal attendance rows (clock-in/out, no account) and deleted rows.\n-- Entries with no account FK are KEPT with a NULL account so consultant hour totals\n-- still reconcile against raw TimeTracking.\n\nSELECT\n  t.RecordID                                            AS time_tracking_record_id,\n\n  -- who did the work\n  t.EntityRecordID                                      AS consultant_record_id,\n  e.EntityFullName                                      AS consultant,\n\n  -- what they worked on\n  t.MethodCompanyAccountRecordID                        AS account_record_id,\n  a.CompanyAccount                                      AS company_account,\n  t.CustomerRecordID                                    AS customer_record_id,\n\n  -- when, and how much\n  DATE(t.TxnDate)                                       AS txn_date,\n  DATE_TRUNC(DATE(t.TxnDate), MONTH)                    AS txn_month,\n  ROUND(t.DurationHours + t.DurationMinutes / 60.0, 4)  AS hours,\n\n  -- how the work was classified\n  t.MethodSupportType                                   AS method_support_type,\n  t.BillableStatus                                      AS billable_status,\n  t.IsInvoiced                                          AS is_invoiced,\n  t.IsDemo                                              AS is_demo,\n\n  -- links back out\n  t.CaseRecordID                                        AS case_record_id,\n  t.ActivityRecordID                                    AS activity_record_id,\n  t.Notes                                               AS notes\n\nFROM {{ source('revenue', 'TimeTracking') }} t\nINNER JOIN {{ source('revenue', 'Entity') }} e\n  ON e.RecordID = t.EntityRecordID\nLEFT JOIN {{ source('revenue', 'Account') }} a\n  ON a.RecordID = t.MethodCompanyAccountRecordID\nWHERE COALESCE(t.IsDeleted, FALSE) = FALSE\n  AND COALESCE(t.IsAttendenceEntry, FALSE) = FALSE\n","tests":["not_null(consultant)","not_null(consultant_record_id)","not_null(hours)","not_null(time_tracking_record_id)","not_null(txn_date)","not_null(txn_month)","unique(time_tracking_record_id)"]},{"name":"int_cookie_clicks","alias":"int_cookie_clicks","relation_name":"`project-for-method-dw`.`revenue`.`int_cookie_clicks`","description":"One row per marketing click, with the channel corrected in dbt and AIO\npromoted to a top-level channel.\n\nWHY IT EXISTS. Channel classification happens upstream in Alocet: ~60\nordered rules in CampaignCookieSplitRules, applied by\nCookieClickSplitterService, landing in CampaignAdjustedRecordID and\nSplitAdjusted. Those columns only began syncing to BigQuery on\n2026-09-01. The rule engine has three defects this model corrects\nwithout waiting on a data edit to a production table that has no review\nstep:\n\n  1. Gemini invisible. Rule 23 matches any referrer containing \"google\"\n     at RuleOrder 130; every AI rule sits at 194+ and first match wins.\n     245 Gemini clicks in 2026 were credited to SEO.\n  2. Perplexity labelled \"ChatGPT\" (rule 70 sets the wrong SetSplitTo).\n  3. AI traffic with no referrer lands in Direct. Rule 15 catches blank\n     referrers at RuleOrder 50, before the UTM-based AI rules at 199+.\n     514 AI clicks in 2026 were credited to Direct.\n\nThe rules table stays the operational source for Slack alerts and the\nAtt_* stamp. This model is the reporting truth, and it is versioned,\nreviewable and retroactive over the full click history back to 2013.\n\nWHAT AIO MEANS. A click whose referrer or landing URL names an AI\nassistant. Captures referred and UTM-tagged traffic. It CANNOT capture\nGoogle AI Overview clicks, which arrive as ordinary google.com organic\nwith nothing to distinguish them. AIO is therefore a floor, not a total.\n\nSIZE, so nobody is surprised. 2,036 AIO clicks in 2026 (0.24% of all\nclicks). As a top-level channel it ranks around tenth, below Reddit Ads\nand Cold Outbound. Volume by year: 625 (2023), 1,714 (2024), 5,246\n(2025), 2,036 (2026 to date). The apparent decline after 2025 is most\nlikely measurement loss — assistants increasingly strip the referrer, so\nthose visits fall into Direct with nothing to match on — rather than a\nreal fall in AI-sourced traffic. Do not present it as a downward trend.\n\nGRAIN: one row per click. Deleted and IsToBeIgnored clicks excluded.\n","original_file_path":"models/marketing/int_cookie_clicks.sql","refs":[],"sources":["CampaignCookieClicks","Campaign"],"columns":[{"name":"click_id","description":"CampaignCookieClicks.RecordID. Unique per click."},{"name":"cookie_id","description":"CampaignCookie.RecordID. Joins to Account.CookieRecordID to reach a customer."},{"name":"clicked_at","description":"Click timestamp, UTC."},{"name":"clicked_date","description":"Click date, UTC."},{"name":"referrer","description":"Referring URL as captured by the pixel. Blank for direct and for assistants that strip it."},{"name":"landing_url","description":"The page the pixel fired on. Carries UTM parameters where present."},{"name":"keyword","description":"Campaign keyword token. \"DirectSEO\" is the catch-all for untagged organic and direct."},{"name":"browser_info","description":"User agent string. The upstream catch-all rule matches on this."},{"name":"campaign_raw_id","description":"CampaignRecordID before the split rules ran. Frequently null for organic traffic."},{"name":"campaign_adjusted_id","description":"CampaignRecordID after the split rules ran. Populated on 99.9% of 2026 clicks."},{"name":"adjusted_by_rule_id","description":"Which CampaignCookieSplitRules row set the adjusted campaign. Lineage, but not immutable — editing a rule changes what that id means retroactively."},{"name":"override_id","description":"Manual human correction. Rare (111 rows in 2026) and deliberate; do not discard."},{"name":"split_adjusted","description":"The sub-channel label the rule engine assigned. Unreliable for AI — Perplexity is labelled ChatGPT and Gemini is labelled Google. Prefer ai_source."},{"name":"channel_as_recorded","description":"Channel name from the rule engine, before AIO promotion. Use to reconcile against Looker and the Slack alerts."},{"name":"ai_source","description":"Which assistant the click came from: ChatGPT, Perplexity, Gemini, Claude, Copilot, or Other AI. NULL for non-AI clicks. Derived from referrer and landing URL, independent of the upstream rules.\n"},{"name":"is_aio","description":"TRUE when ai_source is set."},{"name":"channel","description":"The corrected top-level channel. AIO where is_aio, otherwise channel_as_recorded unchanged. This is the column reporting should group by.\n"},{"name":"channel_before_aio_promotion","description":"For AIO clicks, the channel they were previously credited to. Makes the promotion auditable and lets you quantify what SEO and Direct lose. NULL for non-AIO clicks."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- One row per marketing click, with the channel CORRECTED in dbt.\n--\n-- WHY THIS MODEL EXISTS\n--\n-- Channel classification for clicks happens in Alocet: a table of ~60 ordered\n-- rules (CampaignCookieSplitRules) is applied by CookieClickSplitterService,\n-- and the result lands in CampaignAdjustedRecordID / SplitAdjusted. Those\n-- columns finally reach BigQuery as of 2026-09-01.\n--\n-- That rule engine has three defects we can correct here rather than wait on:\n--\n--   1. Gemini is invisible. Rule 23 matches any referrer containing \"google\"\n--      at RuleOrder 130; every AI rule sits at 194 or later and first match\n--      wins. Result: 245 Gemini clicks in 2026 credited to SEO.\n--   2. Perplexity is labelled \"ChatGPT\". Rule 70 sets the wrong SetSplitTo.\n--   3. AI traffic with no referrer lands in Direct. Rule 15 catches blank\n--      referrers at RuleOrder 50, before the UTM-based AI rules at 199+.\n--      Result: 514 AI clicks in 2026 credited to Direct.\n--\n-- Correcting in dbt rather than in the rules table is deliberate. It is\n-- versioned, reviewable, retroactive over the full 13-year click history, and\n-- it does not require write access to a production table that has no review\n-- step. The rules table stays the operational source for the Slack alerts and\n-- the Att_* stamp; this model is the reporting truth.\n--\n-- WHAT AIO MEANS HERE\n--\n-- A click whose referrer or landing URL names an AI assistant. That captures\n-- referred traffic and UTM-tagged traffic. It CANNOT capture Google AI\n-- Overview clicks, which arrive as ordinary google.com organic with nothing to\n-- distinguish them -- so AIO is a floor, not a total.\n--\n-- GRAIN: one row per click. Deleted and ignored clicks are excluded.\n\nwith clicks as (\n\n    select\n        cc.RecordID                     as click_id,\n        cc.CampaignCookieRecordID       as cookie_id,\n        cc.CreatedDate                  as clicked_at,\n        cc.Referrer                     as referrer,\n        cc.URL                          as landing_url,\n        cc.Keyword                      as keyword,\n        cc.BrowserInfo                  as browser_info,\n        cc.CampaignRecordID             as campaign_raw_id,\n        cc.CampaignAdjustedRecordID     as campaign_adjusted_id,\n        nullif(cc.SplitAdjusted, '')    as split_adjusted,\n        cc.AdjustedByRuleRecordID       as adjusted_by_rule_id,\n        cc.OverrideRecordID             as override_id,\n        lower(concat(ifnull(cc.Referrer, ''), ' ', ifnull(cc.URL, ''))) as match_text\n    from {{ source('marketing', 'CampaignCookieClicks') }} cc\n    where coalesce(cc.IsDeleted, false) = false\n      and coalesce(cc.IsToBeIgnored, false) = false\n\n), classified as (\n\n    select\n        c.*,\n        cam.Name as channel_as_recorded,\n\n        -- Which assistant, if any. Ordered so the specific wins over generic.\n        case\n            when regexp_contains(c.match_text, r'chatgpt|openai')   then 'ChatGPT'\n            when regexp_contains(c.match_text, r'perplexity')       then 'Perplexity'\n            when regexp_contains(c.match_text, r'gemini')           then 'Gemini'\n            when regexp_contains(c.match_text, r'claude')           then 'Claude'\n            when regexp_contains(c.match_text, r'copilot')          then 'Copilot'\n            when regexp_contains(c.match_text, r'deepseek|grok\\.|mistral|meta\\.ai|you\\.com|poe\\.com')\n                                                                    then 'Other AI'\n        end as ai_source\n\n    from clicks c\n    -- Coalesce adjusted -> raw before joining the campaign name.\n    --\n    -- CampaignAdjustedRecordID only began syncing to BigQuery on 2026-09-01 and\n    -- carries no history: it is 100% NULL before 2025, which left `channel`\n    -- NULL on every pre-2025 click. The raw CampaignRecordID has always been\n    -- present, so falling back to it recovers the channel for tagged traffic\n    -- across the full history.\n    --\n    -- This matches what the upstream engine already does:\n    -- MultiTouchAttribution.cs:255 selects\n    -- ISNULL(CampaignAdjustedRef, CampaignRef) AS CampaignAdjustedRef.\n    --\n    -- Untagged pre-2025 clicks (campaign 0, never adjusted) still resolve to\n    -- NULL, because nothing recorded what they were. That is a genuine gap,\n    -- not a join bug. is_aio is unaffected either way: it is derived from the\n    -- referrer and URL, never from the campaign.\n    left join {{ source('marketing', 'Campaign') }} cam\n      on cam.RecordID = coalesce(nullif(c.campaign_adjusted_id, 0),\n                                 nullif(c.campaign_raw_id, 0))\n\n)\n\nselect\n    click_id,\n    cookie_id,\n    clicked_at,\n    date(clicked_at)            as clicked_date,\n    referrer,\n    landing_url,\n    keyword,\n    browser_info,\n    campaign_raw_id,\n    campaign_adjusted_id,\n    adjusted_by_rule_id,\n    override_id,\n    split_adjusted,\n\n    channel_as_recorded,\n    ai_source,\n    ai_source is not null       as is_aio,\n\n    -- The corrected top-level channel. AIO is promoted out of whatever the\n    -- rule engine credited it to; everything else passes through untouched.\n    case\n        when ai_source is not null then 'AIO'\n        else channel_as_recorded\n    end                         as channel,\n\n    -- Kept so the promotion is auditable: which channel each AIO click was\n    -- taken from. NULL for non-AIO clicks.\n    case when ai_source is not null then channel_as_recorded end\n                                as channel_before_aio_promotion\n\nfrom classified\n","tests":[]},{"name":"int_customer_annual_mrr","alias":"int_customer_annual_mrr","relation_name":"`project-for-method-dw`.`revenue`.`int_customer_annual_mrr`","description":"Per-customer annual-cohort MRR with movement classification. Pairs month M\nagainst M-12 (12-month offset), unlike int_customer_mrr which pairs M vs M-1.\nThis is the engine behind the live annual MRR-family metrics (#384–389).\n\nGrain: one row per (Month, EntityRecordID), customer level. A customer is an\nEntityRecordID (can own multiple CompanyAccounts). Movement is split into\nPARALLEL columns (NewMRR / Expansions / Downgrades / Cancellations), not a\nsingle movement_kind — a customer-month with all four = 0 is steady-state.\nDo not SUM the movement columns together expecting a net; combine per the\nmetric definitions.\n\nMigrated from an orphaned BQ view (2026-06-04) and materialized as a table for\nperformance. Parity-verified via scripts/parity_int_customer_mrr.py against the\npre-migration baseline. Methodology: CEO-confirmed symmetric Prepay-Expiry (PE)\nexclusion (2026-04-28; see memory project_annual_retention) — a customer-month\nwhose SaaS lines are entirely Prepay-Expiry Income is excluded from the base.\nExcludes test accounts (CompanyAccount m11%/m18%) and the current incomplete\nmonth.\n","original_file_path":"models/intermediate/int_customer_annual_mrr.sql","refs":["int_customers"],"sources":["TransLineFlattened"],"columns":[{"name":"Month","description":"First day of the month (the M side of the M vs M-12 pair)."},{"name":"EntityRecordID","description":"Stable numeric customer ID (customer grain). Use for temporal joins — not Company, which drifts on rename.\n"},{"name":"Company","description":"Representative CompanyAccount for the customer-month (largest by SaaSAmount, CompanyAccount ASC tiebreak). Coalesced from the current or prior-year side.\n"},{"name":"p1_saas","description":"Prior-year (M-12) SaaS total for the customer, entity-allocated. 0 when the customer had no M-12 activity (a new-cohort customer).\n"},{"name":"p2_saas","description":"Current-month (M) SaaS total for the customer, entity-allocated. 0 when the customer has churned out by M.\n"},{"name":"StartMRR","description":"Prior-year base MRR that counts toward the annual retention denominator (= p1_saas, unless the entire prior-year book was Prepay-Expiry, in which case 0 under the symmetric PE exclusion). Entity-allocated from the company-level classification.\n"},{"name":"Cancellations","description":"MRR lost to full churn over the year (had prior-year SaaS, zero current SaaS, not a PE-only base). Positive number representing the drop. Entity-allocated.\n"},{"name":"Downgrades","description":"MRR lost to partial contraction (current < prior, both > 0): p1_saas - p2_saas. Positive number representing the decrease. Entity-allocated.\n"},{"name":"Expansions","description":"MRR gained from existing customers (current > prior, both > 0): p2_saas - p1_saas. Entity-allocated.\n"},{"name":"NewMRR","description":"MRR from customers new in the annual window (zero prior-year SaaS, positive current SaaS). Entity-allocated.\n"},{"name":"Segment","description":"Product/pricing segment (Team AI Plus / 4+ no DEP / 2-3 no DEP / Solo no DEP), joined from int_customers. Taken from the M-12 month for existing customers and the M month for new customers. [REVIEW: confirm this as-of-start vs as-of-current dim join is the intended slice for annual movement breakdowns.]\n"},{"name":"UserTier","description":"Seat band (Solo / Small Team / Team) from int_customers, joined as above."},{"name":"HasDEP","description":"Per-customer-month DEP flag from int_customers (point-in-time), joined as above."},{"name":"AttributionChannel","description":"Single-touch acquisition channel from int_customers, joined as above."},{"name":"SignupCountry","description":"Founding-account signup country from int_customers, joined as above."},{"name":"Vertical","description":"Founding-account vertical from int_customers, joined as above."},{"name":"SyncType","description":"Founding-account sync type from int_customers, joined as above."}],"compiled_sql":"-- Migrated from the orphaned BQ view of the same name (2026-06-04).\n-- Original DDL captured in knowledge/verified-queries/int_customer_annual_mrr-pre-migration-ddl.sql.\n-- Parity-verified via scripts/parity_int_customer_mrr.py against the pre-migration baseline.\n--\n-- Annual cohort: pairs month M vs M-12 (12-month offset), unlike int_customer_mrr\n-- which pairs M vs M-1. Same symmetric Prepay-Expiry exclusion methodology\n-- (CEO-confirmed 2026-04-28; see memory: project_annual_retention).\n--\n-- Materialized as a TABLE for performance (the orphaned view was expensive to\n-- re-scan on every annual-metric query).\n--\n-- Grain: one row per (Month, EntityRecordID). Movement columns\n-- (NewMRR, Expansions, Downgrades, Cancellations) are PARALLEL — there is no\n-- single movement_kind dimension. A customer-month with all four = 0 is a\n-- steady-state row.\n\n{{ config(materialized='table') }}\n\nWITH entity_monthly AS (\n  SELECT\n    FORMAT_DATE('%Y-%m', TxnDate) AS month_str,\n    DATE_TRUNC(TxnDate, MONTH) AS Month,\n    EntityRecordID,\n    ARRAY_AGG(CompanyAccount ORDER BY SaaSAmount DESC, CompanyAccount ASC LIMIT 1)[OFFSET(0)] AS company,\n    SUM(SaaSAmount) AS total_saas,\n    COUNTIF(SaaSAmount != 0) AS saas_lines,\n    COUNTIF(SaaSAmount != 0 AND AccountFullName LIKE '%Prepay Expiry Income%') AS expiry_lines\n  FROM {{ source('revenue','TransLineFlattened') }}\n  WHERE TxnDate >= '2021-12-01'\n    AND FORMAT_DATE('%Y-%m', TxnDate) < FORMAT_DATE('%Y-%m', CURRENT_DATE())\n    AND CompanyAccount NOT LIKE 'm11%' AND CompanyAccount NOT LIKE 'm18%'\n  GROUP BY 1, 2, 3\n),\nentity_paired AS (\n  SELECT p2.Month, p2.month_str, p2.EntityRecordID,\n    COALESCE(p2.company, p1.company) AS Company,\n    COALESCE(p1.total_saas, 0) AS p1_saas, p2.total_saas AS p2_saas,\n    COALESCE(p1.expiry_lines, 0) AS p1_expiry_lines, COALESCE(p1.saas_lines, 0) AS p1_saas_lines\n  FROM entity_monthly p2\n  LEFT JOIN entity_monthly p1\n    ON p2.EntityRecordID = p1.EntityRecordID\n    AND p1.month_str = FORMAT_DATE('%Y-%m', DATE_SUB(p2.Month, INTERVAL 12 MONTH))\n  WHERE p2.month_str >= '2023-01'\n  UNION ALL\n  SELECT\n    DATE_ADD(p1.Month, INTERVAL 12 MONTH) AS Month,\n    FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 12 MONTH)) AS month_str,\n    p1.EntityRecordID, p1.company AS Company,\n    p1.total_saas AS p1_saas, 0 AS p2_saas,\n    p1.expiry_lines AS p1_expiry_lines, p1.saas_lines AS p1_saas_lines\n  FROM entity_monthly p1\n  LEFT JOIN entity_monthly p2\n    ON p1.EntityRecordID = p2.EntityRecordID\n    AND p2.month_str = FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 12 MONTH))\n  WHERE p2.EntityRecordID IS NULL\n    AND FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 12 MONTH)) < FORMAT_DATE('%Y-%m', CURRENT_DATE())\n    AND FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 12 MONTH)) >= '2023-01'\n),\ncompany_level AS (\n  SELECT month_str, Month, Company,\n    SUM(p1_saas) AS p1_saas, SUM(p2_saas) AS p2_saas,\n    SUM(p1_expiry_lines) AS p1_expiry_lines, SUM(p1_saas_lines) AS p1_saas_lines\n  FROM entity_paired GROUP BY 1, 2, 3\n),\ncompany_classified AS (\n  SELECT month_str, Month, Company, p1_saas, p2_saas,\n    CASE WHEN p1_saas > 0 AND NOT (p1_expiry_lines > 0 AND p1_expiry_lines = p1_saas_lines) THEN p1_saas ELSE 0 END AS StartMRR,\n    CASE WHEN p1_saas > 0 AND p2_saas = 0 AND NOT (p1_expiry_lines > 0 AND p1_expiry_lines = p1_saas_lines) THEN p1_saas ELSE 0 END AS Cancellations,\n    CASE WHEN p1_saas > 0 AND p2_saas > 0 AND p2_saas < p1_saas THEN p1_saas - p2_saas ELSE 0 END AS Downgrades,\n    CASE WHEN p1_saas > 0 AND p2_saas > p1_saas THEN p2_saas - p1_saas ELSE 0 END AS Expansions,\n    CASE WHEN p1_saas = 0 AND p2_saas > 0 THEN p2_saas ELSE 0 END AS NewMRR\n  FROM company_level\n)\nSELECT\n  ep.Month, ep.EntityRecordID, cc.Company,\n  CAST(ep.p1_saas AS NUMERIC) AS p1_saas,\n  CAST(ep.p2_saas AS NUMERIC) AS p2_saas,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.StartMRR * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS StartMRR,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.Cancellations * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS Cancellations,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.Downgrades * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS Downgrades,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.Expansions * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS Expansions,\n  CAST(CASE WHEN cc.p2_saas > 0 AND cc.p1_saas = 0 THEN cc.NewMRR * SAFE_DIVIDE(ep.p2_saas, cc.p2_saas) ELSE 0 END AS NUMERIC) AS NewMRR,\n  vc_dim.Segment, vc_dim.UserTier, vc_dim.HasDEP, vc_dim.AttributionChannel,\n  vc_dim.SignupCountry, vc_dim.Vertical, vc_dim.SyncType\nFROM entity_paired ep\nJOIN company_classified cc ON ep.month_str = cc.month_str AND ep.Company = cc.Company\nLEFT JOIN {{ ref('int_customers') }} vc_dim\n  ON vc_dim.EntityRecordID = ep.EntityRecordID\n  AND vc_dim.Month = CASE WHEN ep.p1_saas > 0 THEN DATE_SUB(ep.Month, INTERVAL 12 MONTH) ELSE ep.Month END\n","tests":["not_null(EntityRecordID)","not_null(Month)"]},{"name":"int_customer_firmographics","alias":"int_customer_firmographics","relation_name":"`project-for-method-dw`.`revenue`.`int_customer_firmographics`","description":"Firmographic dimension table, entity grain: one row per EntityRecordID, deduped from revenue.Account (~1.22 rows per entity — the dedup is the point of this model). Centralizes size-band / country / tenure derivations previously retyped ad-hoc. annual_sales is QuickBooks-derived: the most recent month's QB invoices + sales receipts x 12, refreshed nightly on active accounts and frozen at the last value when an account goes inactive. Caveats: (1) seasonality — it is one month x 12, so seasonal businesses are over/under-stated depending on the snapshot month; (2) no currency normalization — ~30% of the base is non-US, use is_us to control; (3) NULL means no synced invoice history, which is itself a low-engagement signal, not missing-at-random. Tenure gotchas: junk epoch dates (pre-2000 / future) are cleaned to NULL; do NOT use int_customer_mrr for tenure — its history starts too late to measure customer age. Directional intermediate, not a verified metric.\n","original_file_path":"models/intermediate/int_customer_firmographics.sql","refs":["int_customer_annual_mrr"],"sources":["Account"],"columns":[{"name":"EntityRecordID","description":"Customer-level primary key (one row per entity)."},{"name":"annual_sales","description":"Cleaned QB-derived annual sales: most recent month's invoices + sales receipts x 12. Junk filter keeps values between 1 and 1e10; everything else is NULL. Pre-FX — no currency normalization.\n"},{"name":"size_band","description":"Annual-sales band: <$500K, $500K-$1M, $1M-$5M, $5M-$10M, $10M+. NULL when annual_sales is NULL (no synced invoice history).\n"},{"name":"signup_country","description":"SignupCountry of the earliest-signup account (ties broken by RecordID), first non-null.\n"},{"name":"is_us","description":"TRUE when signup_country is United States / USA / US. FALSE otherwise, including when signup_country is NULL.\n"},{"name":"signup_date","description":"Earliest valid SignUpDate across the entity's accounts (2000-01-01 through today; epoch junk and future dates cleaned to NULL).\n"},{"name":"first_invoice_date","description":"Earliest valid FirstSaaSInvoiceTxnDate across the entity's accounts (2000-01-01 through today; junk cleaned to NULL). Basis for tenure.\n"},{"name":"tenure_years","description":"Years since first_invoice_date (month-precision, rounded to 2 decimals). NULL when first_invoice_date is NULL.\n"},{"name":"current_start_mrr","description":"Entity's StartMRR summed over the latest complete month in int_customer_annual_mrr (annual GRR base). NULL if not in that month's paying base.\n"},{"name":"mrr_rank_pct","description":"PERCENT_RANK of current_start_mrr (descending) over the latest complete month's paying base. 0 = largest customer. NULL if not in the base.\n"},{"name":"is_top30_mrr","description":"TRUE when mrr_rank_pct <= 0.30 (top 30% of the current paying base by MRR). FALSE otherwise, including entities not in the current base."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Entity-grain firmographic dims for customer-level analysis. One row per\n-- EntityRecordID, deduped from revenue.Account (~1.22 rows per entity).\n-- Centralizes derivations previously retyped ad-hoc: cleaned annual sales,\n-- size band, US flag, cleaned lifecycle dates, tenure, and current-MRR rank.\n\nWITH acct AS (\n  SELECT\n    EntityRecordID,\n    MAX(IF(CustDatAnnualSales BETWEEN 1 AND 1e10, CustDatAnnualSales, NULL)) AS annual_sales,\n    ARRAY_AGG(SignupCountry IGNORE NULLS ORDER BY SignUpDate, RecordID LIMIT 1)[SAFE_OFFSET(0)] AS signup_country,\n    MIN(IF(SignUpDate BETWEEN '2000-01-01' AND CURRENT_DATE(), SignUpDate, NULL)) AS signup_date,\n    MIN(IF(FirstSaaSInvoiceTxnDate BETWEEN '2000-01-01' AND CURRENT_DATE(), FirstSaaSInvoiceTxnDate, NULL)) AS first_invoice_date\n  FROM {{ source('revenue', 'Account') }}\n  WHERE EntityRecordID IS NOT NULL\n  GROUP BY 1\n),\ncurrent_mrr AS (  -- latest complete month's annual GRR base, for MRR ranking\n  SELECT EntityRecordID, SUM(StartMRR) AS start_mrr\n  FROM {{ ref('int_customer_annual_mrr') }}\n  WHERE Month = (SELECT MAX(Month) FROM {{ ref('int_customer_annual_mrr') }})\n    AND StartMRR > 0\n  GROUP BY 1\n),\nranked AS (\n  SELECT EntityRecordID, start_mrr,\n    PERCENT_RANK() OVER (ORDER BY start_mrr DESC) AS mrr_rank_pct\n  FROM current_mrr\n)\nSELECT\n  a.EntityRecordID,\n  a.annual_sales,\n  CASE\n    WHEN a.annual_sales IS NULL THEN NULL\n    WHEN a.annual_sales < 500000 THEN '<$500K'\n    WHEN a.annual_sales < 1000000 THEN '$500K-$1M'\n    WHEN a.annual_sales < 5000000 THEN '$1M-$5M'\n    WHEN a.annual_sales < 10000000 THEN '$5M-$10M'\n    ELSE '$10M+'\n  END AS size_band,\n  a.signup_country,\n  COALESCE(a.signup_country IN ('United States', 'USA', 'US'), FALSE) AS is_us,\n  a.signup_date,\n  a.first_invoice_date,\n  ROUND(DATE_DIFF(CURRENT_DATE(), a.first_invoice_date, MONTH) / 12, 2) AS tenure_years,\n  r.start_mrr AS current_start_mrr,\n  r.mrr_rank_pct,\n  COALESCE(r.mrr_rank_pct <= 0.30, FALSE) AS is_top30_mrr\nFROM acct a\nLEFT JOIN ranked r USING (EntityRecordID)\n","tests":["not_null(EntityRecordID)","unique(EntityRecordID)"]},{"name":"int_customer_mrr","alias":"int_customer_mrr","relation_name":"`project-for-method-dw`.`revenue`.`int_customer_mrr`","description":"Per-customer per-month MRR with movement classification. Migrated from the\norphaned BQ view 2026-06-03 (see knowledge/verified-queries/int_customer_mrr-pre-migration-ddl.sql).\nParity-verified via scripts/parity_int_customer_mrr.py (MRR math bit-identical to legacy).\nMethodology: CEO-confirmed symmetric Prepay-Expiry exclusion (2026-04-28).\nMovements are PARALLEL columns (NewMRR/Expansions/Downgrades/Cancellations), not a single movement_kind.\n","original_file_path":"models/intermediate/int_customer_mrr.sql","refs":["int_customers"],"sources":["TransLineFlattened"],"columns":[{"name":"Month","description":"First day of the month."},{"name":"EntityRecordID","description":"Stable numeric customer ID. Use for temporal joins (not Company, which drifts on rename)."},{"name":"p2_saas","description":"Current-month SaaS total for the entity (raw book; PE exclusion does not touch this)."}],"compiled_sql":"-- Migrated from the orphaned BQ view of the same name.\n-- Original DDL captured in knowledge/verified-queries/int_customer_mrr-pre-migration-ddl.sql.\n-- Parity-verified via scripts/parity_int_customer_mrr.py (Task 5 of the validation plan).\n--\n-- Methodology: CEO-confirmed symmetric Prepay Expiry exclusion (2026-04-28).\n-- See memory: project_annual_retention.\n--\n-- Grain: one row per (Month, EntityRecordID). Movement columns\n-- (NewMRR, Expansions, Downgrades, Cancellations) are PARALLEL — there is no\n-- single movement_kind dimension. A customer-month with all four = 0 is a\n-- steady-state row.\n\n{{ config(materialized='table') }}\n\nWITH entity_monthly AS (\n  SELECT\n    FORMAT_DATE('%Y-%m', TxnDate) AS month_str,\n    DATE_TRUNC(TxnDate, MONTH) AS Month,\n    EntityRecordID,\n    ARRAY_AGG(CompanyAccount ORDER BY SaaSAmount DESC, CompanyAccount ASC LIMIT 1)[OFFSET(0)] AS company,\n    SUM(SaaSAmount) AS total_saas,\n    COUNTIF(SaaSAmount != 0) AS saas_lines,\n    COUNTIF(SaaSAmount != 0 AND AccountFullName LIKE '%Prepay Expiry Income%') AS expiry_lines\n  FROM {{ source('revenue', 'TransLineFlattened') }}\n  WHERE TxnDate >= '2021-12-01'\n    AND FORMAT_DATE('%Y-%m', TxnDate) < FORMAT_DATE('%Y-%m', CURRENT_DATE())\n    AND CompanyAccount NOT LIKE 'm11%'\n    AND CompanyAccount NOT LIKE 'm18%'\n  GROUP BY 1, 2, 3\n),\nentity_paired AS (\n  SELECT p2.Month, p2.month_str, p2.EntityRecordID,\n    COALESCE(p2.company, p1.company) AS Company,\n    COALESCE(p1.total_saas, 0) AS p1_saas, p2.total_saas AS p2_saas,\n    COALESCE(p1.expiry_lines, 0) AS p1_expiry_lines,\n    COALESCE(p1.saas_lines, 0) AS p1_saas_lines\n  FROM entity_monthly p2\n  LEFT JOIN entity_monthly p1\n    ON p2.EntityRecordID = p1.EntityRecordID\n    AND p1.month_str = FORMAT_DATE('%Y-%m', DATE_SUB(p2.Month, INTERVAL 1 MONTH))\n  WHERE p2.month_str >= '2022-01'\n  UNION ALL\n  SELECT\n    DATE_ADD(p1.Month, INTERVAL 1 MONTH) AS Month,\n    FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 1 MONTH)) AS month_str,\n    p1.EntityRecordID, p1.company AS Company,\n    p1.total_saas AS p1_saas, 0 AS p2_saas,\n    p1.expiry_lines AS p1_expiry_lines, p1.saas_lines AS p1_saas_lines\n  FROM entity_monthly p1\n  LEFT JOIN entity_monthly p2\n    ON p1.EntityRecordID = p2.EntityRecordID\n    AND p2.month_str = FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 1 MONTH))\n  WHERE p2.EntityRecordID IS NULL\n    AND FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 1 MONTH)) < FORMAT_DATE('%Y-%m', CURRENT_DATE())\n    AND FORMAT_DATE('%Y-%m', DATE_ADD(p1.Month, INTERVAL 1 MONTH)) >= '2022-01'\n),\ncompany_level AS (\n  SELECT month_str, Month, Company,\n    SUM(p1_saas) AS p1_saas, SUM(p2_saas) AS p2_saas,\n    SUM(p1_expiry_lines) AS p1_expiry_lines, SUM(p1_saas_lines) AS p1_saas_lines\n  FROM entity_paired GROUP BY 1, 2, 3\n),\ncompany_classified AS (\n  SELECT month_str, Month, Company, p1_saas, p2_saas,\n    CASE WHEN p1_saas > 0 AND NOT (p1_expiry_lines > 0 AND p1_expiry_lines = p1_saas_lines) THEN p1_saas ELSE 0 END AS StartMRR,\n    CASE WHEN p1_saas > 0 AND p2_saas = 0 AND NOT (p1_expiry_lines > 0 AND p1_expiry_lines = p1_saas_lines) THEN p1_saas ELSE 0 END AS Cancellations,\n    CASE WHEN p1_saas > 0 AND p2_saas > 0 AND p2_saas < p1_saas THEN p1_saas - p2_saas ELSE 0 END AS Downgrades,\n    CASE WHEN p1_saas > 0 AND p2_saas > p1_saas THEN p2_saas - p1_saas ELSE 0 END AS Expansions,\n    CASE WHEN p1_saas = 0 AND p2_saas > 0 THEN p2_saas ELSE 0 END AS NewMRR\n  FROM company_level\n)\nSELECT\n  ep.Month, ep.EntityRecordID, cc.Company,\n  CAST(ep.p1_saas AS NUMERIC) AS p1_saas,\n  CAST(ep.p2_saas AS NUMERIC) AS p2_saas,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.StartMRR * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS StartMRR,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.Cancellations * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS Cancellations,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.Downgrades * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS Downgrades,\n  CAST(CASE WHEN cc.p1_saas > 0 THEN cc.Expansions * SAFE_DIVIDE(ep.p1_saas, cc.p1_saas) ELSE 0 END AS NUMERIC) AS Expansions,\n  CAST(CASE WHEN cc.p2_saas > 0 AND cc.p1_saas = 0 THEN cc.NewMRR * SAFE_DIVIDE(ep.p2_saas, cc.p2_saas) ELSE 0 END AS NUMERIC) AS NewMRR,\n  vc_dim.Segment, vc_dim.UserTier, vc_dim.HasDEP, vc_dim.AttributionChannel,\n  vc_dim.SignupCountry, vc_dim.Vertical, vc_dim.SyncType\nFROM entity_paired ep\nJOIN company_classified cc ON ep.month_str = cc.month_str AND ep.Company = cc.Company\nLEFT JOIN {{ ref('int_customers') }} vc_dim\n  ON vc_dim.EntityRecordID = ep.EntityRecordID\n  AND vc_dim.Month = CASE WHEN ep.p1_saas > 0 THEN DATE_SUB(ep.Month, INTERVAL 1 MONTH) ELSE ep.Month END\n","tests":["not_null(EntityRecordID)","not_null(Month)","not_null(p2_saas)"]},{"name":"int_customer_mrr_lines","alias":"int_customer_mrr_lines","relation_name":"`project-for-method-dw`.`revenue`.`int_customer_mrr_lines`","description":"Monthly line composition per customer-entity (month, entity, item) with qty (seats proxy), saas, and an is_discount flag. Mirrors int_customer_mrr's raw monthly book but keeps line detail. Validated 2026-06-03: rolls up bit-exact to int_customer_mrr's customer-month SaaS total (scripts/parity_customer_mrr_lines.py).\n","original_file_path":"models/intermediate/int_customer_mrr_lines.sql","refs":[],"sources":["TransLineFlattened"],"columns":[{"name":"month","description":"First of the calendar month (DATE_TRUNC of TxnDate)."},{"name":"entity_record_id","description":"Stable numeric entity id (join key; survives company renames)."},{"name":"item","description":"ItemFullName — the app/module (Service) or discount line."},{"name":"qty","description":"Sum of Qty on the line — proxy for paid users/seats."},{"name":"saas","description":"Sum of SaaSAmount on the line for the month."}],"compiled_sql":"-- VALIDATED 2026-06-03. Monthly line composition per customer-entity: one row per\n-- (month, entity, item). Mirrors int_customer_mrr's raw monthly book — bit-exact rollup\n-- verified via scripts/parity_customer_mrr_lines.py (177,241 (month,entity) pairs, $0.00).\n-- Feeds int_mrr_movement_decomposed.\n--\n-- Monthly line composition per customer-entity: one row per (month, entity, item).\n-- Mirrors int_customer_mrr's monthly book — SUM(SaaSAmount) by DATE_TRUNC(month),\n-- same filters — but KEEPS line detail so MRR movements can be split into\n-- seats (Qty) / apps (ItemFullName) / price (Rate, Discount lines).\n-- Feeds int_mrr_movement_decomposed.\n\n{{ config(materialized='table') }}\n\nselect\n    date_trunc(TxnDate, month)   as month,\n    EntityRecordID               as entity_record_id,\n    ItemFullName                 as item,\n    ItemType = 'Discount'        as is_discount,\n    sum(Qty)                     as qty,    -- proxy for seats / paid users on the line\n    max(UserPaidCount)           as user_paid_count,  -- per-line paid-user count; MAX in group = account paid users\n    sum(SaaSAmount)              as saas\nfrom {{ source('revenue', 'TransLineFlattened') }}\nwhere TxnDate >= '2021-12-01'\n  and format_date('%Y-%m', TxnDate) < format_date('%Y-%m', current_date())  -- exclude in-progress month\n  and CompanyAccount not like 'm11%'   -- exclude internal Method accounts (matches int_customer_mrr)\n  and CompanyAccount not like 'm18%'\ngroup by 1, 2, 3, 4\n","tests":["not_null(entity_record_id)","not_null(item)","not_null(month)"]},{"name":"int_customer_proserv","alias":"int_customer_proserv","relation_name":"`project-for-method-dw`.`revenue`.`int_customer_proserv`","description":"Professional-services / customization signal, entity grain. One row per customer that bought project hours (any PS-grouped billing line with positive gross). ps_gross = total PSBeforeDiscount; first_ps_date = earliest such line. Project-hours magnitude (delivered time) is NOT here — revenue.TimeTracking is empty in BQ. Directional input to the motion funnel.\n","original_file_path":"models/intermediate/int_customer_proserv.sql","refs":[],"sources":["TransLineFlattened"],"columns":[{"name":"EntityRecordID","description":""},{"name":"ps_gross","description":""},{"name":"is_customized","description":""}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- First professional-services / \"customization\" signal in dbt. Entity grain.\n-- Customization = the customer bought project hours = any PS-grouped billing line\n-- with positive gross. Uses PSBeforeDiscount (gross); PSAmount is net-of-discount\n-- and drifts. Project-HOURS magnitude is intentionally absent here — revenue.TimeTracking\n-- is empty in BQ (deferred to V2). is_customized is always TRUE in this view (presence\n-- = customized); downstream LEFT JOINs and COALESCE the flag to FALSE for everyone else.\n\nSELECT\n  EntityRecordID,\n  CAST(SUM(PSBeforeDiscount) AS NUMERIC) AS ps_gross,\n  MIN(TxnDate) AS first_ps_date,\n  TRUE AS is_customized\nFROM {{ source('revenue', 'TransLineFlattened') }}\nWHERE InvoiceGrouping = 'PS'\n  AND PSBeforeDiscount > 0\nGROUP BY 1\n","tests":["not_null(EntityRecordID)","not_null(is_customized)","not_null(ps_gross)","unique(EntityRecordID)"]},{"name":"int_customer_retention_triangle","alias":"int_customer_retention_triangle","relation_name":"`project-for-method-dw`.`revenue`.`int_customer_retention_triangle`","description":"Customer retention CUBE, customer grain (EntityRecordID). One row per (cohort_month, tenure_k, l1, segment, country, channel). Frontend sums the filtered slice and derives four views: Customers vs MRR, each From-start (active/start) or Previous-month (active/prior). Dims frozen at cohort start; l1 is current V7 classification (Multi-client or Unclassified bucket where no label exists). No in-model n_start threshold; apply at display time. Monthly cohorts; signup gate Trial >= 2021-06-01; right-censored at the latest complete month. Sources: int_customer_mrr + v7_classification.v_entity_primary_label.\n","original_file_path":"models/intermediate/int_customer_retention_triangle.sql","refs":["int_customer_mrr"],"sources":["Funnel","v_entity_primary_label"],"columns":[{"name":"cohort_month","description":""},{"name":"tenure_k","description":""},{"name":"l1","description":""},{"name":"segment","description":""},{"name":"country","description":""},{"name":"channel","description":""},{"name":"n_start","description":""},{"name":"n_active","description":""},{"name":"mrr_start","description":""}],"compiled_sql":"{{ config(materialized='table') }}\n\n-- Customer retention CUBE: monthly cohorts x tenure x (l1, segment, country, channel).\n-- Customer grain (EntityRecordID). Dims frozen at cohort start; l1 is current classification.\n-- Additive measures: the frontend sums the filtered slice and derives the four views.\n-- No in-model n_start threshold: the cube is complete so the \"All\" rollup is exact;\n-- the min-cohort threshold is applied at display time in the frontend.\n\nWITH monthly_mrr AS (\n  SELECT Month, EntityRecordID, SUM(StartMRR) AS mrr\n  FROM {{ ref('int_customer_mrr') }}\n  GROUP BY 1, 2\n),\nsignup AS (\n  SELECT EntityRecordID, MIN(Date) AS sd\n  FROM {{ source('revenue', 'Funnel') }}\n  WHERE EventType = 'Trial'\n  GROUP BY 1\n),\nfirst_pay AS (\n  SELECT EntityRecordID, MIN(Month) AS cohort_month\n  FROM monthly_mrr WHERE mrr > 0 GROUP BY 1\n),\ndims AS (  -- cohort-start attributes, one row per entity at its first paying month\n  SELECT\n    d.EntityRecordID,\n    COALESCE(d.Segment, '(unknown)') AS segment,\n    COALESCE(d.SignupCountry, '(unknown)') AS country,\n    COALESCE(d.AttributionChannel, '(unknown)') AS channel\n  FROM {{ ref('int_customer_mrr') }} d\n  JOIN first_pay fp ON fp.EntityRecordID = d.EntityRecordID AND d.Month = fp.cohort_month\n),\nbase AS (\n  SELECT\n    fp.EntityRecordID AS eid, fp.cohort_month, b.mrr AS mrr0,\n    dm.segment, dm.country, dm.channel,\n    CASE WHEN lbl.is_multi_client THEN 'Multi-client' ELSE COALESCE(lbl.l1, 'Unclassified') END AS l1\n  FROM first_pay fp\n  JOIN monthly_mrr b ON b.EntityRecordID = fp.EntityRecordID AND b.Month = fp.cohort_month\n  JOIN signup s ON s.EntityRecordID = fp.EntityRecordID AND s.sd >= '2021-06-01'\n  LEFT JOIN dims dm ON dm.EntityRecordID = fp.EntityRecordID\n  LEFT JOIN {{ source('v7_classification', 'v_entity_primary_label') }} lbl ON lbl.customer_record_id = fp.EntityRecordID\n),\njoined AS (\n  SELECT\n    base.cohort_month, k AS tenure_k, base.mrr0, IFNULL(f.mrr, 0) AS mrrk,\n    base.l1, base.segment, base.country, base.channel\n  FROM base, UNNEST(GENERATE_ARRAY(0, 24)) AS k\n  LEFT JOIN monthly_mrr f\n    ON f.EntityRecordID = base.eid\n    AND f.Month = DATE_ADD(base.cohort_month, INTERVAL k MONTH)\n  WHERE DATE_ADD(base.cohort_month, INTERVAL k MONTH) <=\n    {%- if var('retention_censor_month', none) is not none %}\n    DATE('{{ var(\"retention_censor_month\") }}')\n    {%- else %}\n    DATE_SUB(DATE_TRUNC(CURRENT_DATE(), MONTH), INTERVAL 1 MONTH)\n    {%- endif %}\n)\nSELECT\n  cohort_month, tenure_k, l1, segment, country, channel,\n  COUNT(*) AS n_start,\n  COUNTIF(mrrk > 0) AS n_active,\n  SUM(mrr0) AS mrr_start,\n  SUM(mrrk) AS mrr_active\nFROM joined\nGROUP BY 1, 2, 3, 4, 5, 6\nORDER BY 1, 2, 3, 4, 5, 6\n","tests":["accepted_values(tenure_k)","assert_retention_triangle_invariants","assert_retention_triangle_unique","not_null(channel)","not_null(cohort_month)","not_null(country)","not_null(l1)","not_null(mrr_start)","not_null(n_active)","not_null(n_start)","not_null(segment)","not_null(tenure_k)"]},{"name":"int_customer_survival","alias":"int_customer_survival","relation_name":"`project-for-method-dw`.`revenue`.`int_customer_survival`","description":"Cohort survival by first-pay vintage, entity grain. One row per (vintage, tenure_k). Logo survival = n_alive / n_start (count-weighted). GRR = retained_mrr / base_mrr (dollar-weighted, expansion capped). The two differ; \"still paying\" describes only the logo line. Anchor = each entity's first paying month; signup gate Trial >= 2021-06-01; cells with n_start < 30 dropped; right-censored at the latest complete month. Parity: VINTAGE_SQL + §18 verification-queries.md (2026-06).\n","original_file_path":"models/intermediate/int_customer_survival.sql","refs":["int_customer_mrr"],"sources":["Funnel"],"columns":[{"name":"vintage","description":"Calendar year of the entity's first paying month."},{"name":"tenure_k","description":"Months since first paying month (0–24)."},{"name":"n_start","description":""},{"name":"n_alive","description":""},{"name":"base_mrr","description":""}],"compiled_sql":"{{ config(materialized='table') }}\n\n-- Cohort survival by first-pay vintage. ENTITY grain (EntityRecordID).\n-- Mirrors VINTAGE_SQL (build_expanders_doc.py) + §18 of verification-queries.md.\n-- See docs/metric-definitions.md and docs/superpowers/specs/2026-06-22-cohort-survival-vintage-design.md.\n\nWITH mrr AS (\n  SELECT Month, EntityRecordID, SUM(StartMRR) AS month_mrr\n  FROM {{ ref('int_customer_mrr') }}\n  GROUP BY 1, 2\n),\nsignup AS (\n  SELECT EntityRecordID, MIN(Date) AS sd\n  FROM {{ source('revenue', 'Funnel') }}\n  WHERE EventType = 'Trial'\n  GROUP BY 1\n),\nfirst_pay AS (  -- anchor = each entity's FIRST paying month\n  SELECT EntityRecordID, MIN(Month) AS t0\n  FROM mrr\n  WHERE month_mrr > 0\n  GROUP BY 1\n),\nbase AS (\n  SELECT\n    fp.EntityRecordID AS eid,\n    fp.t0,\n    CAST(EXTRACT(YEAR FROM fp.t0) AS STRING) AS vintage,\n    b.month_mrr AS mrr0\n  FROM first_pay fp\n  JOIN mrr b\n    ON b.EntityRecordID = fp.EntityRecordID\n    AND b.Month = fp.t0\n  JOIN signup s  -- signup gate: first-pay anchor is genuine, not left-censored\n    ON s.EntityRecordID = fp.EntityRecordID\n    AND s.sd >= '2021-06-01'\n),\njoined AS (\n  SELECT\n    base.vintage,\n    k AS tenure_k,\n    base.mrr0,\n    IFNULL(f.month_mrr, 0) AS mrrk\n  FROM base, UNNEST(GENERATE_ARRAY(0, 24)) AS k\n  LEFT JOIN mrr f\n    ON f.EntityRecordID = base.eid\n    AND f.Month = DATE_ADD(base.t0, INTERVAL k MONTH)\n  WHERE DATE_ADD(base.t0, INTERVAL k MONTH)\n        <= DATE('{{ var(\"survival_censor_month\", \"2026-05-01\") }}')\n)\nSELECT\n  vintage,\n  tenure_k,\n  -- n_start = entities in the vintage observable at tenure k (right-censoring drops entities whose t0+k exceeds the censor month), so it shrinks at later k for recent vintages. GRR ratio is unaffected (num & denom share this subset).\n  COUNT(*) AS n_start,\n  COUNTIF(mrrk > 0) AS n_alive,\n  SUM(mrr0) AS base_mrr,\n  SUM(LEAST(mrrk, mrr0)) AS retained_mrr,\n  SUM(mrrk) AS net_mrr\nFROM joined\nGROUP BY 1, 2\nHAVING n_start >= {{ var(\"survival_min_n\", 30) }}\nORDER BY 1, 2\n","tests":["accepted_values(tenure_k)","assert_survival_invariants","assert_survival_unique","not_null(base_mrr)","not_null(n_alive)","not_null(n_start)","not_null(tenure_k)","not_null(vintage)"]},{"name":"int_customers","alias":"int_customers","relation_name":"`project-for-method-dw`.`revenue`.`int_customers`","description":"Customer-month spine. One row per (Month, EntityRecordID) for every paying\ncustomer that had SaaS activity that month (from 2024-01-01 onward). This is\nthe dimension backbone the rest of the stack joins to for segment / tier /\nDEP / attribution / country slices.\n\nGrain: customer-month at the EntityRecordID level. A \"customer\" is an\nEntityRecordID, which can own multiple CompanyAccounts (see AccountCount) —\nthis is the dbt-stack customer definition (matches Customers #373), NOT the\nCompanyAccount-as-customer convention used in some legacy Excel decks.\n\nMigrated from an orphaned BQ view (2026-06-04). The dimension picks\n(AttributionChannel / SignupCountry / Vertical / SyncType) were made\ndeterministic by adding a CompanyAccount ASC tiebreaker to the founding-account\nselection — see the .sql header for the full determinism-fix rationale.\n\nExcludes internal Method Integration partner rows and conversion-exception\naccounts. HasDEP / Segment / UserTier here are per-customer-month\n(point-in-time / honest), unlike the \"ever\"-style rollups in int_motion_funnel.\n","original_file_path":"models/intermediate/int_customers.sql","refs":[],"sources":["TransLineFlattened","Account","Entity"],"columns":[{"name":"Month","description":"First day of the customer-activity month (DATE_TRUNC to month)."},{"name":"EntityRecordID","description":"Stable numeric customer ID (customer grain). One EntityRecordID can own multiple CompanyAccounts. Use for temporal joins — Company strings drift on rename.\n"},{"name":"EntityFullName","description":"Customer display name, joined from revenue.Entity on RecordID."},{"name":"AccountCount","description":"Distinct CompanyAccounts this customer billed under that month. >1 means one customer spanning multiple Method accounts.\n"},{"name":"TotalUsers","description":"Sum of UserPaidCount across the customer's accounts that month (seat count)."},{"name":"HasDEP","description":"TRUE if any of the customer's accounts had a DEP line (Premium App / Enhancement Plan with nonzero SaaS) that month. Per-customer-month / point-in-time — this is the honest \"on DEP this month\" flag. (The int_motion_funnel ever_had_dep rollup is \"ever had DEP\" and is a different semantic — do not conflate.)\n"},{"name":"UserTier","description":"Seat band from TotalUsers: Solo (1), Small Team (2-3), Team (4+).\n"},{"name":"Segment","description":"Product/pricing segment: Team AI Plus (HasDEP), 4+ no DEP (4+ seats, no DEP), 2-3 no DEP, Solo no DEP. Derived from HasDEP + TotalUsers.\n"},{"name":"AttributionChannel","description":"Single-touch acquisition channel of the founding account (earliest FirstSaaSInvoiceTxnDate, CompanyAccount ASC tiebreak), from the Account Att_* flags (SEO, PPC, OPN, Social, Email, Referral, Direct, Partners, Content, Remarketing, Other, None; Unknown when no flag set). This is the single-touch dimension — for real multi-touch credit use int_attribution_fractional.\n"},{"name":"SignupCountry","description":"SignupCountry of the founding account (first non-null, deterministic pick)."},{"name":"Vertical","description":"Vertical of the founding account (first non-null, deterministic pick)."},{"name":"SyncType","description":"SyncType of the founding account (first non-null, deterministic pick)."},{"name":"IsActive","description":"⚠️ Always TRUE by construction — every row here is already a paying customer-month, so this is NOT a real state filter. Do not read it as \"active vs inactive\"; there are no inactive rows to filter against. [REVIEW: keep this column at all, or drop it since it carries no information?]\n"},{"name":"IsNew","description":"TRUE for the customer's first observed month (no prior month row for this EntityRecordID). New-customer flag.\n"},{"name":"IsChurned","description":"⚠️ Means \"no revenue row in the NEXT month\" (last observed month, via LEAD), NOT \"cancelled.\" Method hard-holds non-payers before cancelling, so a customer with no CancellationDate can still show IsChurned here, and vice-versa (see CLAUDE.md \"Hard-hold billing state\"). Treat as a revenue-presence proxy for churn, not a lifecycle cancellation event. The current (max) month is forced to FALSE to avoid flagging the in-progress month as churn."}],"compiled_sql":"-- Migrated from the orphaned BQ view of the same name.\n-- Original DDL captured in knowledge/verified-queries/int_customers-pre-migration-ddl.sql.\n--\n-- Grain: one row per (Month, EntityRecordID) for active paying customers.\n--\n-- DETERMINISM FIX (2026-06-04, branch validation/int-customers-dedup):\n--   The original view's dim columns (AttributionChannel / SignupCountry /\n--   Vertical / SyncType) FLICKERED run-to-run. There are no duplicate output\n--   rows — the row count per (EntityRecordID, Month) is already exactly 1. The\n--   non-determinism lived INSIDE entity_monthly: each dim is picked via\n--   ARRAY_AGG(... ORDER BY ad.FirstSaaSInvoiceTxnDate LIMIT 1). When a customer\n--   owns >1 CompanyAccount sharing the SAME MIN(FirstSaaSInvoiceTxnDate), that\n--   ORDER BY ties and BigQuery picks an arbitrary tied row per execution.\n--   ~260 customer-months had differing dims among tied accounts (the flicker).\n--\n--   Tiebreaker chosen: append `, ad.CompanyAccount ASC` to every dim ORDER BY.\n--   Rationale:\n--     - CompanyAccount is unique within an (EntityRecordID, Month) group, so it\n--       fully resolves every tie — zero ambiguity remains.\n--     - It preserves the original semantics: FirstSaaSInvoiceTxnDate ASC stays\n--       the primary key, so the \"founding account\" still wins; CompanyAccount\n--       only decides among exact date ties. Non-tied rows are unchanged vs prod.\n--     - CompanyAccount ASC mirrors int_customer_mrr's own first-company logic\n--       (ARRAY_AGG(CompanyAccount ORDER BY SaaSAmount DESC, CompanyAccount ASC\n--       LIMIT 1)), keeping the two models' tiebreak sense consistent.\n\n{{ config(materialized='table') }}\n\nWITH monthly_accounts AS (\n  SELECT\n    t.EntityRecordID, DATE_TRUNC(t.TxnDate, MONTH) AS Month, t.CompanyAccount,\n    MAX(t.UserPaidCount) AS UserPaidCount,\n    MAX(CASE WHEN (t.AccountFullName LIKE '%Premium App%' OR t.AccountFullName LIKE '%Enhancement Plan%') AND t.SaaSAmount != 0 THEN 1 ELSE 0 END) AS has_dep_txn\n  FROM {{ source('revenue', 'TransLineFlattened') }} t\n  WHERE t.Partner != 'Method Integration' AND t.TxnDate >= '2024-01-01'\n  GROUP BY 1, 2, 3\n),\naccount_dims AS (\n  SELECT\n    a.CompanyAccount, a.FirstSaaSInvoiceTxnDate,\n    CASE\n      WHEN a.Att_SEO = 1 THEN 'SEO' WHEN a.Att_Pay_Per_Click = 1 THEN 'PPC'\n      WHEN a.Att_OPN_Other_Peoples_Networks = 1 THEN 'OPN' WHEN a.Att_Social = 1 THEN 'Social'\n      WHEN a.Att_Email = 1 THEN 'Email' WHEN a.Att_Referral_Link = 1 THEN 'Referral'\n      WHEN a.Att_Direct = 1 THEN 'Direct' WHEN a.Att_Partners = 1 THEN 'Partners'\n      WHEN a.Att_Content = 1 THEN 'Content' WHEN a.Att_Remarketing = 1 THEN 'Remarketing'\n      WHEN a.Att_Other = 1 THEN 'Other' WHEN a.Att_None = 1 THEN 'None' ELSE 'Unknown' END AS AttributionChannel,\n    a.SignupCountry, a.Vertical, a.SyncType\n  FROM {{ source('revenue', 'Account') }} a\n  WHERE a.IsConversionException = FALSE AND a.Partner != 'Method Integration'\n),\nentity_monthly AS (\n  SELECT\n    ma.EntityRecordID, ma.Month,\n    COUNT(DISTINCT ma.CompanyAccount) AS AccountCount,\n    SUM(ma.UserPaidCount) AS TotalUsers,\n    MAX(ma.has_dep_txn) AS HasDEP,\n    -- Deterministic dim pick: founding account (earliest FirstSaaSInvoiceTxnDate),\n    -- ties broken by CompanyAccount ASC (unique within the group) so no run-to-run flicker.\n    ARRAY_AGG(ad.AttributionChannel IGNORE NULLS ORDER BY ad.FirstSaaSInvoiceTxnDate, ad.CompanyAccount ASC LIMIT 1)[SAFE_OFFSET(0)] AS AttributionChannel,\n    ARRAY_AGG(ad.SignupCountry      IGNORE NULLS ORDER BY ad.FirstSaaSInvoiceTxnDate, ad.CompanyAccount ASC LIMIT 1)[SAFE_OFFSET(0)] AS SignupCountry,\n    ARRAY_AGG(ad.Vertical           IGNORE NULLS ORDER BY ad.FirstSaaSInvoiceTxnDate, ad.CompanyAccount ASC LIMIT 1)[SAFE_OFFSET(0)] AS Vertical,\n    ARRAY_AGG(ad.SyncType           IGNORE NULLS ORDER BY ad.FirstSaaSInvoiceTxnDate, ad.CompanyAccount ASC LIMIT 1)[SAFE_OFFSET(0)] AS SyncType\n  FROM monthly_accounts ma LEFT JOIN account_dims ad ON ma.CompanyAccount = ad.CompanyAccount\n  GROUP BY 1, 2\n),\nmax_month AS (SELECT MAX(Month) AS m FROM entity_monthly)\nSELECT\n  em.Month, em.EntityRecordID, e.EntityFullName,\n  em.AccountCount, em.TotalUsers, em.HasDEP = 1 AS HasDEP,\n  CASE WHEN em.TotalUsers = 1 THEN 'Solo' WHEN em.TotalUsers BETWEEN 2 AND 3 THEN 'Small Team' ELSE 'Team' END AS UserTier,\n  CASE WHEN em.HasDEP = 1 THEN 'Team AI Plus' WHEN em.TotalUsers >= 4 THEN '4+ no DEP' WHEN em.TotalUsers >= 2 THEN '2-3 no DEP' ELSE 'Solo no DEP' END AS Segment,\n  em.AttributionChannel, em.SignupCountry, em.Vertical, em.SyncType,\n  TRUE AS IsActive,\n  LAG(em.Month) OVER (PARTITION BY em.EntityRecordID ORDER BY em.Month) IS NULL AS IsNew,\n  LEAD(em.Month) OVER (PARTITION BY em.EntityRecordID ORDER BY em.Month) IS NULL AND em.Month < (SELECT m FROM max_month) AS IsChurned\nFROM entity_monthly em\nJOIN {{ source('revenue', 'Entity') }} e ON em.EntityRecordID = e.RecordID\n","tests":["not_null(EntityRecordID)","not_null(Month)"]},{"name":"int_demo_calls","alias":"int_demo_calls","relation_name":"`project-for-method-dw`.`revenue`.`int_demo_calls`","description":"Demo meetings joined to their recorded calls, ONE ROW PER CALL. This is\nthe transcript-finding surface.\n\nint_demos answers \"how many demos\". This answers \"show me the calls, and\ntell me where each one sits in the customer's lifecycle\". The lifecycle\nzone is what conversations.call_type cannot give you — call_type is derived\nfrom the same Method-side activity subject line, so it can say \"demo\" but\nnever \"demo to someone who had already subscribed\".\n\nGRAIN: one row per conversation_id. NOT one row per meeting — 559 meetings\nmatch more than one recorded call (max 4). Do NOT count demos from this\nview: only ~46% of demos have a same-day recording, and multi-call meetings\nappear more than once. Count from int_demos, read from here.\n\ntranscript_text is deliberately not selected. Join conversation_id back to\ncustomer_signals.conversations to read it, so there is exactly one copy.\n","original_file_path":"models/intermediate/int_demo_calls.sql","refs":["int_accounts","int_demos"],"sources":["conversations"],"columns":[{"name":"conversation_id","description":"Primary key. Join to customer_signals.conversations for the transcript text."},{"name":"entity_record_id","description":"Customer key (EntityRecordID), reached from conversations.account_id via int_accounts."},{"name":"account_id","description":"The account the call was recorded against (= revenue.Account.RecordID)."},{"name":"company_account","description":"Account name, for human identification when browsing calls."},{"name":"occurred_at","description":"When the call actually happened, from the recording. Independent of Activity.DueDateStart."},{"name":"call_date","description":"DATE(occurred_at)."},{"name":"call_type","description":"conversations.call_type — demo / free_hour / customization / support. NOT independent evidence: it is a 1:1 mapping of the activity subject line (\"AI Summary - Demo\" etc.), so it agrees with ActivityType by construction rather than by observation.\n"},{"name":"topic","description":"The activity subject line the call_type was derived from."},{"name":"transcript_chars","description":"Length of the transcript. 0 means the call was recorded but not transcribed."},{"name":"has_transcript","description":"transcript_chars > 200. Roughly half of demos have one."},{"name":"demo_date","description":"The Activity meeting date this call was matched to."},{"name":"days_from_meeting","description":"Signed day offset between the call and the Activity meeting date."},{"name":"match_quality","description":"same_day or adjacent_day. Same-day matches are clean (1,436 demo + 29 free_hour call_types). adjacent_day adds 363 matches but more ambiguity — filter to same_day when precision matters more than recall.\n"},{"name":"zone","description":"Lifecycle zone of the meeting, from int_demos. The reason to use this view. pre_signup / pre_subscription are demos; post_subscription, no_funnel_record and excluded are not.\n"},{"name":"is_demo","description":"Whether the underlying meeting counts as a demo. See int_demos."},{"name":"attended","description":"Someone showed up (Demo / Pre-sales Demo, not Cancelled)."},{"name":"missed","description":"Demo Missed row present for this meeting."},{"name":"returning_customer","description":"Customer had previously subscribed and cancelled as of the meeting."},{"name":"after_sync","description":"Meeting happened at or after the customer's first QuickBooks sync."},{"name":"typed_deliberately","description":"Rep chose \"Pre-sales Demo\" rather than the generic \"Demo\"."},{"name":"multi_account_customer","description":"Customer owns 2+ accounts, so fences may be set by a different account's history."},{"name":"first_signup","description":"Trials"},{"name":"first_subscribed","description":"Conversions"},{"name":"first_sync","description":"Syncs"},{"name":"last_cancel_month","description":"Cancellations"},{"name":"classification_reason","description":"Plain-English sentence explaining why this call is or is not a demo. Passed through from int_demos, where it is defined once so that every meeting carries it — not only the ~36% with a recording.\n"},{"name":"confidence_note","description":"How much to trust the classification, in words. Passed through from int_demos."},{"name":"days_since_signup","description":"Days between the customer's first trial signup and this meeting. Negative before signup."},{"name":"days_since_subscribed","description":"Days between the customer's subscription date and this meeting. Negative before subscribing. Large positive values on a demo-labelled call are the clearest signal that it is not a new-business demo — some run past 4,600 days."}],"compiled_sql":"{{ config(\n    materialized='view',\n    labels={'status': 'directional', 'layer': 'intermediate',\n            'grain': 'call', 'owner': 'nic'}\n) }}\n\n-- Demo meetings joined to their recorded calls, ONE ROW PER CALL.\n--\n-- This is the transcript-finding surface. int_demos answers \"how many demos\";\n-- this answers \"show me the calls, and tell me where each one sits in the\n-- customer's lifecycle\". The lifecycle zone is the part conversations.call_type\n-- cannot give you — call_type is derived from the same activity subject line,\n-- so it can say \"demo\" but not \"demo to someone who had already subscribed\".\n--\n-- GRAIN: exactly one row per conversation_id (enforced by a test). NOT one row\n-- per meeting — 559 meetings match more than one recorded call, so meeting-grain\n-- would drop transcripts. Where a call matches two meetings, the closest one is\n-- kept.\n-- Counting demos from this view will UNDERCOUNT (only ~46% of demos have a\n-- same-day recording) and DOUBLE-COUNT the multi-call meetings. Count demos\n-- from int_demos; read calls from here.\n--\n-- transcript_text is deliberately NOT selected — it lives in\n-- customer_signals.conversations and should have exactly one copy. Join on\n-- conversation_id to read it.\n\nWITH bridge AS (\n  SELECT DISTINCT entity_record_id, account_record_id, company_account\n  FROM {{ ref('int_accounts') }}\n  WHERE entity_record_id IS NOT NULL\n),\n\ncalls AS (\n  SELECT\n    cv.conversation_id,\n    cv.account_id,\n    b.entity_record_id,\n    b.company_account,\n    cv.occurred_at,\n    DATE(cv.occurred_at)                                        AS call_date,\n    cv.call_type,\n    cv.topic,\n    LENGTH(COALESCE(cv.transcript_text, ''))                    AS transcript_chars\n  FROM {{ source('customer_signals', 'conversations') }} cv\n  JOIN bridge b ON b.account_record_id = cv.account_id\n)\n\nSELECT\n  c.conversation_id,\n  c.entity_record_id,\n  c.account_id,\n  c.company_account,\n  c.occurred_at,\n  c.call_date,\n  c.call_type,\n  c.topic,\n  c.transcript_chars,\n  c.transcript_chars > 200                                      AS has_transcript,\n\n  -- the meeting this call belongs to\n  d.demo_date,\n  DATE_DIFF(c.call_date, d.demo_date, DAY)                      AS days_from_meeting,\n  IF(c.call_date = d.demo_date, 'same_day', 'adjacent_day')     AS match_quality,\n\n  -- lifecycle classification: the reason to use this view at all\n  d.zone,\n  d.is_demo,\n\n  -- classification_reason and confidence_note are defined ONCE in int_demos so\n  -- that every meeting carries them, including the 64% with no recording.\n  d.classification_reason,\n  d.confidence_note,\n\n  -- context a reader wants without having to compute it\n  DATE_DIFF(d.demo_date, d.first_signup, DAY)                   AS days_since_signup,\n  DATE_DIFF(d.demo_date, d.first_subscribed, DAY)               AS days_since_subscribed,\n\n  d.attended,\n  d.missed,\n  d.returning_customer,\n  d.after_sync,\n  d.typed_deliberately,\n  d.multi_account_customer,\n\n  -- fences, so a reader can see why the call was classified this way\n  d.first_signup,\n  d.first_subscribed,\n  d.first_sync,\n  d.last_cancel_month\n\nFROM calls c\nJOIN {{ ref('int_demos') }} d\n  ON d.entity_record_id = c.entity_record_id\n AND ABS(DATE_DIFF(c.call_date, d.demo_date, DAY)) <= 1\n\n-- One call can fall within +/-1 day of two different meetings. Keep the single\n-- best match so the view is genuinely one row per conversation: same-day wins,\n-- then the earlier meeting. Without this the view silently duplicates 1,437\n-- calls and inflates any count taken from it.\nQUALIFY ROW_NUMBER() OVER (\n  PARTITION BY c.conversation_id\n  ORDER BY ABS(DATE_DIFF(c.call_date, d.demo_date, DAY)), d.demo_date\n) = 1\n","tests":["assert_int_demo_calls_one_row_per_call","not_null(conversation_id)"]},{"name":"int_demos","alias":"int_demos","relation_name":"`project-for-method-dw`.`revenue`.`int_demos`","description":"One row per demo MEETING at customer × date grain, classified by the\ncustomer's subscription state on the day of the meeting.\n\nA demo is an attended sales meeting that happens before the customer\nsubscribes. The `Demo` activity type alone cannot establish that — it is\nproduced by the calendar booking, not by what happened on the call — so\nthe classification comes from date fences built on canonical metrics:\nTrials #54 (SignupDate), Syncs #55 (SyncDate), Conversions #56\n(FirstSaaSInvoiceTxnDate) and Cancellations #379.\n\nPublished demos = `is_demo`. FAIL-CLOSED: a meeting is only a demo when a\nfence can actually be evaluated. Meetings on entities with no trial, no\nsubscription and no sync record are zoned `no_funnel_record` and excluded;\nentities the canonical funnel models drop (IsConversionException, Method\nIntegration, internal m11/m18) are zoned `excluded`. Post-subscription\nmeetings are also NOT demos — a hand read of 40 transcripts found only\n~33% of them are genuine demos.\n\nDIRECTIONAL. ~85% of published demos are genuinely demos (n=10 per zone,\nso +/-15pp). That figure was judged by Claude (AI assistant) in an analysis\nsession with Nic on 2026-08-18, was not reviewed by anyone at Method, and\nno per-call record was kept. It is also STALE: it was measured before the\nsubscription-date fence and fail-closed zoning removed 2,220 meetings.\nNever quote as accounting-grade.\nSee docs/metric-definitions.md §4b.\n","original_file_path":"models/intermediate/int_demos.sql","refs":["int_trials","int_syncs","int_accounts","int_customer_mrr"],"sources":["int_conversions","Account","Activity"],"columns":[{"name":"entity_record_id","description":"Customer-level key (EntityRecordID). Demos cannot be attributed to a specific Method account — 88.3% of demo activity rows have no MethodCompanyAccountRecordID — so customer grain is the finest grain available. \"Demos per account\" is not a measurable quantity.\n"},{"name":"demo_date","description":"Date of the meeting, from Activity.DueDateStart. DueDateStart is used rather than CreatedDate because CreatedDate is NULL on ~93% of rows. Future-dated rows are excluded — `Demo booked` carries scheduled dates that have not happened yet.\n"},{"name":"attended","description":"Someone showed up: ActivityType in (Demo, Pre-sales Demo, Live Product Demonstration). `AI Summary - Demo` is deliberately NOT sufficient on its own — it is a recording artifact, not evidence a demo occurred. Excluding summary-only meetings costs a known ~11% undercount.\n"},{"name":"booked","description":"A booking was recorded: ActivityType in (Demo booked, Phone Call Demo Booked). NOT a show-rate denominator — `Demo booked` frequently carries the booking date rather than the meeting date, so it lands on a different (entity, date) key: 3,479 meetings are booked-only with no attended or missed row, and 3,313 are attended with no booking row. attended/booked computes to 101.3% and is meaningless at this grain.\n"},{"name":"missed","description":"No-show: ActivityType = Demo Missed. This type sits in no legacy IN-list, which is why show rate previously had no denominator.\n"},{"name":"has_ai_summary","description":"An `AI Summary - Demo` row exists for this meeting. Corroborating evidence only; it never makes a meeting count on its own.\n"},{"name":"typed_deliberately","description":"The rep chose `Pre-sales Demo` rather than the generic `Demo`. A useful quality signal: deliberate typing tracks hand-read precision almost monotonically (84% deliberate → ~100% precise; 0.9% → ~33%).\n"},{"name":"zone","description":"Subscription state on the day of the meeting. Five values.\n\npre_signup        — has a funnel record, and the meeting predates the\n                    customer's first SignupDate. Sales-led entry.\npre_subscription  — has trialled and is NOT subscribed on this date.\n                    Includes returning customers (see\n                    returning_customer).\npost_subscription — subscribed on this date. NOT a demo.\nno_funnel_record  — no trial, no subscription and no sync record, so\n                    no fence can be evaluated. NOT a demo. This is the\n                    fail-closed bucket; absence of a record must never\n                    assert a sales motion.\nexcluded          — entity is dropped by the canonical funnel models\n                    (IsConversionException, Partner = Method\n                    Integration, internal m11/m18 accounts). NOT a\n                    demo, and excluded for consistency with Trials #54\n                    and Conversions #56.\n"},{"name":"is_demo","description":"TRUE when the meeting is attended and the customer was not subscribed. This is the published Demos metric — sum this column, do not count rows.\n"},{"name":"returning_customer","description":"The customer previously subscribed and had cancelled as of this meeting. A dimension, not a zone: these are ordinary pre-subscription demos to someone who left and came back. 42 demos / 35 customers (2025-01 to 2026-07). NO CONVERSION CLAIM IS MADE — an earlier draft reported that returning customers convert better at higher MRR; that finding reversed sign under an alternative outcome definition and was withdrawn. The population is too small for a rate.\n"},{"name":"restarted_trial","description":"The customer started a new trial AFTER their last cancellation and at or before this meeting. Compares against the cancellation, not the first subscription — the earlier version fired whenever a multi-account customer added an account while still paying (61% of its TRUE values were not restarts). Separates self-driven returns (18 demos) from outbound win-back (24 demos).\n"},{"name":"after_sync","description":"The meeting happened at or after the customer's first QuickBooks sync (Syncs #55). Quote this only with its denominator stated: it is ~86% of pre_subscription demos, ~56% of all published demos, and ~98% among customers who ever synced. pre_signup demos are 0% by construction. Customers with no sync record are counted as not-after-sync.\n"},{"name":"first_signup","description":"Trials"},{"name":"first_subscribed","description":"Conversions #56 — earliest FirstSaaSInvoiceTxnDate for this customer, bridged from CompanyAccount via int_accounts. A real date, which is why it is used instead of the first paying month.\n"},{"name":"first_sync","description":"Syncs"},{"name":"classification_reason","description":"Plain-English sentence explaining why THIS meeting is or is not a demo, with the relevant dates inlined. Wording authored by Claude (AI assistant), 2026-08-27, not reviewed by anyone at Method. The FENCE LOGIC it describes is verifiable from the columns; the prose is one person's phrasing of it. Defined on this model so every meeting carries it, including the ~64% that have no recorded call. Never NULL — asserted by tests/assert_int_demos_every_meeting_explained.sql. This is the column that answers \"why does this say demo when it clearly isn't\".\n"},{"name":"confidence_note","description":"How much to trust the classification, in words — e.g. \"about 1 in 3 of these is a real demo\". PROVISIONAL AND UNREVIEWED. Judged by Claude (AI assistant) during an analysis session with Nic on 2026-08-18. NOT reviewed by anyone at Method, and no per-call record was retained, so the judgments cannot be audited or reproduced. The wording was written by Claude too, not by anyone at Method, so read it as one analyst's provisional estimate rather than an established Method figure. It is additionally stale: measured on the zone assignment that existed before the subscription-date fence and fail-closed zoning, which removed 2,220 meetings and should have moved the pre_subscription band in particular. Never NULL.\n"},{"name":"days_since_signup","description":"Days from the customer's first trial signup to this meeting. Negative before signup."},{"name":"days_since_subscribed","description":"Days from the customer's subscription date to this meeting. Negative before subscribing. A large positive value on a demo-labelled meeting is the clearest single signal that it is not new business — some run past 4,600 days.\n"},{"name":"in_complete_month","description":"FALSE for meetings in the current, still-running month. Counting consumers MUST filter on this — Demos = COUNTIF(is_demo AND in_complete_month) — or the number changes between rebuilds. Transcript readers should NOT filter on it; they need recent calls.\n"},{"name":"multi_account_customer","description":"The customer owns more than one Method account. Fences are set by MIN() across ALL their accounts, so an old account's subscription date can fence a new account's genuinely-new-business demo. 531 published demos (17%) are affected; the error direction is an UNDERCOUNT of real demos. Cannot be fixed at account grain — 94.3% of demo activity rows carry no account reference.\n"},{"name":"account_count","description":"Number of Method accounts owned by this customer, from int_accounts."},{"name":"last_cancel_month","description":"Cancellations #379 — most recent month with Cancellations > 0 at or before the meeting month. Month grain, because that is how cancellations are reported. A cancellation is always a full departure is always a full departure, so no grace window is applied. NOTE: the \"4,598 of 4,598\" check previously cited for this is circular — int_customer_mrr defines Cancellations as (p1_saas > 0 AND p2_saas = 0) and the next month's StartMRR IS this month's p2_saas, so the check is an algebraic identity, not evidence. The claim rests on the model's design, not on an independent test."}],"compiled_sql":"{{ config(\n    materialized='table',\n    labels={'status': 'directional', 'layer': 'intermediate',\n            'grain': 'customer_meeting', 'owner': 'nic'}\n) }}\n\n-- One row per demo MEETING at (customer × date) grain, classified by the\n-- customer's subscription state on the day of the meeting.\n--\n-- Why this model exists: `Demo` in revenue.Activity is produced by the calendar\n-- booking, not by what happened on the call, so the activity type alone cannot\n-- tell a new-business demo from a support call. The date fences below do that\n-- work. See docs/metric-definitions.md §4b.\n--\n-- Every fence reuses a canonical, already-reported definition — no new\n-- thresholds are invented here:\n--   signup       Trials #54         int_trials.SignupDate\n--   sync         Syncs #55          int_syncs.SyncDate\n--   subscription Conversions #56    int_conversions.FirstSaaSInvoiceTxnDate\n--   cancellation Cancellations #379 int_customer_mrr.Cancellations\n--\n-- GOTCHA baked in: `int_customer_mrr.StartMRR` is START-of-month MRR, so it\n-- reads 0 for 96.3% of customers during the very month they subscribe. It\n-- therefore CANNOT be used to test \"subscribed on this date\". Subscription state\n-- is derived from FirstSaaSInvoiceTxnDate (a real date) plus cancellation\n-- recency instead.\n--\n-- FAIL-CLOSED: a meeting with no trial, no subscription and no sync record has\n-- no fence to evaluate, so it is zoned 'no_funnel_record' and is NOT a demo.\n-- Absence of a record must never assert a zone.\n--\n-- Window: 2020-01-01 to today. The current partial month is INCLUDED so that\n-- transcript readers can reach recent calls, and flagged `in_complete_month =\n-- FALSE` so counting consumers can exclude it per the repo-wide rule.\n--   Demos (reproducible) = COUNTIF(is_demo AND in_complete_month)\n--\n-- Directional: ~85% of published demos are genuinely demos. PROVISIONAL — that\n-- figure and the confidence_note wording were judged and written by Claude (AI\n-- assistant) with Nic on 2026-08-18/27, reviewed by nobody at Method, with no\n-- per-call record retained. It is also stale: measured before the\n-- subscription-date fence and fail-closed zoning removed 2,220 meetings.\n-- Never quote as accounting-grade.\n\nWITH signup AS (\n  SELECT EntityRecordID AS entity_record_id, MIN(SignupDate) AS first_signup\n  FROM {{ ref('int_trials') }}\n  GROUP BY 1\n),\n\nall_trials AS (   -- every trial, to detect a restart after a cancellation\n  SELECT EntityRecordID AS entity_record_id, SignupDate AS signup_date\n  FROM {{ ref('int_trials') }}\n),\n\nsyncs AS (\n  SELECT EntityRecordID AS entity_record_id, MIN(SyncDate) AS first_sync\n  FROM {{ ref('int_syncs') }}\n  GROUP BY 1\n),\n\n-- int_conversions carries CompanyAccount but not EntityRecordID, so bridge\n-- through int_accounts to reach customer grain.\naccount_bridge AS (\n  SELECT DISTINCT company_account, entity_record_id\n  FROM {{ ref('int_accounts') }}\n  WHERE entity_record_id IS NOT NULL\n),\n\nsubscribed AS (\n  SELECT b.entity_record_id, MIN(c.FirstSaaSInvoiceTxnDate) AS first_subscribed\n  FROM {{ source('revenue', 'int_conversions') }} c\n  JOIN account_bridge b ON b.company_account = c.CompanyAccount\n  WHERE c.FirstSaaSInvoiceTxnDate > DATE '0001-01-01'   -- 0001-01-01 = never paid\n  GROUP BY 1\n),\n\ncancellations AS (\n  SELECT DISTINCT EntityRecordID AS entity_record_id, Month AS cancel_month\n  FROM {{ ref('int_customer_mrr') }}\n  WHERE Cancellations > 0\n),\n\n-- StartMRR is START-of-month, so a re-subscription is invisible in its own\n-- month (verified: 5,894 of 5,894 comeback months have StartMRR = 0). NewMRR\n-- must be OR-ed in or every comeback is missed.\npaying_months AS (\n  SELECT DISTINCT EntityRecordID AS entity_record_id, Month AS paying_month\n  FROM {{ ref('int_customer_mrr') }}\n  WHERE StartMRR > 0 OR NewMRR > 0\n),\n\n-- Entities excluded from the canonical funnel models. int_trials and\n-- int_conversions both drop these, so without an explicit exclusion here their\n-- silence would be read as \"never signed up\" and published as a demo.\nexcluded_entities AS (\n  SELECT DISTINCT EntityRecordID AS entity_record_id\n  FROM {{ source('revenue', 'Account') }}\n  WHERE COALESCE(IsConversionException, FALSE) = TRUE\n     OR Partner = 'Method Integration'\n     OR LOWER(CompanyAccount) LIKE 'm11%'\n     OR LOWER(CompanyAccount) LIKE 'm18%'\n),\n\n-- Collapse to one row per meeting. Demo booked / Demo / Demo Missed /\n-- AI Summary - Demo all fire for a single meeting, so counting activity rows\n-- overstates volume by roughly 50%.\nmeetings AS (\n  SELECT\n    EntityRecordID AS entity_record_id,\n    DueDateStart   AS demo_date,\n    -- 'Live Product Demonstration' is deliberately NOT here: 85 of its 86 rows\n    -- predate the 2020 floor and the one live row is an existing-account\n    -- delivery demo. Including it changed the count by zero.\n    LOGICAL_OR(ActivityType IN ('Demo', 'Pre-sales Demo')\n               AND COALESCE(ActivityStatus, '') != 'Cancelled')      AS attended,\n    LOGICAL_OR(ActivityType IN ('Demo booked',\n                                'Phone Call Demo Booked'))          AS booked,\n    LOGICAL_OR(ActivityType = 'Demo Missed')                        AS missed,\n    LOGICAL_OR(ActivityType = 'AI Summary - Demo')                  AS has_ai_summary,\n    LOGICAL_OR(ActivityType = 'Pre-sales Demo')                     AS typed_deliberately\n  FROM {{ source('revenue', 'Activity') }}\n  WHERE COALESCE(IsDeleted, FALSE) = FALSE\n    AND EntityRecordID IS NOT NULL\n    AND EntityRecordID != 0                     -- entity 0 is the test-account bucket\n    -- Recent meetings ARE included, because reading transcripts needs them.\n    -- Counting excludes the partial month via in_complete_month below.\n    AND DueDateStart <= CURRENT_DATE()\n    AND DueDateStart >= DATE '2020-01-01'        -- Activity before 2020 is too sparse to fence\n    AND ActivityType IN ('Demo', 'Pre-sales Demo',\n                         'Demo booked', 'Phone Call Demo Booked', 'Demo Missed',\n                         'AI Summary - Demo')\n  GROUP BY 1, 2\n),\n\nstate AS (\n  SELECT\n    m.*,\n    sg.first_signup,\n    sb.first_subscribed,\n    sy.first_sync,\n    sb.first_subscribed IS NOT NULL\n      AND m.demo_date >= sb.first_subscribed                        AS subscribed_at_demo,\n    ex.entity_record_id IS NOT NULL                                 AS funnel_excluded,\n    COALESCE(ac.account_count, 0)                                   AS account_count,\n    (SELECT MAX(c.cancel_month) FROM cancellations c\n      WHERE c.entity_record_id = m.entity_record_id\n        AND c.cancel_month <= DATE_TRUNC(m.demo_date, MONTH))       AS last_cancel_month\n  FROM meetings m\n  LEFT JOIN signup     sg ON sg.entity_record_id = m.entity_record_id\n  LEFT JOIN subscribed sb ON sb.entity_record_id = m.entity_record_id\n  LEFT JOIN syncs      sy ON sy.entity_record_id = m.entity_record_id\n  LEFT JOIN excluded_entities ex ON ex.entity_record_id = m.entity_record_id\n  LEFT JOIN (SELECT entity_record_id, COUNT(*) AS account_count\n             FROM {{ ref('int_accounts') }} GROUP BY 1) ac\n    ON ac.entity_record_id = m.entity_record_id\n),\n\nchurn_state AS (\n  SELECT\n    s.*,\n    -- Churned as of the demo month: a cancellation landed at or before it and\n    -- no paying month has occurred since. Cancellations #379 is always a FULL\n    -- departure (verified: 4,598 of 4,598 cancellation-months are followed by\n    -- zero MRR), so no threshold or grace window is needed.\n    s.last_cancel_month IS NOT NULL\n      AND NOT EXISTS (\n        SELECT 1 FROM paying_months p\n        WHERE p.entity_record_id = s.entity_record_id\n          AND p.paying_month > s.last_cancel_month\n          AND p.paying_month <= DATE_TRUNC(s.demo_date, MONTH)\n      )                                                             AS churned_at_demo\n  FROM state s\n)\n\nSELECT\n  entity_record_id,\n  demo_date,\n\n  -- meeting state\n  attended,\n  booked,\n  missed,\n  has_ai_summary,\n  typed_deliberately,\n\n  -- FOUR zones. no_funnel_record is the fail-closed bucket: no trial, no\n  -- subscription and no sync record exists, so there is no fence to evaluate\n  -- and no claim can be made. It is NOT a demo.\n  CASE\n    WHEN funnel_excluded                                     THEN 'excluded'\n    WHEN first_signup IS NULL\n         AND first_subscribed IS NULL\n         AND first_sync IS NULL                              THEN 'no_funnel_record'\n    WHEN first_signup IS NULL OR demo_date < first_signup    THEN 'pre_signup'\n    WHEN subscribed_at_demo AND NOT churned_at_demo          THEN 'post_subscription'\n    ELSE                                                          'pre_subscription'\n  END                                                             AS zone,\n\n  -- A demo is an attended meeting in a zone we can actually stand behind.\n  attended\n    AND NOT funnel_excluded\n    AND NOT (first_signup IS NULL AND first_subscribed IS NULL AND first_sync IS NULL)\n    AND NOT (subscribed_at_demo AND NOT churned_at_demo)           AS is_demo,\n\n  -- Plain-English reason, per meeting. Defined here rather than downstream so\n  -- EVERY meeting carries it, not only the ~36% that have a recorded call.\n  -- Written for someone reading a pile of demo-labelled material who should not\n  -- have to learn the fence rules first.\n  CASE\n    WHEN funnel_excluded THEN\n      'Not a demo — this account is excluded from Method funnel reporting '\n      || '(conversion exception, Method Integration partner, or internal test account). '\n      || 'Trials and Conversions drop it too.'\n    WHEN first_signup IS NULL AND first_subscribed IS NULL AND first_sync IS NULL THEN\n      'Cannot classify — this customer has no trial, no subscription and no sync record, '\n      || 'so there is nothing to place the meeting against. The meeting is real; the context is missing.'\n    WHEN subscribed_at_demo AND NOT churned_at_demo THEN\n      'Not a demo — the customer was already a subscriber on this date (subscribed '\n      || FORMAT_DATE('%d %b %Y', first_subscribed) || ', '\n      || CAST(DATE_DIFF(demo_date, first_subscribed, DAY) AS STRING)\n      || ' days earlier). Meetings here are usually support, training, billing or '\n      || 'customization scoping — only about a third are genuine upsell demos.'\n    WHEN NOT attended AND missed THEN\n      'Not a demo — booked but recorded as a no-show (Demo Missed).'\n    WHEN NOT attended THEN\n      'Not a demo — no attendance was recorded, only a booking or an AI summary. '\n      || 'The meeting may well have happened; we have no evidence either way.'\n    WHEN first_signup IS NULL OR demo_date < first_signup THEN\n      'Demo — happened before the customer started a trial'\n      || IF(first_signup IS NULL, '', ' (signed up ' || FORMAT_DATE('%d %b %Y', first_signup) || ')')\n      || '. Sales-led entry.'\n    WHEN subscribed_at_demo AND churned_at_demo THEN\n      'Demo — the customer had subscribed before and cancelled (last cancellation '\n      || FORMAT_DATE('%b %Y', last_cancel_month) || '), so they were being sold to again.'\n    WHEN first_subscribed IS NULL THEN\n      'Demo — the customer had trialled but never subscribed as of this meeting.'\n    ELSE\n      'Demo — between trial signup and subscription (subscribed '\n      || FORMAT_DATE('%d %b %Y', first_subscribed) || ').'\n  END                                                             AS classification_reason,\n\n  -- how much to trust the classification for this zone, in words\n  CASE\n    WHEN funnel_excluded                                     THEN 'out of scope for funnel reporting'\n    WHEN first_signup IS NULL AND first_subscribed IS NULL\n         AND first_sync IS NULL                              THEN 'unverified — no funnel context exists'\n    WHEN NOT attended AND missed                             THEN 'recorded no-show'\n    WHEN NOT attended                                        THEN 'no attendance evidence either way'\n    WHEN subscribed_at_demo AND NOT churned_at_demo          THEN 'about 1 in 3 of these is a real demo'\n    WHEN first_signup IS NULL OR demo_date < first_signup    THEN 'nearly all of these are real demos'\n    ELSE                                                          'about 4 in 5 of these are real demos'\n  END                                                             AS confidence_note,\n\n  -- context a reader wants without having to compute it\n  DATE_DIFF(demo_date, first_signup, DAY)                         AS days_since_signup,\n  DATE_DIFF(demo_date, first_subscribed, DAY)                     AS days_since_subscribed,\n\n  -- dimensions, not zones\n  subscribed_at_demo AND churned_at_demo                          AS returning_customer,\n\n  -- Must postdate the CANCELLATION, not the first subscription. Comparing to\n  -- first_subscribed fired on multi-account customers adding a new account\n  -- while still paying on an old one (61% of its former TRUE values).\n  EXISTS (SELECT 1 FROM all_trials t\n          WHERE t.entity_record_id = churn_state.entity_record_id\n            AND t.signup_date <= churn_state.demo_date\n            AND churn_state.last_cancel_month IS NOT NULL\n            AND t.signup_date > churn_state.last_cancel_month)     AS restarted_trial,\n\n  first_sync IS NOT NULL AND demo_date >= first_sync               AS after_sync,\n\n  -- FALSE for meetings in the current, still-running month. Counting consumers\n  -- must filter on this; transcript readers should not.\n  demo_date < DATE_TRUNC(CURRENT_DATE(), MONTH)                    AS in_complete_month,\n\n  -- fences are set by MIN() across ALL of a customer's accounts, so for\n  -- multi-account customers an old account's history can fence a new account's\n  -- demo. This flag marks the affected population.\n  account_count > 1                                               AS multi_account_customer,\n\n  -- fence values, exposed so consumers never re-derive them\n  first_signup,\n  first_subscribed,\n  first_sync,\n  last_cancel_month,\n  account_count\n\nFROM churn_state\n","tests":["accepted_values(zone)","assert_int_demos_every_meeting_explained","assert_int_demos_grain_unique","assert_int_demos_is_demo_matches_zone","assert_int_demos_no_partial_month","not_null(demo_date)","not_null(entity_record_id)"]},{"name":"int_ga4_events","alias":"int_ga4_events","relation_name":"`project-for-method-dw`.`revenue`.`int_ga4_events`","description":"Flattened GA4 events with QA/dev traffic removed. The only model that\nshould read the raw analytics_274554544 export.\n\nExcludes ~23.3% of raw events that come from automated test instances\n(m11*.methodwarehouse.com regression suites, mta*) and local dev boxes\n(*.methodlocal.com) carrying the production GTM container. Regex validated\nagainst all 290 hostnames in the export: 14,993 of 64,298 events excluded,\n9 missed, zero false positives. Customer app subdomains on method.me are\nreal traffic and are kept.\n\nGRAIN: one row per GA4 event.\n\nNO IDENTITY YET. user_id is NULL on 100% of rows because nothing writes it\nat sign-in. Events cannot be joined to a Method account. user_pseudo_id\n(the _ga client id) is the only visitor handle, and it is per-browser, so\nany journey built here is anonymous and does not survive a device change.\n\nNO HISTORY BEFORE 2026-08-28. The GA4 BigQuery link was created that day\nand GA4 does not backfill.\n\nMIXED COMPLETENESS. Spans complete daily tables and partial intraday\ntables. Filter is_intraday = false for anything that must reconcile.\n","original_file_path":"models/marketing/int_ga4_events.sql","refs":[],"sources":["events___"],"columns":[{"name":"event_date","description":"Event date in the property's timezone, parsed from the GA4 event_date string."},{"name":"event_at","description":"Event timestamp (UTC), from event_timestamp microseconds."},{"name":"is_intraday","description":"TRUE when the row came from an events_intraday_* table (partial, best-effort). FALSE for the complete daily export. Exclude intraday for reconciliation."},{"name":"event_name","description":"GA4 event name (page_view, session_start, form_start, scroll, etc.)."},{"name":"user_pseudo_id","description":"GA4 client id, the value of the _ga cookie. Per-browser, not per-person. The only visitor identifier available today."},{"name":"user_id","description":"GA4 reserved User-ID, set by GTM from 2026-08-31. Kept for GA4's own UI reporting and cross-device stitching. PREFER account_slug for warehouse joins: user_id has no history before 2026-08-31 and briefly carried malformed full-URL values that day, which this model nulls out.\n"},{"name":"account_slug","description":"Method account slug parsed from the URL. THIS is the identity the warehouse should join on, not user_id. Sourced from ?accountName= on the signup completion page and from ?returnUrl=https://<slug>.method.me on sign-in, so it resolves for every event ever exported rather than only from the day GTM identification was switched on. Non-account subdomains (www, grow, forums, help, info, blog, status, signin, signup) are excluded. Joins to revenue.Account.CompanyAccount; about 24% do not match, mostly QA accounts and nightly-mirror lag on same-day signups.\n"},{"name":"page_host","description":"Hostname parsed from page_location, lowercased. NULL on events with no page_location."},{"name":"page_path","description":"Path portion of page_location with query string and trailing slash stripped. NULL when empty."},{"name":"page_location","description":"Full page URL as GA4 recorded it."},{"name":"page_referrer","description":"Referring URL for the pageview, where GA4 captured one."},{"name":"page_title","description":"Document title at the time of the event."},{"name":"ga_session_id","description":"GA4 session identifier. Unique only within a user_pseudo_id, so key sessions as (user_pseudo_id, ga_session_id)."},{"name":"gclid","description":"Google Ads click id when present. Bridges to the google_ads dataset. Sparse (285 of 64,414 events at time of writing)."},{"name":"user_type","description":"Custom parameter pushed by the sign-in app's dataLayer (e.g. SignedUp_More_1_Day_Ago). Coarse, not an identity."},{"name":"traffic_source","description":"GA4's session traffic source dimension."},{"name":"traffic_medium","description":"GA4's session traffic medium dimension."},{"name":"traffic_campaign","description":"GA4's session campaign name dimension."},{"name":"device_category","description":"desktop / mobile / tablet."},{"name":"country","description":"Country from GA4 geo enrichment."},{"name":"page_area","description":"Coarse bucket over page_host and page_path so downstream models stop re-deriving it. One of blog, pricing_guides, resources, marketing_site, campaign_landing, signup, signin, forums, app, unknown. `app` is real customer usage on *.method.me instances, not noise."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Flattened, cleaned GA4 events. The ONLY thing that should read the raw\n-- analytics_274554544 export. Everything downstream reads this.\n--\n-- Two jobs:\n--\n-- 1. Drop QA and dev traffic. ~23.3% of raw events come from automated test\n--    instances (m11*.methodwarehouse.com regression suites, mta*) and local\n--    dev boxes (*.methodlocal.com) that carry the production GTM container.\n--    290 distinct hostnames. The regex was tested against every host present:\n--    it excludes 14,993 of 64,298 events, misses 9 (two low-volume test hosts),\n--    and produces zero false positives. Customer app subdomains on method.me\n--    (watterslandscape.method.me etc.) are REAL traffic and are kept.\n--\n--    Filtering here rather than in GTM is deliberate: it is reversible, it is\n--    in git, and it preserves the raw rows. A GTM exception would discard them\n--    permanently. Quota is not a factor (0.07M events/day against a 1M limit).\n--    GTM filtering is still worth doing for the GA4 UI's own reporting, which\n--    this model cannot fix -- but it is not a prerequisite for anything here.\n--\n-- 2. Flatten the event_params people actually use, so downstream models are not\n--    re-writing UNNEST subqueries. Raw nested columns stay available via the\n--    source if something unusual is needed.\n--\n-- Grain: one row per GA4 event.\n--\n-- CAVEAT -- no identity. user_id is NULL on 100% of rows today (0 of 64,392 as\n-- of 2026-08-30) because nothing writes it at sign-in. user_pseudo_id is the\n-- _ga client id and is the only handle on a visitor. Until user_id is set,\n-- these events CANNOT be joined to a Method account, and any \"journey\" built\n-- from this model is anonymous. See docs: T3 in the attribution plan.\n--\n-- CAVEAT -- both complete and partial days. events_* spans daily tables\n-- (complete) and events_intraday_* (best-effort, incomplete). is_intraday\n-- flags which is which; exclude intraday for anything that must reconcile.\n\nwith raw_events as (\n\n    select\n        _TABLE_SUFFIX as table_suffix,\n        event_date,\n        event_timestamp,\n        event_name,\n        user_pseudo_id,\n\n        -- Guard against malformed user_id. On 2026-08-31 between 20:20 and\n        -- ~20:30 UTC a GTM RegEx Table variable shipped without capture-group\n        -- substitution enabled, so user_id was set to the whole page URL\n        -- instead of the account slug (101 events, 14 distinct values). Fixed\n        -- in GTM, but those rows persist in the raw export, which we never\n        -- edit -- Google owns those tables and rewrites intraday nightly.\n        -- A valid value is a CompanyAccount slug: lowercase, no scheme, no\n        -- slashes. Anything else becomes NULL rather than being dropped, so\n        -- the event still counts as traffic and only the identity is voided.\n        case\n            when user_id is null                                  then null\n            when regexp_contains(user_id, r'^[a-z0-9][a-z0-9-]{1,62}$') then user_id\n            else null\n        end                                                       as user_id,\n        event_params,\n        user_properties,\n        traffic_source,\n        device,\n        geo\n    from {{ source('ga4', 'events_*') }}\n\n), flattened as (\n\n    select\n        parse_date('%Y%m%d', event_date)                      as event_date,\n        timestamp_micros(event_timestamp)                     as event_at,\n        starts_with(table_suffix, 'intraday_')                as is_intraday,\n        event_name,\n        user_pseudo_id,\n        user_id,\n\n        (select value.string_value from unnest(event_params) where key = 'page_location') as page_location,\n        (select value.string_value from unnest(event_params) where key = 'page_referrer') as page_referrer,\n        (select value.string_value from unnest(event_params) where key = 'page_title')    as page_title,\n        (select value.int_value    from unnest(event_params) where key = 'ga_session_id') as ga_session_id,\n        (select value.string_value from unnest(event_params) where key = 'gclid')         as gclid,\n        (select value.string_value from unnest(event_params) where key = 'userType')      as user_type,\n\n        traffic_source.source                                 as traffic_source,\n        traffic_source.medium                                 as traffic_medium,\n        traffic_source.name                                   as traffic_campaign,\n        device.category                                       as device_category,\n        geo.country                                           as country\n\n    from raw_events\n\n), parsed as (\n\n    select\n        *,\n\n        -- Account slug derived from the URL, independent of GA4's user_id.\n        --\n        -- This is the better source and it should be preferred. The account has\n        -- always been present in page_location -- ?accountName=<slug> on the\n        -- signup completion page, and ?returnUrl=https://<slug>.method.me on\n        -- sign-in -- so it can be extracted here for EVERY event already\n        -- collected, back to the export going live on 2026-08-28.\n        --\n        -- GTM's user_id only applies going forward and is what produced the\n        -- malformed rows above. Keep both: user_id drives GA4's own UI\n        -- reporting and cross-device stitching, account_slug is what the\n        -- warehouse should join on.\n        --\n        -- Non-account subdomains are excluded so a returnUrl pointing at\n        -- www/grow/forums does not masquerade as a customer.\n        coalesce(\n            lower(regexp_extract(page_location, r'(?i)[?&]accountName=([^&#]+)')),\n            nullif(\n                regexp_replace(\n                    lower(coalesce(regexp_extract(\n                        page_location,\n                        r'(?i)returnUrl=https?(?::|%3A)(?:/{2}|%2F%2F)([^./%]+)\\.method\\.me'\n                    ), '')),\n                    r'^(www|grow|forums|help|info|blog|status|signin|signup)$', ''\n                ), ''\n            )\n        )                                                                    as account_slug,\n\n        lower(regexp_extract(page_location, r'^(?:https?://)?([^/?#]+)'))    as page_host,\n        regexp_replace(\n            regexp_extract(lower(page_location), r'^(?:https?://)?[^/?#]*([^?#]*)'),\n            r'/$', ''\n        )                                                                    as page_path\n    from flattened\n\n)\n\nselect\n    event_date,\n    event_at,\n    is_intraday,\n    event_name,\n    user_pseudo_id,\n    user_id,\n    account_slug,\n    page_host,\n    nullif(page_path, '') as page_path,\n    page_location,\n    page_referrer,\n    page_title,\n    ga_session_id,\n    gclid,\n    user_type,\n    traffic_source,\n    traffic_medium,\n    traffic_campaign,\n    device_category,\n    country,\n\n    -- Coarse buckets so downstream models stop re-deriving them. `app` is real\n    -- customer usage on *.method.me subdomains and on signin/signup, not noise.\n    case\n        when page_host in ('www.method.me', 'method.me')\n             and starts_with(page_path, '/blog')              then 'blog'\n        when page_host in ('www.method.me', 'method.me')\n             and starts_with(page_path, '/pricing-guides')    then 'pricing_guides'\n        when page_host in ('www.method.me', 'method.me')\n             and starts_with(page_path, '/resources')         then 'resources'\n        when page_host in ('www.method.me', 'method.me')      then 'marketing_site'\n        when page_host = 'grow.method.me'                     then 'campaign_landing'\n        when starts_with(page_host, 'signup.')                then 'signup'\n        when starts_with(page_host, 'signin.')                then 'signin'\n        when page_host = 'forums.method.me'                   then 'forums'\n        when page_host is null                                then 'unknown'\n        else 'app'\n    end as page_area\n\nfrom parsed\n\n-- QA / dev exclusion. See the header comment for how this regex was validated.\nwhere page_host is null\n   or not regexp_contains(page_host, r'^m\\d|^mta|\\.methodlocal\\.com$')\n","tests":[]},{"name":"int_marketing_spend","alias":"int_marketing_spend","relation_name":"`project-for-method-dw`.`revenue`.`int_marketing_spend`","description":"Marketing spend actuals per channel per month, pivoted from marketing.sheet_cac_raw (the scripted load of the marketing tracker's CAC:ARR tabs, scripts/marketing_spend_sync.py). One row per (channel, block, month), Actuals scenario only. Includes non-canonical blocks (TOTAL, PPC sub-splits, Partners w/o events); int_channel_cac selects the canonical set. sheet_* columns are the sheet's own computed numbers, kept for reconciliation.\n","original_file_path":"models/marketing/int_marketing_spend.sql","refs":[],"sources":["sheet_cac_raw"],"columns":[{"name":"channel","description":"Channel key. Canonical values match int_customers.AttributionChannel (PPC, SEO, OPN, Partners, Email); TOTAL / PPC_TRIALGEN / PPC_BRANDING / Partners_NO_EVENTS are sheet-only rollups or splits."},{"name":"month","description":"Calendar month (first of month)."},{"name":"media_spend","description":"Channel media dollars (the per-channel \"Mktg X Spend\" sheet line; for SEO this is the content spend line)."},{"name":"team_spend","description":"Sheet's \"Mktg Team Overall Spend\" allocation for the channel-month."},{"name":"mktg_sales_cost","description":"Sheet's \"Mktg + Sales Cost\" allocation (people cost) for the channel-month."},{"name":"total_spend","description":"Sheet's \"Total Spend\" line (media + team + mktg/sales cost). Fully-loaded numerator."},{"name":"sheet_customers","description":"New customers as counted BY THE SHEET. Reconciliation only — differs from int_customers.IsNew by 10–25% on SEO/Partners. Do not use as a denominator."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Marketing spend actuals per channel per month, pivoted from the sheet load\n-- (marketing.sheet_cac_raw). One row per (channel, month), Actuals only —\n-- Budget/Forecast scenarios stay in the raw table for anyone who wants them.\n--\n-- media_spend picks up the per-channel media line, whose measure name varies\n-- by block (\"Mktg PPC Spend\", \"Mktg Content Spend\" for SEO, \"Mktg Partners +\n-- Events Spend\", ...) — everything shaped 'Mktg%Spend' except the team line.\n-- sheet_* columns are the sheet's own numbers, kept for reconciliation;\n-- int_channel_cac recomputes CAC against canonical customer counts.\n\nSELECT\n  channel,\n  block,\n  month,\n  SUM(IF(measure LIKE 'Mktg%Spend' AND measure != 'Mktg Team Overall Spend', value, 0)) AS media_spend,\n  SUM(IF(measure = 'Mktg Team Overall Spend', value, 0)) AS team_spend,\n  SUM(IF(measure = 'Mktg + Sales Cost', value, 0)) AS mktg_sales_cost,\n  SUM(IF(measure = 'Total Spend', value, 0)) AS total_spend,\n  MAX(IF(measure = 'Customers', value, NULL)) AS sheet_customers,\n  MAX(IF(measure = 'ARR', value, NULL)) AS sheet_arr,\n  MAX(IF(measure = 'CAC:ARR', value, NULL)) AS sheet_cac_arr\nFROM {{ source('marketing', 'sheet_cac_raw') }}\nWHERE scenario = 'Actuals'\nGROUP BY channel, block, month\n-- Future months exist in the sheet as formula zeros; keep only months where\n-- the block reported anything real.\nHAVING media_spend != 0 OR total_spend != 0 OR sheet_customers IS NOT NULL\n","tests":[]},{"name":"int_method_monday","alias":"int_method_monday","relation_name":"`project-for-method-dw`.`revenue`.`int_method_monday`","description":"Method Monday MTD + trajectory building block for the CURRENT month only. One row. Feeds the twelve v_metric__*_mtd / *_trajectory / *_forecast_mtd views. Every *_mtd figure excludes today. Trajectory is calendar-day linear (MTD / complete elapsed days * days in month) and is suppressed (NULL) for the first 7 days of the month — a one-or-two-day sample extrapolated across a month is dominated by day-of-week shape. Cannot answer for any month other than the current one.\n","original_file_path":"models/intermediate/int_method_monday.sql","refs":["int_trials","int_syncs"],"sources":["int_conversions","int_cancellations","int_bom_customers","method_forecast"],"columns":[{"name":"period","description":"First day of the current month. Always `DATE_TRUNC(CURRENT_DATE(), MONTH)`."},{"name":"elapsed_days","description":"Complete days elapsed this month, excluding today. Zero on the 1st."},{"name":"days_in_month","description":"Calendar days in the current month."},{"name":"min_trajectory_days","description":"Threshold below which trajectory is suppressed (7). Exposed so consumers read it rather than hardcoding the number.\n"},{"name":"trials_mtd","description":"Trials so far this month, excluding today."},{"name":"syncs_mtd","description":"Syncs so far this month, excluding today."},{"name":"conversions_mtd","description":"Conversions so far this month, excluding today."},{"name":"churn_mtd","description":"Distinct CompanyAccounts cancelled so far this month, excluding today."},{"name":"bom_customers","description":"Beginning-of-month customer base at CompanyAccount grain, from the most recently SETTLED month — not the current month, whose row is still accumulating.\n"},{"name":"trials_trajectory","description":"Calendar-day linear projection of full-month trials. NULL before day 7."},{"name":"syncs_trajectory","description":"Calendar-day linear projection of full-month syncs. NULL before day 7."},{"name":"conversions_trajectory","description":"Calendar-day linear projection of full-month conversions. NULL before day 7."},{"name":"churn_trajectory","description":"Calendar-day linear projection of full-month churn. NULL before day 7."},{"name":"churn_rate_mtd","description":"churn_mtd / (bom_customers + conversions_mtd), as a percentage."},{"name":"churn_rate_trajectory","description":"churn_trajectory / (bom_customers + conversions_trajectory), as a percentage. NULL before day 7, inherited from its inputs.\n"},{"name":"conversions_forecast_mtd","description":"Full-month conversion forecast prorated to the elapsed window. Not suppressed early."},{"name":"churn_forecast_mtd","description":"Full-month churn forecast prorated to the elapsed window. Not suppressed early."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Method Monday MTD + trajectory building block. ONE row, always the CURRENT\n-- month — `period` is `DATE_TRUNC(CURRENT_DATE(), MONTH)` by construction, so\n-- this view cannot answer for any other month.\n--\n-- Feeds twelve v_metric__* views: trials/syncs/conversions/churn × mtd and\n-- trajectory, plus sync_rate_mtd, churn_rate_mtd, churn_rate_trajectory and the\n-- two *_forecast_mtd views.\n--\n-- Adopted into dbt 2026-09-02. It was previously hand-written in the BigQuery\n-- console and existed nowhere in version control, while being the shared\n-- upstream for 15 of the 19 unmanaged views in `revenue_metrics`. The SQL below\n-- is that view's DDL, unchanged apart from ref()/source() substitution — the\n-- only behavioural change (the trajectory floor) was applied to the BQ view\n-- first and verified before this model was written. See\n-- knowledge/snapshots/2026-09-01-under-review-metrics-baseline.md.\n--\n-- MTD convention: every *_mtd figure EXCLUDES today. `elapsed_days` is\n-- complete days only, matching int_channel_funnel_trajectory.\n\nWITH bounds AS (\n  SELECT\n    DATE_TRUNC(CURRENT_DATE(), MONTH)                     AS period,\n    EXTRACT(DAY FROM CURRENT_DATE()) - 1                  AS elapsed_days,\n    EXTRACT(DAY FROM LAST_DAY(CURRENT_DATE(), MONTH))     AS days_in_month,\n    -- WHY TRAJECTORY IS BLANK EARLY IN THE MONTH (added 2026-09-02)\n    --\n    -- Trajectory is a calendar-day linear run-rate: MTD / elapsed_days *\n    -- days_in_month. With only a day or two elapsed that extrapolates a\n    -- single day across the whole month, and trials/syncs have a strong\n    -- day-of-week shape -- a month opening on a weekend projects far too\n    -- low, one opening on a Monday far too high. On 2026-09-02 the\n    -- unguarded formula read 450 trials against an August actual of 440,\n    -- off one day's data.\n    --\n    -- Seven days is the floor because it is the shortest window that\n    -- contains every weekday exactly once, so day-of-week effects cancel\n    -- instead of dominating. Below it we return NULL rather than a\n    -- plausible-looking wrong number: consumers should show MTD and the\n    -- prior month instead, and a blank cell reads as \"too early to say\".\n    --\n    -- This makes the family consistent with int_channel_funnel_trajectory,\n    -- which uses the same complete-days convention (anchored to a dated\n    -- Looker PDF: 14.5 / 8 * 31 = 56.19). Note this deliberately does NOT\n    -- match Looker's own Method Monday page, whose trajectory divides by\n    -- day-of-month while its MTD excludes today -- an inconsistent pairing\n    -- that understates by day/(day-1).\n    7                                                     AS min_trajectory_days\n),\nactuals AS (\n  SELECT\n    (SELECT COUNT(*) FROM {{ ref('int_trials') }} t, bounds b\n     WHERE DATE_TRUNC(t.SignupDate, MONTH) = b.period\n       AND t.SignupDate < CURRENT_DATE())                                    AS trials_mtd,\n    (SELECT COUNT(*) FROM {{ ref('int_syncs') }} s, bounds b\n     WHERE DATE_TRUNC(s.SyncDate, MONTH) = b.period\n       AND s.SyncDate < CURRENT_DATE())                                      AS syncs_mtd,\n    (SELECT COUNT(*) FROM {{ source('revenue', 'int_conversions') }} c, bounds b\n     WHERE DATE_TRUNC(c.FirstSaaSInvoiceTxnDate, MONTH) = b.period\n       AND c.FirstSaaSInvoiceTxnDate < CURRENT_DATE())                       AS conversions_mtd,\n    -- Churn is counted at CompanyAccount grain, matching metric 344's basis.\n    (SELECT COUNT(DISTINCT x.CompanyAccount)\n     FROM {{ source('revenue', 'int_cancellations') }} x, bounds b\n     WHERE DATE_TRUNC(x.CancellationDate, MONTH) = b.period\n       AND x.CancellationDate < CURRENT_DATE())                              AS churn_mtd,\n    -- Beginning-of-month customer base, CompanyAccount grain. This is the Churn\n    -- Rate denominator's BOM term; it does NOT scale with elapsed days, unlike\n    -- every other *_mtd figure above -- see the churn-rate section below.\n    --\n    -- BUG FIX 2026-08-17: the CURRENT month's own row is NOT used. It derives\n    -- from billing transactions that land throughout the month (July settled at\n    -- 3,788; August read 2,171 mid-month), so it understates the real base and\n    -- inflates the rate by ~75% if used directly. Metric 344's own pre-existing\n    -- chart_sql already knew this -- its `bom_curr` CTE reads\n    -- `DATE_SUB(CURRENT_DATE(), INTERVAL 1 MONTH)`, i.e. the PRIOR (settled)\n    -- month's row, specifically because the current month's isn't done\n    -- accumulating. This reproduces that exact behaviour: always read the most\n    -- recently SETTLED month's BOM row, one month back from `period`.\n    (SELECT COUNT(DISTINCT bc.CompanyAccount)\n     FROM {{ source('revenue', 'int_bom_customers') }} bc, bounds b\n     WHERE DATE_TRUNC(bc.TxnDate, MONTH) = DATE_SUB(b.period, INTERVAL 1 MONTH)) AS bom_customers\n),\nforecast AS (\n  SELECT\n    SUM(f.Forecasted_Trials)     AS trials_forecast,\n    SUM(f.Forecasted_Syncs)      AS syncs_forecast,\n    SUM(f.Forecasted_Conversion) AS conversions_forecast,\n    SUM(f.Forecasted_Churn)      AS churn_forecast\n  FROM {{ source('revenue', 'method_forecast') }} f, bounds b\n  WHERE f.Date IS NOT NULL\n    AND DATE_TRUNC(f.Date, MONTH) = b.period\n),\ncomputed AS (\n  SELECT\n    b.period,\n    b.elapsed_days,\n    b.days_in_month,\n    b.min_trajectory_days,\n\n    a.trials_mtd,\n    a.syncs_mtd,\n    a.conversions_mtd,\n    a.churn_mtd,\n    a.bom_customers,\n\n    f.trials_forecast,\n    f.syncs_forecast,\n    f.conversions_forecast,\n    f.churn_forecast,\n\n    -- Suppressed below min_trajectory_days -- see the bounds CTE comment.\n    IF(b.elapsed_days >= b.min_trajectory_days,\n       SAFE_DIVIDE(a.trials_mtd,      b.elapsed_days) * b.days_in_month, NULL) AS trials_trajectory,\n    IF(b.elapsed_days >= b.min_trajectory_days,\n       SAFE_DIVIDE(a.syncs_mtd,       b.elapsed_days) * b.days_in_month, NULL) AS syncs_trajectory,\n    IF(b.elapsed_days >= b.min_trajectory_days,\n       SAFE_DIVIDE(a.conversions_mtd, b.elapsed_days) * b.days_in_month, NULL) AS conversions_trajectory,\n    IF(b.elapsed_days >= b.min_trajectory_days,\n       SAFE_DIVIDE(a.churn_mtd,       b.elapsed_days) * b.days_in_month, NULL) AS churn_trajectory,\n\n    -- Forecast prorated to the same elapsed window, so the MTD bars compare\n    -- like with like. Looker's Conversions and Churn cards do this.\n    -- NOT suppressed early: these restate the forecast over the elapsed window\n    -- rather than extrapolating a small sample, so they carry no day-of-week risk.\n    SAFE_DIVIDE(f.conversions_forecast * b.elapsed_days, b.days_in_month) AS conversions_forecast_mtd,\n    SAFE_DIVIDE(f.churn_forecast       * b.elapsed_days, b.days_in_month) AS churn_forecast_mtd\n  FROM bounds b, actuals a, forecast f\n)\nSELECT\n  c.*,\n\n  -- Churn Rate: churn / (BOM + conversions), on the same complete-days\n  -- convention as everything else here. BOM does NOT scale with elapsed days --\n  -- unlike the sync conversion rate, actual and trajectory are genuinely\n  -- different numbers, not one value twice.\n  --\n  -- Denominator choice (BOM + conversions, not BOM alone) is empirically\n  -- settled: verified against Looker on 2026-08-04, Apr 2026 = 2.41% and\n  -- Jun 2026 = 2.70%, both exact only with conversions included.\n  -- bom_customers is already the PRIOR (settled) month's BOM row -- see that\n  -- column's own comment above for why.\n  --\n  -- We divide by the real (settled) base. Looker's Churn Rate Trajectory\n  -- divides by the FORECAST's implied base instead (Forecasted_Churn /\n  -- Forecasted_Churn_Rate__ = 99 / 0.025 = 3960), which is why its reading\n  -- (3.61% as of 2026-08-17) differs from ours (3.73%) -- an intentional,\n  -- documented divergence, not a parity failure.\n  --\n  -- churn_rate_trajectory inherits the early-month suppression for free:\n  -- churn_trajectory and conversions_trajectory are NULL below\n  -- min_trajectory_days, so this is NULL too.\n  SAFE_DIVIDE(c.churn_mtd, c.bom_customers + c.conversions_mtd) * 100 AS churn_rate_mtd,\n  SAFE_DIVIDE(c.churn_trajectory, c.bom_customers + c.conversions_trajectory) * 100 AS churn_rate_trajectory\nFROM computed c\n","tests":[]},{"name":"int_motion_funnel","alias":"int_motion_funnel","relation_name":"`project-for-method-dw`.`revenue`.`int_motion_funnel`","description":"Per-customer motion + lifecycle funnel, entity grain — one row per trialer. Spine (trial/sync/convert) + talked-to-us fork (demo or free consulting, attended, pre-convert) + customization + DEP/prepay/industry lenses + a 1/3/6/12-month retention curve (numerator retained_Kmo, denominator eligible_Kmo). Directional: motion is only trustworthy for 2024+ cohorts (motion_trackable). Status directional; lives in revenue, not revenue_metrics.\n","original_file_path":"models/intermediate/int_motion_funnel.sql","refs":["int_trials","int_syncs","int_customer_mrr","int_customers","int_presale_touches","int_customer_proserv"],"sources":["TransLineFlattened","v_entity_primary_label"],"columns":[{"name":"EntityRecordID","description":""},{"name":"signup_month","description":""},{"name":"motion","description":""},{"name":"user_tier","description":""},{"name":"ever_had_dep","description":"TRUE if the customer EVER had a DEP module on their subscription (LOGICAL_OR(HasDEP) over int_customers). Ever-flag, not point-in-time. Renamed from has_dep -> ever_had_dep 2026-07-10 (name-vs-math fix) to make the \"ever\" semantics explicit; zero logic change.\n"},{"name":"ever_prepay","description":"TRUE if the customer EVER had a SaaS line with SaaSPayType = 'Prepay' and a non-zero amount in TransLineFlattened. Ever-flag, not point-in-time. Renamed from is_prepay -> ever_prepay 2026-07-10 (name-vs-math fix); zero logic change."}],"compiled_sql":"{{ config(materialized='table') }}\n\n-- Per-customer motion + lifecycle funnel row. ENTITY grain — one row per trialer.\n-- Assembles the shipped spine (int_trials -> int_syncs -> int_customer_mrr) with the\n-- talked-to-us fork (int_presale_touches), customization (int_customer_proserv),\n-- DEP/prepay/industry lenses, and a multi-horizon retention curve computed per-entity\n-- from int_customer_mrr (same first-pay anchor int_customer_survival uses; no engine\n-- rebuilt). Directional: Activity-based motion is only trustworthy for 2024+ cohorts\n-- (motion_trackable). See docs/superpowers/specs/2026-06-29-acquisition-funnel-phase2-motion-lifecycle-design.md.\n\n{% set censor = var('motion_censor_month', none) %}\n\nWITH trials AS (\n  SELECT EntityRecordID, DATE_TRUNC(MIN(SignupDate), MONTH) AS signup_month\n  FROM {{ ref('int_trials') }}\n  GROUP BY 1\n),\nsyncs AS (\n  SELECT EntityRecordID, MIN(SyncDate) AS sync_date\n  FROM {{ ref('int_syncs') }}\n  GROUP BY 1\n),\nmrr AS (\n  SELECT EntityRecordID, Month, SUM(StartMRR) AS m\n  FROM {{ ref('int_customer_mrr') }}\n  GROUP BY 1, 2\n),\nconv AS (  -- first paying month = convert anchor t0\n  SELECT EntityRecordID, MIN(Month) AS convert_month\n  FROM mrr\n  WHERE m > 0\n  GROUP BY 1\n),\nconv_mrr AS (\n  SELECT c.EntityRecordID, c.convert_month, mr.m AS mrr0\n  FROM conv c JOIN mrr mr\n    ON mr.EntityRecordID = c.EntityRecordID AND mr.Month = c.convert_month\n),\ndep AS (\n  SELECT EntityRecordID, LOGICAL_OR(HasDEP) AS ever_had_dep\n  FROM {{ ref('int_customers') }}\n  GROUP BY 1\n),\nsizes AS (\n  SELECT EntityRecordID, MAX(TotalUsers) AS users\n  FROM {{ ref('int_customers') }}\n  GROUP BY 1\n),\nprepay AS (\n  SELECT EntityRecordID,\n    LOGICAL_OR(InvoiceGrouping = 'SaaS' AND SaaSPayType = 'Prepay' AND SaaSAmount != 0) AS ever_prepay\n  FROM {{ source('revenue', 'TransLineFlattened') }}\n  GROUP BY 1\n),\ncensor AS (\n  SELECT\n    {% if censor %}DATE('{{ censor }}'){% else %}DATE_SUB(DATE_TRUNC(CURRENT_DATE(), MONTH), INTERVAL 1 MONTH){% endif %} AS censor_month\n)\nSELECT\n  t.EntityRecordID,\n  t.signup_month,\n  s.sync_date IS NOT NULL                          AS synced,\n  cm.convert_month IS NOT NULL                     AS converted,\n  cm.convert_month,\n  CAST(cm.mrr0 AS NUMERIC)                         AS mrr0,\n  -- talked = attended a demo/free session on or before the convert month\n  -- (or any time, if never converted). Else self_serve.\n  CASE\n    WHEN COALESCE(pt.attended_any, FALSE)\n      AND (cm.convert_month IS NULL\n           OR pt.first_attended_date < DATE_ADD(cm.convert_month, INTERVAL 1 MONTH))\n    THEN 'talked' ELSE 'self_serve'\n  END                                              AS motion,\n  t.signup_month >= DATE('2024-01-01')             AS motion_trackable,\n  COALESCE(pt.demo_booked, FALSE)                  AS demo_booked,\n  COALESCE(pt.demo_attended, FALSE)                AS demo_attended,\n  COALESCE(pt.free_booked, FALSE)                  AS free_booked,\n  COALESCE(pt.free_attended, FALSE)                AS free_attended,\n  COALESCE(ps.is_customized, FALSE)                AS is_customized,\n  COALESCE(ps.ps_gross, 0)                         AS ps_gross,\n  COALESCE(d.ever_had_dep, FALSE)                   AS ever_had_dep,\n  COALESCE(pp.ever_prepay, FALSE)                   AS ever_prepay,\n  COALESCE(ind.l1, 'Unclassified')                  AS industry_l1,\n  -- customer size bucket derived from peak user count\n  CASE\n    WHEN sz.users IS NULL THEN 'Unknown'\n    WHEN sz.users <= 1     THEN 'Solo'\n    WHEN sz.users <= 4     THEN 'Small (2-4)'\n    WHEN sz.users <= 10    THEN 'SMB (5-10)'\n    ELSE                        'Mid (11+)'\n  END                                               AS user_tier,\n  -- retention horizons (numerator = alive at t0+K; eligible = t0+K observable)\n  COALESCE(r1.m, 0) > 0                             AS retained_1mo,\n  DATE_ADD(cm.convert_month, INTERVAL 1 MONTH)  <= c.censor_month AS eligible_1mo,\n  COALESCE(r3.m, 0) > 0                             AS retained_3mo,\n  DATE_ADD(cm.convert_month, INTERVAL 3 MONTH)  <= c.censor_month AS eligible_3mo,\n  COALESCE(r6.m, 0) > 0                             AS retained_6mo,\n  DATE_ADD(cm.convert_month, INTERVAL 6 MONTH)  <= c.censor_month AS eligible_6mo,\n  COALESCE(r12.m, 0) > 0                            AS retained_12mo,\n  DATE_ADD(cm.convert_month, INTERVAL 12 MONTH) <= c.censor_month AS eligible_12mo\nFROM trials t\nCROSS JOIN censor c\nLEFT JOIN syncs s        ON s.EntityRecordID = t.EntityRecordID\n-- Signup gate: a conversion only counts for this cohort if it happened at/after\n-- the trial. Drops returning customers whose first-ever paying month predates\n-- their (latest) trial signup — otherwise an old conversion pollutes a recent\n-- cohort and shows as bogus \"mature\" retention. Mirrors int_customer_survival.\nLEFT JOIN conv_mrr cm    ON cm.EntityRecordID = t.EntityRecordID\n                        AND cm.convert_month >= t.signup_month\nLEFT JOIN {{ ref('int_presale_touches') }} pt ON pt.EntityRecordID = t.EntityRecordID\nLEFT JOIN {{ ref('int_customer_proserv') }} ps ON ps.EntityRecordID = t.EntityRecordID\nLEFT JOIN dep d          ON d.EntityRecordID = t.EntityRecordID\nLEFT JOIN sizes sz       ON sz.EntityRecordID = t.EntityRecordID\nLEFT JOIN prepay pp      ON pp.EntityRecordID = t.EntityRecordID\nLEFT JOIN {{ source('v7_classification', 'v_entity_primary_label') }} ind\n                         ON ind.customer_record_id = t.EntityRecordID\nLEFT JOIN mrr r1  ON r1.EntityRecordID = t.EntityRecordID  AND r1.Month  = DATE_ADD(cm.convert_month, INTERVAL 1 MONTH)\nLEFT JOIN mrr r3  ON r3.EntityRecordID = t.EntityRecordID  AND r3.Month  = DATE_ADD(cm.convert_month, INTERVAL 3 MONTH)\nLEFT JOIN mrr r6  ON r6.EntityRecordID = t.EntityRecordID  AND r6.Month  = DATE_ADD(cm.convert_month, INTERVAL 6 MONTH)\nLEFT JOIN mrr r12 ON r12.EntityRecordID = t.EntityRecordID AND r12.Month = DATE_ADD(cm.convert_month, INTERVAL 12 MONTH)\nWHERE t.signup_month >= DATE('2020-01-01')\n","tests":["accepted_values(motion)","accepted_values(user_tier)","assert_motion_funnel_invariants","not_null(EntityRecordID)","not_null(motion)","not_null(signup_month)","not_null(user_tier)","unique(EntityRecordID)"]},{"name":"int_mrr_movement_decomposed","alias":"int_mrr_movement_decomposed","relation_name":"`project-for-method-dw`.`revenue`.`int_mrr_movement_decomposed`","description":"Per (month, entity) month-over-month MRR change split into app_mrr / seat_mrr / price_mrr via a price–volume–mix decomposition, plus a movement_kind label (downgrade / expansion / cancellation / new / flat). Buckets sum to the net change. Validated 2026-06-03: reconciles to int_customer_mrr movement totals at $0.00 across 96 (month, kind) pairs (scripts/parity_mrr_decomposition_vs_customer_mrr.py).\n","original_file_path":"models/intermediate/int_mrr_movement_decomposed.sql","refs":["int_customer_mrr_lines"],"sources":["TransLineFlattened"],"columns":[{"name":"month","description":"First of the calendar month (DATE_TRUNC of TxnDate)."},{"name":"entity_record_id","description":"Stable numeric entity id (join key; survives company renames)."},{"name":"movement_kind","description":"downgrade | expansion | cancellation | new | flat (by net MoM change)."},{"name":"seat_mrr","description":"Portion of the change from seat/user (Qty) movement."},{"name":"app_mrr","description":"Portion from modules (ItemFullName) added or dropped."},{"name":"price_mrr","description":"Portion from unit-rate / discount changes."}],"compiled_sql":"-- VALIDATED 2026-06-03. Decomposes each customer-entity's month-over-month MRR\n-- change into APP / SEAT / PRICE via a price–volume–mix split. Grain: one row per (month, entity).\n-- Validation evidence (see _mrr_decomposition.yml):\n--   identity seat+app+price = p2_saas-p1_saas holds within $0.01\n--   reconciles to int_customer_mrr movement totals at $0.00 across 96 (month,kind) pairs\n--   churn detection + symmetric Prepay-Expiry exclusion + in-progress-month guard applied\n--   app   = a module (Service ItemFullName) added or dropped entirely\n--   seat  = same module, change in Qty (users)        -> Δqty * prior unit-rate\n--   price = same module, change in unit-rate (residual) + any Discount-line change\n\n{{ config(materialized='table') }}\n\nwith lines as (\n    select * from {{ ref('int_customer_mrr_lines') }}\n),\n\n-- Per-(entity, month) Prepay-Expiry line counts, computed with the IDENTICAL\n-- filter + COUNTIF logic as int_customer_mrr's `entity_monthly` CTE so the\n-- exclusion condition matches to the penny. A customer-month is PE-excluded\n-- when its prior book was ENTIRELY Prepay-Expiry Income (every non-zero-SaaS\n-- line was a PE line). int_customer_mrr zeroes such a month's StartMRR AND\n-- Cancellations; the decomposition feeder has no PE awareness, so without this\n-- the decomposition over-counts these phantom PE books as real cancellations.\npe_entity_monthly as (\n    select\n        date_trunc(TxnDate, month) as month,\n        EntityRecordID             as entity_record_id,\n        countif(SaaSAmount != 0)   as saas_lines,\n        countif(SaaSAmount != 0 and AccountFullName like '%Prepay Expiry Income%') as expiry_lines\n    from {{ source('revenue', 'TransLineFlattened') }}\n    where TxnDate >= '2021-12-01'\n      and format_date('%Y-%m', TxnDate) < format_date('%Y-%m', current_date())\n      and CompanyAccount not like 'm11%'\n      and CompanyAccount not like 'm18%'\n    group by 1, 2\n),\n\n-- Flag: was this (entity, month) book ENTIRELY Prepay-Expiry?\n-- Mirrors int_customer_mrr's condition: p1_expiry_lines > 0 AND p1_expiry_lines = p1_saas_lines.\npe_flag as (\n    select month, entity_record_id,\n        (expiry_lines > 0 and expiry_lines = saas_lines) as is_all_pe\n    from pe_entity_monthly\n),\n\nem as (\n    select month, entity_record_id, sum(saas) as cur\n    from lines group by 1, 2\n),\n\ntot as (   -- current + prior calendar month, full-outer so churned entities (prior-only) get a cur=0 row\n    select * from (\n        select\n            coalesce(a.month, date_add(b.month, interval 1 month)) as month,\n            coalesce(a.entity_record_id, b.entity_record_id)       as entity_record_id,\n            ifnull(a.cur, 0) as cur,\n            b.cur            as prv\n        from em a\n        full outer join em b\n          on a.entity_record_id = b.entity_record_id\n         and b.month = date_sub(a.month, interval 1 month)\n    )\n    -- Don't synthesize a churn row for the in-progress month: a prior-only entity\n    -- last booked in month M-1 would otherwise appear as a cur=0 cancellation in the\n    -- current (incomplete) month. int_customer_mrr guards the same way (its entity_paired\n    -- UNION ALL excludes synthesizing months >= current). Without this the model carries\n    -- phantom in-progress churn that any consumer must remember to filter.\n    where month < date_trunc(current_date(), month)\n),\n\npaired as (   -- item-level current vs prior month (full outer = catches add/drop)\n    select\n        coalesce(c.entity_record_id, p.entity_record_id)        as entity_record_id,\n        coalesce(c.month, date_add(p.month, interval 1 month))  as month,\n        coalesce(c.is_discount, p.is_discount)                  as is_discount,\n        ifnull(c.qty, 0)  as cq, ifnull(p.qty, 0)  as pq,\n        ifnull(c.saas, 0) as cs, ifnull(p.saas, 0) as ps\n    from lines c\n    full outer join lines p\n      on  c.entity_record_id = p.entity_record_id\n      and c.item  = p.item\n      and c.month = date_add(p.month, interval 1 month)\n),\n\neff as (\n    select entity_record_id, month,\n        case when not is_discount and (cs = 0 or ps = 0)\n             then cs - ps else 0 end as app,\n        case when not is_discount and cs <> 0 and ps <> 0\n             then (cq - pq) * safe_divide(ps, pq) else 0 end as seat,\n        case when not is_discount and cs <> 0 and ps <> 0\n             then (cs - ps) - (cq - pq) * safe_divide(ps, pq)\n             when is_discount then cs - ps\n             else 0 end as price\n    from paired\n)\n\nselect\n    t.month,\n    t.entity_record_id,\n    t.prv as p1_saas,\n    t.cur as p2_saas,\n    case\n        when t.prv > 0 and t.cur > 0 and t.cur < t.prv then 'downgrade'\n        when t.prv > 0 and t.cur > 0 and t.cur > t.prv then 'expansion'\n        -- PE-excluded: prior book (month M-1) was entirely Prepay-Expiry, so\n        -- int_customer_mrr zeroes this cancellation. Reclassify to 'flat' so it\n        -- drops out of the cancellation bucket (and the reconciliation gate,\n        -- which excludes 'flat'). Components / p1_saas / p2_saas are untouched,\n        -- so the seat+app+price = p2_saas-p1_saas identity still holds.\n        when t.prv > 0 and t.cur = 0 and coalesce(pe.is_all_pe, false) then 'flat'\n        when t.prv > 0 and t.cur = 0                   then 'cancellation'\n        when (t.prv is null or t.prv = 0) and t.cur > 0 then 'new'\n        else 'flat'\n    end as movement_kind,\n    round(sum(e.app),   2) as app_mrr,\n    round(sum(e.seat),  2) as seat_mrr,\n    round(sum(e.price), 2) as price_mrr\nfrom tot t\njoin eff e\n  on e.entity_record_id = t.entity_record_id and e.month = t.month\nleft join pe_flag pe\n  on pe.entity_record_id = t.entity_record_id\n and pe.month = date_sub(t.month, interval 1 month)\ngroup by 1, 2, 3, 4, 5\n","tests":["accepted_values(movement_kind)","not_null(entity_record_id)","not_null(month)","not_null(movement_kind)"]},{"name":"int_partner_accounts","alias":"int_partner_accounts","relation_name":"`project-for-method-dw`.`revenue`.`int_partner_accounts`","description":"One row per (Partner, CompanyAccount) — the accounts a referring partner brought in. Partner = raw revenue.Account.Partner string (no normalization in v1). IsActive is a lifecycle flag (FirstSaaSInvoiceTxnDate set AND no CancellationDate), matching the partner CRM \"Active?\" view (validated to an exact match against a reference partner's CRM export). MRR and Licenses are point-in-time billing for the latest complete month; they are 0 for accounts that did not bill that month — including hard-hold accounts that are still lifecycle-active. Excludes the internal \"Method Integration\" partner and m11/m18 test accounts. Account is deduped to one row per CompanyAccount. See spec docs/superpowers/specs/2026-06-25-partner-referral-views-design.md.\n","original_file_path":"models/partners/int_partner_accounts.sql","refs":[],"sources":["Account","TransLineFlattened"],"columns":[{"name":"Partner","description":"Referring partner, raw Account.Partner string (not normalized in v1)."},{"name":"CompanyAccount","description":"Method account identifier. One row per account."},{"name":"EntityRecordID","description":"Customer-level key. One customer can own accounts referred by different partners."},{"name":"SignupDate","description":"Account.FirstSaaSInvoiceTxnDate — first paid invoice date."},{"name":"CancellationDate","description":"Account cancellation date; NULL when not cancelled (the 0001-01-01 sentinel is normalized to NULL)."},{"name":"IsActive","description":"Lifecycle active — has a SignupDate and no CancellationDate. NOT the same as billing > 0 (hard-hold accounts are active but bill $0)."},{"name":"MRR","description":"SaaS billed by this account in the latest complete month (0 if none). Point-in-time, financially correct."},{"name":"Licenses","description":"Paid user count billed in the latest complete month (0 if none)."}],"compiled_sql":"{{ config(materialized='table') }}\n\n-- One row per (Partner, CompanyAccount): the accounts a partner referred.\n--\n-- Partner = revenue.Account.Partner (raw string; NO normalization in v1, so a\n--   partner whose name has a punctuation variant (\", Inc\" vs \" Inc\") stays split\n--   across rows — see spec follow-ups).\n-- IsActive uses the LIFECYCLE definition (first paid + not cancelled). It matches\n--   a reference partner's CRM \"Active?\" export exactly, and is the first\n--   account-grain active definition in the project (int_customers is\n--   customer-grain). MRR/Licenses are POINT-IN-TIME (latest complete month).\n-- IsActive and MRR intentionally do not reconcile: an account on a hard hold has\n--   no CancellationDate (IsActive = true) but bills $0 (MRR = 0), which is the\n--   financially correct figure. See memory project_hard_hold_billing_state and\n--   spec docs/superpowers/specs/2026-06-25-partner-referral-views-design.md.\n\nWITH latest_month AS (\n  -- Latest COMPLETE month, so the in-progress month never shows false zeros.\n  SELECT DATE_SUB(DATE_TRUNC(CURRENT_DATE(), MONTH), INTERVAL 1 MONTH) AS m\n),\naccounts AS (\n  -- revenue.Account fans out (~1.22 rows per EntityRecordID); dedup to one row\n  -- per CompanyAccount. See memory account_table_dedup.\n  SELECT\n    Partner,\n    CompanyAccount,\n    EntityRecordID,\n    FirstSaaSInvoiceTxnDate AS SignupDate,\n    NULLIF(CancellationDate, DATE '0001-01-01') AS CancellationDate\n  FROM (\n    SELECT\n      a.Partner, a.CompanyAccount, a.EntityRecordID,\n      a.FirstSaaSInvoiceTxnDate, a.CancellationDate,\n      ROW_NUMBER() OVER (PARTITION BY a.CompanyAccount ORDER BY a.RecordID DESC) AS rn\n    FROM {{ source('revenue', 'Account') }} a\n    WHERE a.Partner IS NOT NULL\n      AND a.Partner != ''\n      AND a.Partner != 'Method Integration'          -- internal partner\n      AND a.CompanyAccount NOT LIKE 'm11%'            -- test accounts\n      AND a.CompanyAccount NOT LIKE 'm18%'\n  )\n  WHERE rn = 1\n),\nbilling AS (\n  -- Point-in-time billing for the latest complete month, account grain.\n  SELECT\n    t.CompanyAccount,\n    SUM(t.SaaSAmount) AS MRR,\n    MAX(t.UserPaidCount) AS Licenses\n  FROM {{ source('revenue', 'TransLineFlattened') }} t, latest_month\n  WHERE DATE_TRUNC(t.TxnDate, MONTH) = latest_month.m\n    AND t.CompanyAccount NOT LIKE 'm11%'\n    AND t.CompanyAccount NOT LIKE 'm18%'\n  GROUP BY 1\n)\nSELECT\n  ac.Partner,\n  ac.CompanyAccount,\n  ac.EntityRecordID,\n  ac.SignupDate,\n  ac.CancellationDate,\n  (ac.SignupDate IS NOT NULL AND ac.CancellationDate IS NULL) AS IsActive,\n  CAST(COALESCE(b.MRR, 0) AS NUMERIC) AS MRR,\n  COALESCE(b.Licenses, 0) AS Licenses\nFROM accounts ac\nLEFT JOIN billing b ON ac.CompanyAccount = b.CompanyAccount\n","tests":["not_null(CompanyAccount)","not_null(EntityRecordID)","not_null(IsActive)","not_null(Licenses)","not_null(MRR)","not_null(Partner)","unique(CompanyAccount)"]},{"name":"int_presale_touches","alias":"int_presale_touches","relation_name":"`project-for-method-dw`.`revenue`.`int_presale_touches`","description":"Pre-sale human-touch signals per customer (entity grain), from the Activity table. Demo and free-consulting booked/attended states + first attended dates. attended_any drives the funnel's talked-to-us fork. Date is DueDateStart; tracking effectively starts 2024 (older cohorts read as untouched).\n","original_file_path":"models/intermediate/int_presale_touches.sql","refs":[],"sources":["Activity"],"columns":[{"name":"EntityRecordID","description":""},{"name":"attended_any","description":""}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Pre-sale human-touch signals per entity, from the Activity table.\n-- Date = DueDateStart (CreatedDate is NULL on ~93% of rows). Attended types only\n-- set the *_attended flags; booked/missed are tracked separately for show-rate.\n-- Tracking effectively starts 2024 — older cohorts read as untouched.\n\nWITH acts AS (\n  SELECT EntityRecordID, ActivityType, DueDateStart\n  FROM {{ source('revenue', 'Activity') }}\n  WHERE COALESCE(IsDeleted, FALSE) = FALSE\n    AND EntityRecordID IS NOT NULL\n)\nSELECT\n  EntityRecordID,\n  LOGICAL_OR(ActivityType IN ('Demo booked', 'Phone Call Demo Booked'))            AS demo_booked,\n  LOGICAL_OR(ActivityType IN ('Demo', 'Pre-sales Demo'))                           AS demo_attended,\n  MIN(IF(ActivityType IN ('Demo', 'Pre-sales Demo'), DueDateStart, NULL))          AS demo_first_date,\n  LOGICAL_OR(ActivityType = 'Free Consulting Booked')                              AS free_booked,\n  LOGICAL_OR(ActivityType = 'Free Consulting Session')                             AS free_attended,\n  MIN(IF(ActivityType = 'Free Consulting Session', DueDateStart, NULL))            AS free_first_date,\n  LOGICAL_OR(ActivityType IN ('Demo', 'Pre-sales Demo', 'Free Consulting Session')) AS attended_any,\n  MIN(IF(ActivityType IN ('Demo', 'Pre-sales Demo', 'Free Consulting Session'), DueDateStart, NULL)) AS first_attended_date\nFROM acts\nGROUP BY 1\n","tests":["not_null(EntityRecordID)","not_null(attended_any)","unique(EntityRecordID)"]},{"name":"int_syncs","alias":"int_syncs","relation_name":"`project-for-method-dw`.`revenue`.`int_syncs`","description":"One row per sync milestone event from Method's funnel pipeline.\nEvent-grain — ~91% of accounts have exactly 1 sync event; ~9% have 2+\nfrom re-syncs after disconnect/reconnect. Carries EntityRecordID,\nCompanyAccount, SyncDate, SignupDate, and attribution columns. Source:\nrevenue.Funnel filtered to EventType = 'Sync'. The intermediate that\nbacks the Syncs (#55) metric.\n","original_file_path":"models/intermediate/int_syncs.sql","refs":[],"sources":["Funnel"],"columns":[{"name":"EntityRecordID","description":""},{"name":"CompanyAccount","description":""},{"name":"SyncDate","description":""},{"name":"SignupDate","description":""},{"name":"AttributionChannel","description":""},{"name":"SignupCountry","description":""},{"name":"Vertical","description":""},{"name":"SyncType","description":""}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Intermediate model: one row per sync event recorded in the Funnel\n-- pipeline. Source: revenue.Funnel filtered to EventType = 'Sync'.\n--\n-- This is the DEFINITIVE definition. dbt owns this view at run time.\n-- The previous BQ-side hand-written DDL is being replaced by this model.\n-- If you change the filter / projection, change it here and re-run.\n--\n-- Renaming to int_syncs is deferred — handoff §12 commits to a single\n-- one-shot v_* → int_* rename PR after all 20 metrics are migrated.\n\nSELECT\n  EntityRecordID,\n  CAST(Date AS DATE) AS SyncDate,\n  SignupDate,\n  CompanyAccount,\n  EventType,\n  SyncType,\n  SyncTypeRegion,\n  SignupCountry,\n  Vertical,\n  Att_SEO, Att_Pay_Per_Click, Att_OPN_Other_Peoples_Networks, Att_Social,\n  Att_Direct, Att_Partners, Att_Content, Att_Remarketing, Att_Other, Att_None,\n  Att_Backlinks, Att_Banner_Ads, Att_Help_Center, Att_Online_Chat_Tool,\n  Att_Referral_Link, Att_Referral_Program, Att_Seminar_Conference,\n  CASE\n    WHEN Att_SEO = 1 THEN 'SEO'\n    WHEN Att_Pay_Per_Click = 1 THEN 'PPC'\n    WHEN Att_OPN_Other_Peoples_Networks = 1 THEN 'OPN'\n    WHEN Att_Social = 1 THEN 'Social'\n    WHEN Att_Referral_Link = 1 THEN 'Referral'\n    WHEN Att_Direct = 1 THEN 'Direct'\n    WHEN Att_Partners = 1 THEN 'Partners'\n    WHEN Att_Content = 1 THEN 'Content'\n    WHEN Att_Remarketing = 1 THEN 'Remarketing'\n    WHEN Att_Other = 1 THEN 'Other'\n    WHEN Att_None = 1 THEN 'None'\n    ELSE 'Unknown'\n  END AS AttributionChannel\nFROM {{ source('revenue', 'Funnel') }}\nWHERE EventType = 'Sync'\n","tests":[]},{"name":"int_trials","alias":"int_trials","relation_name":"`project-for-method-dw`.`revenue`.`int_trials`","description":"One row per Method account that began a trial. Account-grain — a customer\nwith 2 trial accounts contributes 2 rows. Excludes test accounts\n(IsConversionException), internal Method Integration partner rows, and\nthe '0001-01-01' sentinel date. Carries CompanyAccount, AttributionChannel,\nsignup country/vertical/sync type, and per-attribution-touch (Att_*)\ncolumns. The intermediate that backs the Trials (#54) metric.\n","original_file_path":"models/intermediate/int_trials.sql","refs":[],"sources":["Account"],"columns":[{"name":"EntityRecordID","description":""},{"name":"CompanyAccount","description":""},{"name":"SignupDate","description":""},{"name":"AttributionChannel","description":""},{"name":"SignupCountry","description":""},{"name":"Vertical","description":""},{"name":"SyncType","description":""}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Intermediate model: one row per Method account that began a trial.\n-- Source: revenue.Account, filtered to exclude conversion exceptions,\n-- internal Method Integration partner rows, and the '0001-01-01' sentinel\n-- (which marks \"no trial\").\n--\n-- This is the DEFINITIVE definition. dbt owns this view at run time.\n-- The previous BQ-side hand-written DDL is being replaced by this model.\n-- If you change the filter / projection, change it here and re-run.\n--\n-- Renaming to int_trials is deferred — handoff §12 commits to a single\n-- one-shot v_* → int_* rename PR after all 20 metrics are migrated.\n\nSELECT\n  EntityRecordID,\n  SignupDate, CompanyAccount, SignupCountry, SyncType, SyncTypeRegion, Vertical, CustDatIndustry,\n  Att_SEO, Att_Pay_Per_Click, Att_OPN_Other_Peoples_Networks, Att_Social, Att_Email,\n  Att_Referral_Link, Att_Referral_Program, Att_Direct, Att_Partners, Att_Content,\n  Att_Remarketing, Att_Other, Att_None, Att_Backlinks, Att_Banner_Ads,\n  Att_Help_Center, Att_Online_Chat_Tool, Att_Seminar_Conference,\n  CASE\n    WHEN Att_SEO = 1 THEN 'SEO'\n    WHEN Att_Pay_Per_Click = 1 THEN 'PPC'\n    WHEN Att_OPN_Other_Peoples_Networks = 1 THEN 'OPN'\n    WHEN Att_Social = 1 THEN 'Social'\n    WHEN Att_Email = 1 THEN 'Email'\n    WHEN Att_Referral_Link = 1 THEN 'Referral'\n    WHEN Att_Direct = 1 THEN 'Direct'\n    WHEN Att_Partners = 1 THEN 'Partners'\n    WHEN Att_Content = 1 THEN 'Content'\n    WHEN Att_Remarketing = 1 THEN 'Remarketing'\n    WHEN Att_Other = 1 THEN 'Other'\n    WHEN Att_None = 1 THEN 'None'\n    ELSE 'Unknown'\n  END AS AttributionChannel\nFROM {{ source('revenue', 'Account') }}\nWHERE IsConversionException = FALSE\n  AND Partner != 'Method Integration'\n  AND SignupDate != DATE('0001-01-01')\n","tests":[]},{"name":"v_channel_arr","alias":"v_channel_arr","relation_name":"`project-for-method-dw`.`revenue`.`v_channel_arr`","description":"DIRECTIONAL run-rate, NOT accounting-grade. New-customer ARR by marketing\nchannel, one row per (attribution channel x first-invoice month). Replicates\nthe marketing Looker \"Revenue by Channel\" dashboard. \"SaaS\" is each new\ncustomer's current monthly plan rate (Custdatlastsaasamount) allocated to its\nattribution channel — a run-rate snapshot, NOT invoiced revenue, so it does\nnot tie to QuickBooks/RevCogs (that uses SaaSAmount). Use for directional\nARR-by-channel storytelling only; the canonical run-rate is int_customer_mrr.\nFX is applied by the consumer: emits the pre-FX US / non-US SaaS split so\nCAD ARR = ((saas_us_portion*rate + saas_nonus_portion)/attribution_value)*12.\nExcludes test accounts, internal Method Integration partner rows, and the\ncurrent incomplete month.\n","original_file_path":"models/marketing/v_channel_arr.sql","refs":["int_attribution_fractional"],"sources":[],"columns":[{"name":"channel","description":"Attribution channel (SEO, PPC, Direct, OPN, Partners, Email, etc.), unpivoted from the Account Att_* flags."},{"name":"month","description":"First-invoice month (DATE_TRUNC of FirstSaaSInvoiceTxnDate) — the new-customer acquisition cohort, not signup month."},{"name":"customers","description":"Distinct new customers (CompanyAccount) attributed to this channel in this month."},{"name":"attribution_value","description":"Sum of fractional multi-touch attribution credit for the channel/month (a customer split across channels contributes a fraction to each)."},{"name":"saas_usd","description":"DIRECTIONAL run-rate. SUM(Custdatlastsaasamount * attribution_value), face value (pre-FX). Monthly plan rate allocated by attribution. NOT invoiced revenue."},{"name":"saas_us_portion","description":"Portion of saas_usd from US-AR accounts (gets the USD->CAD multiplier downstream)."},{"name":"saas_nonus_portion","description":"Portion of saas_usd from non-US accounts (CAN/Other — no FX multiplier; already local)."},{"name":"first_invoice_weighted","description":"Attribution-weighted SUM(first_invoice_net_saas * attribution_value). Divide by attribution_value for \"Avg First Invoice Revenue\" (matches Looker). Invoiced basis (SaaSAmount+SaaSExpense on TxnDate=FirstSaaSInvoiceTxnDate)."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- DIRECTIONAL metric: \"Channel ARR\" — marketing Revenue-by-Channel replica.\n-- Grain: (attribution channel x first-invoice month). One row per channel per month.\n--\n-- Built on the int_attribution_fractional PRIMITIVE (real multi-touch attribution\n-- — each customer's 1.0 of credit spread across the channels that touched them).\n--\n-- BASIS NOTE: uses Custdatlastsaasamount (the run-rate snapshot), NOT SaaSAmount.\n-- A deliberate, documented exception to the canonical-revenue-column rule — see\n-- migrate-metric-to-dbt SKILL.md \"run-rate / ARR carve-out\" and\n-- docs/metric-definitions.md \"Channel ARR\". DIRECTIONAL, not accounting-grade;\n-- lives in `revenue` (not `revenue_metrics`). Replicates the marketing Looker\n-- \"Revenue by Channel\" dashboard, penny-matched for May 2026.\n--\n-- FX applied DOWNSTREAM (the consumer) so the USD->CAD rate stays adjustable;\n-- this view emits the pre-FX US / non-US split:\n--   cad_arr = ((saas_us_portion * rate + saas_nonus_portion) / attribution_value) * 12\n--\n-- Current incomplete month excluded; trailing 24-month window.\n\nSELECT\n  channel,\n  DATE_TRUNC(FirstSaaSInvoiceTxnDate, MONTH)              AS month,\n  COUNT(DISTINCT CompanyAccount)                          AS customers,\n  SUM(attribution_weight)                                 AS attribution_value,\n  SUM(plan_rate * attribution_weight)                     AS saas_usd,\n  SUM(IF(is_us,     plan_rate * attribution_weight, 0))   AS saas_us_portion,\n  SUM(IF(NOT is_us, plan_rate * attribution_weight, 0))   AS saas_nonus_portion,\n  -- attribution-weighted, to match Looker's \"Avg First Invoice Revenue\"\n  -- (= first_invoice_weighted / attribution_value)\n  SUM(first_invoice_revenue * attribution_weight)         AS first_invoice_weighted\nFROM {{ ref('int_attribution_fractional') }}\nWHERE FirstSaaSInvoiceTxnDate IS NOT NULL\n  AND FirstSaaSInvoiceTxnDate != DATE('0001-01-01')\n  AND FirstSaaSInvoiceTxnDate >= DATE_SUB(DATE_TRUNC(CURRENT_DATE(), MONTH), INTERVAL 24 MONTH)\n  AND FirstSaaSInvoiceTxnDate <  DATE_TRUNC(CURRENT_DATE(), MONTH)\nGROUP BY channel, month\nORDER BY channel, month\n","tests":[]},{"name":"v_channel_arr_display","alias":"v_channel_arr_display","relation_name":"`project-for-method-dw`.`revenue`.`v_channel_arr_display`","description":"DIRECTIONAL presentation view for the Channel ARR scorecard — final display\ncolumns per (channel × first-invoice month) computed from v_channel_arr (on\nthe int_attribution_fractional real-multi-touch primitive). \"SaaS\" is the\nrun-rate (Custdatlastsaasamount) allocated by fractional attribution — NOT\ninvoiced revenue, does not tie to RevCogs. CAD ARR baked at a fixed 1.33 (stale\nif the USD→CAD rate moves). Penny-matched to the Looker \"Revenue by Channel\"\ndashboard (May 2026).\n\nPresentation layer only — it duplicates v_channel_arr with formatting + a baked\nFX rate. For definition questions prefer v_channel_arr itself.\n[REVIEW: worth exposing at all as a definition surface, or keep internal since\nit's display formatting over v_channel_arr?]\n","original_file_path":"models/marketing/v_channel_arr_display.sql","refs":["v_channel_arr"],"sources":[],"columns":[{"name":"channel","description":"Attribution channel (real multi-touch — see int_attribution_fractional)."},{"name":"month","description":"First-invoice month (new-customer acquisition cohort)."},{"name":"customers","description":"Distinct new customers attributed to the channel that month."},{"name":"attribution_value","description":"Summed fractional attribution credit (a customer split across channels contributes a fraction to each)."},{"name":"avg_first_invoice","description":"Attribution-weighted average first-invoice net SaaS (invoiced basis). = Σ(first_invoice×w)/attribution_value."},{"name":"saas","description":"DIRECTIONAL run-rate SaaS = Σ(Custdatlastsaasamount × weight). Plan rate, not invoiced."},{"name":"arpc","description":"Average revenue per customer = saas / attribution_value."},{"name":"arr","description":"Annual run-rate per customer = arpc × 12."},{"name":"cad_arr","description":"ARR converted to CAD (US × 1.33, non-US × 1), per customer. = ((US saas×1.33 + non-US saas)/attribution_value)×12."},{"name":"cad_arr_3mo","description":"3-month trailing rolling average of cad_arr per channel."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- DISPLAY view for the Channel ARR scorecard rawTable. Computes the final\n-- presentation columns from v_channel_arr (the channel×month aggregate on the\n-- fractional attribution primitive int_attribution_fractional).\n--\n-- DIRECTIONAL — run-rate basis (Custdatlastsaasamount); see v_channel_arr and\n-- docs/metric-definitions.md \"Channel ARR\". NOT accounting-grade.\n--\n-- FX rate is BAKED at a fixed 1.33 here (the scorecard rawTable SQL is static;\n-- the adjustable-rate box is a possible follow-up).\n--   cad_arr = ((saas_us_portion*1.33 + saas_nonus_portion)/attribution_value)*12\n\nWITH base AS (\n  SELECT\n    channel,\n    month,\n    customers,\n    ROUND(attribution_value, 2)                                                AS attribution_value,\n    ROUND(SAFE_DIVIDE(first_invoice_weighted, attribution_value), 2)           AS avg_first_invoice,\n    ROUND(saas_usd, 2)                                                         AS saas,\n    ROUND(SAFE_DIVIDE(saas_usd, attribution_value), 2)                        AS arpc,\n    ROUND(SAFE_DIVIDE(saas_usd, attribution_value) * 12, 2)                   AS arr,\n    ROUND(SAFE_DIVIDE(saas_us_portion * 1.33 + saas_nonus_portion, attribution_value) * 12, 2) AS cad_arr\n  FROM {{ ref('v_channel_arr') }}\n)\nSELECT\n  channel,\n  month,\n  customers,\n  attribution_value,\n  avg_first_invoice,\n  saas,\n  arpc,\n  arr,\n  cad_arr,\n  -- 3-month trailing rolling avg of CAD ARR per channel (assumes contiguous months)\n  ROUND(AVG(cad_arr) OVER (\n    PARTITION BY channel ORDER BY month\n    ROWS BETWEEN 2 PRECEDING AND CURRENT ROW\n  ), 2) AS cad_arr_3mo\nFROM base\nORDER BY month DESC, attribution_value DESC\n","tests":[]},{"name":"v_channel_funnel","alias":"v_channel_funnel","relation_name":"`project-for-method-dw`.`revenue`.`v_channel_funnel`","description":"CONSUMER VIEW. Trial to sync to paying funnel by CLICK-DERIVED channel,\nincluding AIO. Grain: one row per (channel x signup month).\n\nWHY IT EXISTS. Every other channel view in this project roots in\nrevenue.Account.Att_*. There is no Att_AIO column, and adding one costs\nthree coordinated repo deploys, so AI-sourced traffic can never appear in\nv_channel_scorecard, int_channel_funnel_daily, or anything built on them.\nThis view roots in int_cookie_clicks, which derives the channel from each\nclick's referrer and landing URL, so AIO is a first-class channel value.\n\nDOES NOT RECONCILE to the Att_*-derived views, by design. Those answer\n\"which channel does Alocet credit this account to\". This answers \"which\nchannels actually touched this account's browser\". Directionally\ncomparable, never penny-matched. Do not spend an afternoon on the delta.\n\nATTRIBUTION MODEL. Each trial account's 1.0 of credit is split evenly\nacross the distinct channels that touched its cookie. Simpler than the\nAlocet multi-touch engine, which applies first/last/middle percentages and\nzeroes Direct touches in mixed journeys. Even splitting is the honest\nfloor: touch ORDER is not reliable, because untagged repeat visits are\ndiscarded upstream.\n\nWINDOW STARTS 2025-01-01. CampaignAdjustedRecordID began syncing to\nBigQuery on 2026-09-01 with no history, so the channel is unresolvable for\nmost earlier clicks. Trial-cohort coverage is 30.8% in 2023 and 34.2% in\n2024, against 93.8% in 2025 and 90.7% in 2026. Extending the window back\nwould make every channel appear to grow in January 2025.\n\nAIO ITSELF IS NOT WINDOW-LIMITED. is_aio in int_cookie_clicks is derived\nfrom the referrer and URL, never from the campaign join, so AIO click\ncounts are available back to 2013. Query int_cookie_clicks directly for\nlong-run AIO click trends; use this view for the funnel.\n\nTHESE ARE FIRST-TOUCH MEASURES. Untagged channels (Direct, SEO, AIO) are\nrecorded only on a browser's first ever visit; every later untagged visit\nis discarded by the pixel. Tagged channels carry a trc code and are\nrecorded every time. Measured position in journey, 2025 onward: Facebook\nAds 24.3% later-touch, AdWords 22.2%, Bing 14.6%, against SEO 0.7%,\nDirect 0.2% and AIO 0.2%. Direct at 99.8% first-touch is the proof this\nis mechanical rather than behavioural, since typing a URL from memory is\ndefinitionally a return visit.\n\nSo `trials` for AIO means \"trials whose browser was FIRST introduced to\nMethod by an assistant\", not \"AI-influenced trials\". The second number is\nlarger and currently unmeasurable. The conversion RATE is still a clean\nread, because the cohort is coherent. It is the VOLUME that is understated,\nand understated more for untagged channels than tagged ones, so\ncross-channel volume comparisons are biased against AIO, Direct and SEO.\n","original_file_path":"models/marketing/v_channel_funnel.sql","refs":["int_trials","int_cookie_clicks","int_syncs"],"sources":["Account","Campaign"],"columns":[{"name":"channel","description":"Corrected top-level channel from int_cookie_clicks. AIO is promoted out of whatever the upstream rules credited it to. Other values are the Campaign name as recorded.\n"},{"name":"signup_month","description":"Month the trial account signed up, truncated. Current incomplete month excluded."},{"name":"trials","description":"Fractionally attributed trial accounts, FIRST-TOUCH basis for untagged channels. An account touched by three channels contributes 0.333 to each, so the column sums to the true account count across all channels. Canonical trial definition comes from int_trials. For AIO this counts browsers an assistant introduced, not browsers an assistant influenced.\n"},{"name":"synced","description":"Fractionally attributed trial accounts that reached a sync at any point, from int_syncs deduped to the account. Not restricted to a sync within the signup month, so recent months keep rising.\n"},{"name":"paying","description":"Fractionally attributed trial accounts that ever paid, using FirstSaaSInvoiceTxnDate past the 0001-01-01 never-paid sentinel. Lumpy at low volume: read a channel's rate over several months.\n"},{"name":"trial_accounts_touched","description":"Unweighted distinct account count, for sanity-checking the fractional measures. Double-counts an account touched by several channels, so it sums to more than the real trial total. Never present it as a channel split."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- DIRECTIONAL metric: trial -> sync -> paying funnel by CLICK-DERIVED channel.\n-- Grain: (channel x signup month). One row per channel per month.\n--\n-- WHY THIS EXISTS. Every other channel view in this project roots in\n-- revenue.Account.Att_*: int_attribution_fractional unpacks those 18 columns,\n-- and the single-touch AttributionChannel dimension is a CASE over them. There\n-- is no Att_AIO column and adding one costs three repo deploys, so AI-sourced\n-- traffic can never appear in v_channel_scorecard, int_channel_funnel_daily or\n-- anything built on them.\n--\n-- This view roots in int_cookie_clicks instead, which derives the channel from\n-- the click's referrer and landing URL. That makes AIO a first-class channel\n-- value with no upstream change. See the window note below for how far back\n-- the channel comparison is trustworthy.\n--\n-- DOES NOT RECONCILE to the Att_*-derived views, and is not meant to. They\n-- answer \"which channels does Alocet credit this account to\"; this answers\n-- \"which channels actually touched this account's browser\". Directionally\n-- comparable, never penny-matched. Do not chase the delta.\n--\n-- ATTRIBUTION MODEL. Each trial account's 1.0 of credit is split evenly across\n-- the distinct channels that touched its cookie. Deliberately simpler than the\n-- Alocet multi-touch engine, which applies first/last/middle percentages from\n-- CampaignAttributionTypes and zeroes Direct touches in mixed journeys. Even\n-- splitting is the honest floor: we do not have the touch ORDER for every\n-- cookie, because untagged repeat visits are discarded upstream (TICKETS.md,\n-- \"Pixel Tracker: Three Upstream Fixes for AI Traffic\").\n--\n-- THESE ARE FIRST-TOUCH MEASURES. Read that literally before quoting them.\n--\n-- Untagged channels (Direct, SEO, AIO) are recorded only on a browser's FIRST\n-- ever visit; every later untagged visit is discarded by the pixel. Tagged\n-- channels carry a trc code and are recorded on every visit. Measured position\n-- in journey, 2025 onward:\n--\n--     Facebook Ads  tagged     75.7% first   24.3% later\n--     AdWords       tagged     77.8% first   22.2% later\n--     Bing          tagged     85.4% first   14.6% later\n--     SEO           untagged   99.3% first    0.7% later\n--     Direct        untagged   99.8% first    0.2% later\n--     AIO           untagged   99.8% first    0.2% later\n--\n-- Direct at 99.8% first-touch is the proof this is mechanical, not behavioural:\n-- typing a URL from memory is definitionally a return visit.\n--\n-- CONSEQUENCE FOR AIO. An assistant can only be credited here when it was the\n-- very first thing a browser ever did. A mid-funnel AI consultation, which is\n-- the position assistants actually occupy in a buying process, is discarded.\n-- So `trials` for AIO means \"trials whose browser was FIRST introduced to\n-- Method by an assistant\", not \"AI-influenced trials\". The second number is\n-- larger and currently unmeasurable.\n--\n-- The conversion RATE is still a clean read, because the cohort is coherent:\n-- first-touch-AI browsers, followed through to paying. It is the VOLUME that\n-- is structurally understated, and understated more for untagged channels\n-- than tagged ones, so cross-channel volume comparisons are biased.\n--\n-- Fixing this is two lines in CookieTracker.cs:165. See TICKETS.md,\n-- \"Pixel Tracker: Three Upstream Fixes for AI Traffic\".\n--\n-- WINDOW STARTS 2025-01-01, AND NOT EARLIER. CampaignAdjustedRecordID only\n-- began syncing to BigQuery on 2026-09-01 and carries no history, so the\n-- channel is unresolvable for most pre-2025 clicks. Coverage of the trial\n-- cohort, measured:\n--\n--     2023   30.8%      2025   93.8%\n--     2024   34.2%      2026   90.7%\n--\n-- Extending the window backwards would silently mix a 34%-covered period with\n-- a 94%-covered one and make every channel look like it grew in January 2025.\n-- is_aio is unaffected by this and IS available back to 2013, because it is\n-- derived from the referrer and URL rather than the campaign join. Use\n-- int_cookie_clicks directly for long-run AIO click trends.\n--\n-- Current incomplete month excluded.\n\nWITH trial_cohort AS (\n\n    -- int_trials is the canonical trial definition (excludes conversion\n    -- exceptions, Method Integration partner rows, and the 0001-01-01\n    -- sentinel). Grain is one row per CompanyAccount, verified: 112,556 rows,\n    -- 112,556 distinct CompanyAccount.\n    --\n    -- Stay at ACCOUNT grain deliberately. Collapsing to EntityRecordID with\n    -- MIN(SignupDate) silently drops any customer whose earliest account\n    -- predates the reporting window, which cost 55% of the cohort on the first\n    -- build of this view.\n    SELECT\n        CompanyAccount,\n        SignupDate AS signup_date\n    FROM {{ ref('int_trials') }}\n\n), account_cookie AS (\n\n    -- One cookie per account. revenue.Account carries ~1.22 rows per\n    -- EntityRecordID, so dedupe before joining or weights inflate.\n    SELECT\n        CompanyAccount,\n        ANY_VALUE(CookieRecordID)         AS cookie_id,\n        MIN(FirstSaaSInvoiceTxnDate)      AS first_paid_date\n    FROM {{ source('revenue', 'Account') }}\n    WHERE CookieRecordID IS NOT NULL\n    GROUP BY CompanyAccount\n\n), cookie_channels AS (\n\n    -- The distinct channels that touched each cookie.\n    --\n    -- Keyed on CampaignType, NOT Campaign.Name, so the vocabulary matches the\n    -- rest of the project: PPC, SEO, OPN, Direct, Email, Content, Partners,\n    -- Social, Backlinks, Remarketing. Campaign.Name would give AdWords, Bing\n    -- and \"Intuit App Card Traffic\", which is campaign grain and invents a\n    -- second channel vocabulary nobody else uses.\n    --\n    -- CampaignType is not synced to BigQuery, so the id -> name map is inlined\n    -- from the Alocet CampaignType table (read 2026-09-14, 31 rows). If a new\n    -- type is ever added there it falls to 'Other' rather than breaking, which\n    -- is the opposite of the C# switch's behaviour.\n    --\n    -- The one Att_* channel with no equivalent here is 'None', which means no\n    -- attribution at all. There is no click that represents that.\n    SELECT DISTINCT\n        cookie_id,\n        CASE\n            WHEN is_aio THEN 'AIO'\n            ELSE CASE campaign_type_id\n                WHEN 2  THEN 'Email'      WHEN 4  THEN 'Banner_Ads'\n                WHEN 5  THEN 'Seminar'    WHEN 7  THEN 'Partners'\n                WHEN 8  THEN 'Referral_Program'\n                WHEN 13 THEN 'Online_Chat' WHEN 15 THEN 'PPC'\n                WHEN 16 THEN 'OPN'        WHEN 17 THEN 'Social'\n                WHEN 18 THEN 'Remarketing' WHEN 19 THEN 'SEO'\n                WHEN 20 THEN 'Content'    WHEN 23 THEN 'Direct'\n                WHEN 26 THEN 'Direct'     WHEN 27 THEN 'Referral'\n                WHEN 29 THEN 'Direct'     WHEN 30 THEN 'Backlinks'\n                WHEN 31 THEN 'Webinar'\n                ELSE 'Other'\n            END\n        END AS channel\n    FROM (\n        SELECT cc.cookie_id, cc.is_aio, cam.CampaignTypeRecordID AS campaign_type_id\n        FROM {{ ref('int_cookie_clicks') }} cc\n        LEFT JOIN {{ source('marketing', 'Campaign') }} cam\n          ON cam.RecordID = COALESCE(NULLIF(cc.campaign_adjusted_id, 0),\n                                     NULLIF(cc.campaign_raw_id, 0))\n        WHERE cc.channel IS NOT NULL\n    )\n\n), weighted AS (\n\n    SELECT\n        t.CompanyAccount,\n        t.signup_date,\n        ac.first_paid_date,\n        cc.channel,\n        1.0 / COUNT(*) OVER (PARTITION BY t.CompanyAccount) AS attribution_weight\n    FROM trial_cohort t\n    JOIN account_cookie ac USING (CompanyAccount)\n    JOIN cookie_channels cc ON cc.cookie_id = ac.cookie_id\n\n), synced AS (\n\n    -- Reached a sync at any point. int_syncs is one row per sync EVENT, so\n    -- dedupe to the account before joining or accounts that synced repeatedly\n    -- would count more than once.\n    SELECT DISTINCT CompanyAccount\n    FROM {{ ref('int_syncs') }}\n\n)\n\nSELECT\n    w.channel,\n    DATE_TRUNC(w.signup_date, MONTH)                                 AS signup_month,\n\n    SUM(w.attribution_weight)                                        AS trials,\n    SUM(IF(s.CompanyAccount IS NOT NULL, w.attribution_weight, 0))   AS synced,\n    SUM(IF(w.first_paid_date > DATE('0001-01-01'),\n           w.attribution_weight, 0))                                 AS paying,\n\n    -- Unweighted account count, for sanity-checking the fractional measures.\n    -- Double-counts an account touched by several channels, so these sum to\n    -- more than the true trial total. Never present as a channel split.\n    COUNT(DISTINCT w.CompanyAccount)                                 AS trial_accounts_touched\n\nFROM weighted w\nLEFT JOIN synced s USING (CompanyAccount)\nWHERE w.signup_date >= DATE('2025-01-01')\n  AND w.signup_date <  DATE_TRUNC(CURRENT_DATE(), MONTH)\nGROUP BY channel, signup_month\nORDER BY channel, signup_month\n","tests":[]},{"name":"v_demo_transcripts","alias":"v_demo_transcripts","relation_name":"`project-for-method-dw`.`revenue`.`v_demo_transcripts`","description":"READ-THE-CALLS VIEW. Everything needed to browse demo-family transcripts\nwith no joins required — transcript text, the full V7 classification, and\nthe lifecycle classification are all present.\n\nUse this to READ calls. Use int_demos to COUNT demos: this view contains\nonly calls that were recorded and transcribed (roughly half of all demo\nmeetings), so counting from here undercounts.\n\nWhy not just filter customer_signals.conversations on call_type = 'demo'?\nBecause call_type is a 1:1 copy of the Method-side activity subject line —\nit says \"demo\" whenever the calendar booking said demo, regardless of what\nthe call turned out to be. About a fifth of demo-typed calls are attended\nsessions with customers who had already subscribed: support, training,\nbilling, customization scoping. is_demo and classification_reason\ndistinguish them.\n\nOne row per conversation. Contains customer conversation content — treat\nas sensitive and never copy it out of BigQuery.\n","original_file_path":"models/intermediate/v_demo_transcripts.sql","refs":["int_demo_calls"],"sources":["conversations","v_entity_primary_label"],"columns":[{"name":"conversation_id","description":"Primary key, one row per recorded call."},{"name":"company_account","description":"Account name, for identifying the call."},{"name":"v7_l1","description":"V7 top-level classification — Manufacturing & Distribution, Field Services & Trades, Professional & Business Services, Retail & Consumer, or UNCLASSIFIABLE. NULL where the customer has no V7 label. Source: v7_classification.v_entity_primary_label, joined on customer_record_id = EntityRecordID.\n"},{"name":"v7_l2","description":"V7 second-level classification."},{"name":"v7_l3","description":"V7 third-level classification."},{"name":"v7_operating_model","description":"V7 operating-model label for the customer."},{"name":"v7_confidence","description":"Confidence of the V7 label itself (0-1). Filter on this when the classification needs to be reliable — it is independent of, and should not be confused with, the confidence_note on the call classification.\n"},{"name":"v7_is_multi_business","description":"V7 flag — the customer runs more than one distinct business."},{"name":"v7_is_multi_client","description":"V7 flag — the customer serves multiple end clients (agency/franchise shapes)."},{"name":"call_type","description":"Method's own label — demo / free_hour / customization / support. NOT independent evidence of what the call was about; it is derived from the activity subject line.\n"},{"name":"is_demo","description":"TRUE when this call corresponds to a genuine pre-subscription demo meeting. The column to filter on when you want prospect demos only.\n"},{"name":"zone","description":"Lifecycle position — pre_signup, pre_subscription, post_subscription, no_funnel_record, excluded."},{"name":"classification_reason","description":"Plain-English sentence explaining why this call is or is not a demo, with the relevant dates inlined. Wording authored by Claude (AI assistant), not reviewed by anyone at Method; the fence logic it describes is verifiable from the other columns.\n"},{"name":"confidence_note","description":"PROVISIONAL AND UNREVIEWED. AI-judged from a 40-call read (n=10 per zone, ±15pp), no per-call record retained, and measured before the current zone definitions. A hint, not a fact.\n"},{"name":"days_since_subscribed","description":"Days from the customer's subscription date to this call. Negative before subscribing. A large positive value is the clearest single signal that a demo-typed call is not new business.\n"},{"name":"transcript_text","description":"Full transcript. Sensitive customer content — never copy out of BigQuery."},{"name":"participants","description":"Call participants, from the recording."}],"compiled_sql":"{{ config(\n    materialized='view',\n    labels={'status': 'directional', 'layer': 'consumption',\n            'grain': 'call', 'owner': 'nic'}\n) }}\n\n-- READ-THE-CALLS VIEW. Everything needed to browse demo-family transcripts,\n-- with no joins required by the consumer.\n--\n-- Use this when you want to READ calls. Use int_demos when you want to COUNT\n-- demos — this view only contains calls that were recorded and transcribed\n-- (roughly half of all demo meetings), so counting from here undercounts.\n--\n-- Why not just filter customer_signals.conversations on call_type = 'demo'?\n-- Because call_type is a 1:1 copy of the Method-side activity subject line. It\n-- says \"demo\" whenever the calendar booking said demo, regardless of what the\n-- call turned out to be. About a fifth of demo-typed calls are attended\n-- sessions with customers who had already subscribed — support, training,\n-- billing, customization scoping. `is_demo` and `classification_reason` here\n-- tell you which is which.\n\nSELECT\n  -- identity\n  c.conversation_id,\n  c.entity_record_id,\n  c.account_id,\n  c.company_account,\n\n  -- V7 classification, straight from v7_classification.v_entity_primary_label\n  -- (joined on customer_record_id = EntityRecordID). Exposed in full rather\n  -- than flattened to one \"industry\" column, so L2/L3, operating model and the\n  -- label's own confidence are all available without a second join.\n  v.l1                                              AS v7_l1,\n  v.l2                                              AS v7_l2,\n  v.l3                                              AS v7_l3,\n  v.operating_model                                 AS v7_operating_model,\n  v.confidence                                      AS v7_confidence,\n  v.is_multi_business                               AS v7_is_multi_business,\n  v.is_multi_client                                 AS v7_is_multi_client,\n\n  -- when\n  c.occurred_at,\n  c.call_date,\n\n  -- what Method's own labelling says (NOT independent evidence)\n  c.call_type,\n  c.topic,\n\n  -- what the lifecycle says\n  c.is_demo,\n  c.zone,\n  c.classification_reason,\n  c.confidence_note,\n  c.returning_customer,\n  c.attended,\n  c.missed,\n  c.days_since_signup,\n  c.days_since_subscribed,\n  c.match_quality,\n\n  -- the transcript itself, so no join is needed\n  c.transcript_chars,\n  cv.transcript_text,\n  cv.participants\n\nFROM {{ ref('int_demo_calls') }} c\nJOIN {{ source('customer_signals', 'conversations') }} cv\n  ON cv.conversation_id = c.conversation_id\nLEFT JOIN {{ source('v7_classification', 'v_entity_primary_label') }} v\n  ON v.customer_record_id = c.entity_record_id\nWHERE c.has_transcript\n","tests":["not_null(conversation_id)","unique(conversation_id)"]},{"name":"v_ga4_account_journey","alias":"v_ga4_account_journey","relation_name":"`project-for-method-dw`.`revenue`.`v_ga4_account_journey`","description":"CONSUMER VIEW. Page-by-page website journeys for visitors we can tie to a\nMethod account. Point Looker and ad-hoc analysis here, not at the raw\nGA4 export.\n\nHOW THE STITCH WORKS. user_pseudo_id is the _ga cookie and is stable per\nbrowser. Most events carry no identity. But if any event on that browser\nexposes the account (the signup completion URL, or a sign-in returnUrl),\nthe browser binds to the account and every other event on it is\nattributed too, including pages read weeks earlier while anonymous. That\nis why this works retroactively over data already collected rather than\nonly from the day identification was switched on.\n\nIDENTITY. Uses account_slug parsed from the URL, deliberately NOT GA4's\nuser_id. The slug resolves for every event ever exported; user_id only\nexists from 2026-08-31 and briefly carried malformed values that day.\n\nGRAIN. One row per (browser, pageview). Only page_view events. A browser\nappears only if it was bound to an account at some point.\n\nREAD THIS BEFORE QUOTING ANYTHING:\n\n* No history before 2026-08-28. The GA4 export started then and does not\n  backfill. Journeys for customers acquired earlier do not exist.\n* As of 2026-08-31 the sample is four days old and 90% sign-in traffic.\n  Only 3 blog pageviews are present. It is not yet usable for content\n  attribution; revisit once several months have accumulated.\n* One browser is not one person. Read on a phone, sign in on a laptop,\n  and they are two unlinked rows. Cross-device is unsolved.\n* GA4 is switched off inside the product (the GA4 config tag excludes\n  Method App Traffic), so in-app behaviour is largely absent by design.\n* QA and dev hostnames are already excluded upstream in int_ga4_events.\n","original_file_path":"models/marketing/v_ga4_account_journey.sql","refs":["int_ga4_events"],"sources":["Account"],"columns":[{"name":"account_slug","description":"Method account slug parsed from the URL. The join key. Lowercase."},{"name":"entity_record_id","description":"Customer-level key from revenue.Account. NULL when the slug does not match a known account."},{"name":"company_account","description":"CompanyAccount from revenue.Account. NULL when unmatched."},{"name":"is_known_account","description":"TRUE when account_slug matched revenue.Account. About 24% do not, mostly QA accounts (m11*) and nightly-mirror lag on same-day signups. Filter on this for anything customer-facing.\n"},{"name":"signup_date","description":"Account signup date from revenue.Account."},{"name":"first_paid_date","description":"First SaaS invoice date. Uses the 0001-01-01 sentinel for never-paid."},{"name":"is_paying","description":"TRUE when first_paid_date is past the never-paid sentinel."},{"name":"user_pseudo_id","description":"GA4 client id, the _ga cookie. Per-browser. The stitch key."},{"name":"account_count","description":"Distinct accounts seen on this browser. Greater than 1 means a shared machine, agency or partner; the view keeps only the most recent binding. Treat those journeys with suspicion.\n"},{"name":"step","description":"1-based position of this pageview within the browser's journey, ordered by time."},{"name":"total_steps","description":"Total pageviews in this browser's journey. Often 1, which is a thin journey rather than a broken one."},{"name":"is_first_page","description":"TRUE when step = 1. The earliest page we ever saw this browser on, which is not necessarily their true first visit."},{"name":"is_last_page","description":"TRUE when step = total_steps."},{"name":"minutes_into_journey","description":"Minutes between this pageview and the browser's first recorded pageview."},{"name":"was_identified_at_the_time","description":"TRUE when this specific event carried the account in its URL. FALSE means it was anonymous when it happened and was attributed later by the stitch. The FALSE rows are the ones this model exists to recover.\n"},{"name":"event_date","description":"Event date in the property timezone."},{"name":"event_at","description":"Event timestamp, UTC."},{"name":"is_intraday","description":"TRUE when sourced from a partial intraday table. Exclude for anything that must reconcile."},{"name":"page_area","description":"Coarse bucket: blog, pricing_guides, resources, marketing_site, campaign_landing, signup, signin, forums, app, unknown.\n"},{"name":"page_host","description":"Hostname of the page viewed."},{"name":"page_path","description":"Path of the page viewed, query string and trailing slash stripped."},{"name":"page_referrer","description":"Referring URL where GA4 captured one."},{"name":"traffic_source","description":"GA4 session traffic source."},{"name":"traffic_medium","description":"GA4 session traffic medium."},{"name":"traffic_campaign","description":"GA4 session campaign name."},{"name":"gclid","description":"Google Ads click id when present. Bridges to the google_ads dataset. Sparse."},{"name":"device_category","description":"desktop / mobile / tablet."},{"name":"country","description":"Country from GA4 geo enrichment."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- CONSUMER VIEW. The page-by-page journey for visitors we can tie to a\n-- Method account. This is what marketing points Looker at for \"which content\n-- earns customers\". Built on int_ga4_events; do not read the raw export.\n--\n-- HOW THE STITCH WORKS, and why it recovers anonymous history:\n--\n--   user_pseudo_id is the _ga cookie and is stable per browser. Most events\n--   carry no identity. But if ANY event on that browser exposes the account\n--   (the signup completion URL, or a sign-in returnUrl), we can bind the\n--   browser to the account and then attribute every OTHER event on the same\n--   browser -- including pages read weeks earlier, while anonymous.\n--\n--   That is why this works retroactively over data already collected, rather\n--   than only from the day identification was switched on.\n--\n-- IDENTITY SOURCE: account_slug from int_ga4_events, parsed out of the URL.\n-- Deliberately NOT GA4's user_id. The slug is derivable for every event ever\n-- exported; user_id only exists from 2026-08-31 onward and briefly carried\n-- malformed values. user_id is still populated for GA4's own UI reporting.\n--\n-- GRAIN: one row per (browser, event). A browser appears only if it was bound\n-- to an account at some point.\n--\n-- CAVEATS a consumer must know:\n--\n--  * NO HISTORY BEFORE 2026-08-28. The GA4 export started then and does not\n--    backfill. Journeys for anyone acquired earlier do not exist anywhere.\n--  * ONE BROWSER, NOT ONE PERSON. Read on a phone, sign in on a laptop, and\n--    they are two rows with no link. Cross-device is not solved.\n--  * A browser bound to more than one account (shared machine, agency,\n--    partner) keeps only its most recent binding; account_count flags them.\n--  * GA4 is switched OFF inside the product -- the GA4 config tag excludes\n--    Method App Traffic -- so in-app behaviour is largely absent by design.\n--  * ~24% of slugs do not match revenue.Account. Mostly QA accounts and\n--    nightly-mirror lag on same-day signups. is_known_account flags the join.\n\nwith events as (\n\n    select *\n    from {{ ref('int_ga4_events') }}\n    where user_pseudo_id is not null\n\n), binding as (\n\n    -- One account per browser: the most recently seen slug wins.\n    select\n        user_pseudo_id,\n        array_agg(account_slug order by event_at desc limit 1)[offset(0)] as account_slug,\n        count(distinct account_slug)                                     as account_count,\n        min(event_at)                                                    as first_identified_at\n    from events\n    where account_slug is not null\n    group by 1\n\n), joined as (\n\n    select\n        b.account_slug,\n        b.account_count,\n        e.user_pseudo_id,\n        e.event_date,\n        e.event_at,\n        e.is_intraday,\n        e.event_name,\n        e.page_area,\n        e.page_host,\n        e.page_path,\n        e.page_referrer,\n        e.traffic_source,\n        e.traffic_medium,\n        e.traffic_campaign,\n        e.gclid,\n        e.device_category,\n        e.country,\n        -- Was this specific event the one that revealed the identity, or was it\n        -- anonymous at the time and only attributed later?\n        e.account_slug is not null                                       as was_identified_at_the_time\n    from events e\n    join binding b using (user_pseudo_id)\n\n), sequenced as (\n\n    select\n        *,\n        row_number() over (partition by user_pseudo_id order by event_at)        as step,\n        count(*)     over (partition by user_pseudo_id)                          as total_steps,\n        min(event_at) over (partition by user_pseudo_id)                         as journey_start_at\n    from joined\n    where event_name = 'page_view'\n\n)\n\nselect\n    s.account_slug,\n    a.EntityRecordID                                                     as entity_record_id,\n    a.CompanyAccount                                                     as company_account,\n    a.CompanyAccount is not null                                         as is_known_account,\n    date(a.SignUpDate)                                                   as signup_date,\n    a.FirstSaaSInvoiceTxnDate                                            as first_paid_date,\n    a.FirstSaaSInvoiceTxnDate > date '1900-01-01'                        as is_paying,\n\n    s.user_pseudo_id,\n    s.account_count,\n    s.step,\n    s.total_steps,\n    s.step = 1                                                           as is_first_page,\n    s.step = s.total_steps                                               as is_last_page,\n    timestamp_diff(s.event_at, s.journey_start_at, minute)               as minutes_into_journey,\n    s.was_identified_at_the_time,\n\n    s.event_date,\n    s.event_at,\n    s.is_intraday,\n    s.page_area,\n    s.page_host,\n    s.page_path,\n    s.page_referrer,\n    s.traffic_source,\n    s.traffic_medium,\n    s.traffic_campaign,\n    s.gclid,\n    s.device_category,\n    s.country\n\nfrom sequenced s\nleft join (\n    -- revenue.Account has ~1.22 rows per EntityRecordID; dedup before joining.\n    select * except(rn) from (\n        select\n            EntityRecordID, CompanyAccount, SignUpDate, FirstSaaSInvoiceTxnDate,\n            row_number() over (partition by lower(CompanyAccount) order by SignUpDate) as rn\n        from {{ source('revenue', 'Account') }}\n    ) where rn = 1\n) a\n  on lower(a.CompanyAccount) = s.account_slug\n","tests":[]},{"name":"v_metric__annual_cancellations_mrr","alias":"v_metric__annual_cancellations_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__annual_cancellations_mrr`","description":"Total MRR lost from customer cancellations measured at annual cohort\ngrain, in dollars summed across all customers. Pre-FX — currencies\n(USD, CAD, UK) at face value, not USD-converted. Excludes internal\nMethod accounts. Uses CEO-confirmed Prepay Expiry methodology.\nFoundation for annual GRR (#388).\n","original_file_path":"models/metrics/v_metric__annual_cancellations_mrr.sql","refs":["int_customer_annual_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Annual Cancellations ($)\" (#385)\n\nSELECT\n  Month AS period,\n  ROUND(SUM(Cancellations), 2) AS value\nFROM {{ ref('int_customer_annual_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__annual_downgrades_mrr","alias":"v_metric__annual_downgrades_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__annual_downgrades_mrr`","description":"Total MRR lost from existing-customer downgrades at annual cohort\ngrain (customers paying less than the prior year but not canceling),\nin dollars summed across all customers. Pre-FX — currencies (USD,\nCAD, UK) at face value, not USD-converted. Excludes internal Method\naccounts. Inherits the v_customer_annual_mrr Prepay Expiry methodology.\n","original_file_path":"models/metrics/v_metric__annual_downgrades_mrr.sql","refs":["int_customer_annual_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Annual Downgrades ($)\" (#386)\n\nSELECT\n  Month AS period,\n  ROUND(SUM(Downgrades), 2) AS value\nFROM {{ ref('int_customer_annual_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__annual_expansions_mrr","alias":"v_metric__annual_expansions_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__annual_expansions_mrr`","description":"Total MRR gained from existing-customer expansions at annual cohort\ngrain (customers paying more than the prior year), in dollars summed\nacross all customers. Pre-FX — currencies (USD, CAD, UK) at face\nvalue, not USD-converted. Existing customers only — net-new customer\nrevenue is tracked separately. Foundation for annual NRR (#389).\n","original_file_path":"models/metrics/v_metric__annual_expansions_mrr.sql","refs":["int_customer_annual_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Annual Expansions ($)\" (#387)\n\nSELECT\n  Month AS period,\n  ROUND(SUM(Expansions), 2) AS value\nFROM {{ ref('int_customer_annual_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__annual_grr","alias":"v_metric__annual_grr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__annual_grr`","description":"Annual Gross Revenue Retention — fraction of last year's MRR retained\nthis year-end cohort, excluding expansion. Formula: (Annual StartMRR\n- Annual Cancellations - Annual Downgrades) / Annual StartMRR.\nPre-FX. Typical values 76-78%. Lower than monthly GRR because more\nchurn accumulates over 12 months. Uses CEO-confirmed methodology;\nreconcile against board deck before external use.\n","original_file_path":"models/metrics/v_metric__annual_grr.sql","refs":["v_metric__annual_start_mrr","v_metric__annual_cancellations_mrr","v_metric__annual_downgrades_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Annual GRR %\" (#388)\n-- Type: derived (cross-model)\n-- Formula: (Annual StartMRR - Annual Cancellations - Annual Downgrades) / Annual StartMRR\n\nSELECT\n  s.period,\n  SAFE_DIVIDE(s.value - c.value - d.value, s.value) AS value\nFROM {{ ref('v_metric__annual_start_mrr') }} s\nJOIN {{ ref('v_metric__annual_cancellations_mrr') }} c USING (period)\nJOIN {{ ref('v_metric__annual_downgrades_mrr') }} d USING (period)\nORDER BY s.period\n","tests":[]},{"name":"v_metric__annual_nrr","alias":"v_metric__annual_nrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__annual_nrr`","description":"Annual Net Revenue Retention — fraction of last year's MRR retained\nthis year-end cohort INCLUDING expansion from existing customers.\nFormula: (Annual StartMRR - Annual Cancellations - Annual Downgrades\n+ Annual Expansions) / Annual StartMRR. Pre-FX. Typical values\n88-90%. Lower than monthly NRR because more churn accumulates over\n12 months. Uses CEO-confirmed methodology; reconcile against board\ndeck before external use.\n","original_file_path":"models/metrics/v_metric__annual_nrr.sql","refs":["v_metric__annual_start_mrr","v_metric__annual_cancellations_mrr","v_metric__annual_downgrades_mrr","v_metric__annual_expansions_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Annual NRR %\" (#389)\n-- Type: derived (cross-model)\n-- Formula: (Annual StartMRR - Annual Cancellations - Annual Downgrades + Annual Expansions) / Annual StartMRR\n\nSELECT\n  s.period,\n  SAFE_DIVIDE(s.value - c.value - d.value + e.value, s.value) AS value\nFROM {{ ref('v_metric__annual_start_mrr') }} s\nJOIN {{ ref('v_metric__annual_cancellations_mrr') }} c USING (period)\nJOIN {{ ref('v_metric__annual_downgrades_mrr') }} d USING (period)\nJOIN {{ ref('v_metric__annual_expansions_mrr') }} e USING (period)\nORDER BY s.period\n","tests":[]},{"name":"v_metric__annual_start_mrr","alias":"v_metric__annual_start_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__annual_start_mrr`","description":"Total MRR at the start of each annual cohort, summed across all\ncustomers. Pre-FX — currencies (USD, CAD, UK) at face value, not\nUSD-converted. Excludes internal Method accounts. Uses CEO-confirmed\nmethodology that excludes one-time Prepay Expiry write-offs.\nFoundation for annual GRR / NRR (#388 / #389). Annual cohort\nreported monthly (trailing comparison).\n","original_file_path":"models/metrics/v_metric__annual_start_mrr.sql","refs":["int_customer_annual_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Annual Start MRR\" (#384)\n-- Type: simple SUM(StartMRR) from v_customer_annual_mrr, rounded to 2 decimals\n\nSELECT\n  Month AS period,\n  ROUND(SUM(StartMRR), 2) AS value\nFROM {{ ref('int_customer_annual_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__churn","alias":"v_metric__churn","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__churn`","description":"Monthly count of distinct Method customers that canceled, grouped by\ncancellation month. Customer-grain — uses COUNT(DISTINCT CompanyAccount)\nso a customer with multiple canceling accounts in the same month\ncounts ONCE. Excludes test accounts and internal Method Integration\npartner rows. Note: this is account-count churn, not dollar churn —\nsee Monthly Cancellations ($) (#379) for MRR lost.\n","original_file_path":"models/metrics/v_metric__churn.sql","refs":[],"sources":["int_cancellations"],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Churn\" (#59)\n-- Type: simple COUNT(DISTINCT CompanyAccount) from v_cancellations, by CancellationDate\n\nSELECT\n  DATE_TRUNC(CancellationDate, MONTH) AS period,\n  COUNT(DISTINCT CompanyAccount) AS value\nFROM {{ source('revenue', 'int_cancellations') }}\nWHERE CancellationDate >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__churn_forecast_mtd","alias":"v_metric__churn_forecast_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__churn_forecast_mtd`","description":"Full-month churn forecast prorated to the elapsed window. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__churn_forecast_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Full-month churn forecast prorated to the elapsed window.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('churn_forecast_mtd') }}\n","tests":[]},{"name":"v_metric__churn_mtd","alias":"v_metric__churn_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__churn_mtd`","description":"Distinct CompanyAccounts cancelled so far this month, excluding today. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__churn_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Distinct CompanyAccounts cancelled so far this month, excluding today.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('churn_mtd') }}\n","tests":[]},{"name":"v_metric__churn_rate_mtd","alias":"v_metric__churn_rate_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__churn_rate_mtd`","description":"Month-to-date churn rate as a percentage. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__churn_rate_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Month-to-date churn rate as a percentage.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('churn_rate_mtd') }}\n","tests":[]},{"name":"v_metric__churn_rate_trajectory","alias":"v_metric__churn_rate_trajectory","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__churn_rate_trajectory`","description":"Calendar-day linear projection of the full-month churn rate. A projection of int_method_monday into (period, value); the definition lives upstream in that model. Returns NULL for the first 7 days of the month — see int_method_monday.\n","original_file_path":"models/metrics/v_metric__churn_rate_trajectory.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Calendar-day linear projection of the full-month churn rate, as a percentage.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('churn_rate_trajectory') }}\n","tests":[]},{"name":"v_metric__churn_trajectory","alias":"v_metric__churn_trajectory","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__churn_trajectory`","description":"Calendar-day linear projection of full-month churn. A projection of int_method_monday into (period, value); the definition lives upstream in that model. Returns NULL for the first 7 days of the month — see int_method_monday.\n","original_file_path":"models/metrics/v_metric__churn_trajectory.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Calendar-day linear projection of full-month churn.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('churn_trajectory') }}\n","tests":[]},{"name":"v_metric__conversions","alias":"v_metric__conversions","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__conversions`","description":"Monthly count of Method accounts that converted from trial to paying\n(i.e., received their first SaaS invoice). Account-grain — a customer\nwith 2 accounts that both converted contributes 2 conversions, by\ndesign. Excludes test accounts and internal Method Integration\npartner rows. Foundation for Conversion Rate metrics (#301, #302).\n","original_file_path":"models/metrics/v_metric__conversions.sql","refs":[],"sources":["int_conversions"],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Conversions\" (#56)\n-- Type: simple COUNT(*) from v_conversions, by FirstSaaSInvoiceTxnDate\n\nSELECT\n  DATE_TRUNC(FirstSaaSInvoiceTxnDate, MONTH) AS period,\n  COUNT(*) AS value\nFROM {{ source('revenue', 'int_conversions') }}\nWHERE FirstSaaSInvoiceTxnDate >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__conversions_forecast_mtd","alias":"v_metric__conversions_forecast_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__conversions_forecast_mtd`","description":"Full-month conversion forecast prorated to the elapsed window. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__conversions_forecast_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Full-month conversion forecast prorated to the elapsed window.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('conversions_forecast_mtd') }}\n","tests":[]},{"name":"v_metric__conversions_mtd","alias":"v_metric__conversions_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__conversions_mtd`","description":"Conversions so far this month, excluding today. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__conversions_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Conversions so far this month, excluding today.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('conversions_mtd') }}\n","tests":[]},{"name":"v_metric__conversions_trajectory","alias":"v_metric__conversions_trajectory","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__conversions_trajectory`","description":"Calendar-day linear projection of full-month conversions. A projection of int_method_monday into (period, value); the definition lives upstream in that model. Returns NULL for the first 7 days of the month — see int_method_monday.\n","original_file_path":"models/metrics/v_metric__conversions_trajectory.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Calendar-day linear projection of full-month conversions.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('conversions_trajectory') }}\n","tests":[]},{"name":"v_metric__customers","alias":"v_metric__customers","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__customers`","description":"Monthly count of unique active Method customers — companies with\nrevenue activity in the month. Customer-grain — a company with\nmultiple Method accounts counts ONCE per month (unlike Trials,\nwhich counts each account separately). Current month is incomplete;\npartial values until month-end.\n","original_file_path":"models/metrics/v_metric__customers.sql","refs":["int_customers"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Customers\" (#373)\n-- Type: simple count_distinct of EntityRecordID from v_customers\n-- Filter: IsActive = TRUE (matches Supabase semantic_filters)\n-- Grain: customer (NOT account) — a customer with multiple accounts counts ONCE\n-- Materialization: rolling 24 months ending at the current day\n\nSELECT\n  Month AS period,\n  COUNT(DISTINCT EntityRecordID) AS value\nFROM {{ ref('int_customers') }}\nWHERE IsActive = TRUE\n  AND Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__monthly_cancellations_mrr","alias":"v_metric__monthly_cancellations_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__monthly_cancellations_mrr`","description":"Total MRR lost from customer cancellations each month, in dollars\nsummed across all customers. Pre-FX — currencies (USD, CAD, UK) at\nface value, not USD-converted. Excludes internal Method accounts.\nUses CEO-confirmed methodology that excludes one-time Prepay Expiry\nwrite-offs from both StartMRR and Cancellations. Foundation for\nmonthly GRR.\n","original_file_path":"models/metrics/v_metric__monthly_cancellations_mrr.sql","refs":["int_customer_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Monthly Cancellations ($)\" (#379)\n-- Type: simple SUM(Cancellations) from v_customer_mrr, rounded to 2 decimals\n-- Methodology: inherits CEO-confirmed symmetric Prepay Expiry Income\n--   exclusion from v_customer_mrr (see knowledge/verified-queries/v_customer_mrr.sql)\n-- Materialization: rolling 24 months ending at the current day\n\nSELECT\n  Month AS period,\n  ROUND(SUM(Cancellations), 2) AS value\nFROM {{ ref('int_customer_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__monthly_downgrades_mrr","alias":"v_metric__monthly_downgrades_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__monthly_downgrades_mrr`","description":"Total MRR lost from customer downgrades each month (existing customers\npaying less than the previous month, but not canceling), in dollars\nsummed across all customers. Pre-FX — currencies (USD, CAD, UK) at\nface value, not USD-converted. Excludes internal Method accounts.\nInherits the v_customer_mrr Prepay Expiry methodology.\n","original_file_path":"models/metrics/v_metric__monthly_downgrades_mrr.sql","refs":["int_customer_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Monthly Downgrades ($)\" (#380)\n-- Type: simple SUM(Downgrades) from v_customer_mrr, rounded to 2 decimals\n\nSELECT\n  Month AS period,\n  ROUND(SUM(Downgrades), 2) AS value\nFROM {{ ref('int_customer_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__monthly_expansions_mrr","alias":"v_metric__monthly_expansions_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__monthly_expansions_mrr`","description":"Total MRR gained from customer expansions each month (existing\ncustomers paying more than the previous month), in dollars summed\nacross all customers. Pre-FX — currencies (USD, CAD, UK) at face\nvalue, not USD-converted. Excludes internal Method accounts.\nInherits the v_customer_mrr Prepay Expiry methodology. Foundation\nfor monthly NRR.\n","original_file_path":"models/metrics/v_metric__monthly_expansions_mrr.sql","refs":["int_customer_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Monthly Expansions ($)\" (#381)\n-- Type: simple SUM(Expansions) from v_customer_mrr, rounded to 2 decimals\n\nSELECT\n  Month AS period,\n  ROUND(SUM(Expansions), 2) AS value\nFROM {{ ref('int_customer_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__monthly_grr","alias":"v_metric__monthly_grr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__monthly_grr`","description":"Monthly Gross Revenue Retention — fraction of last month's MRR\nretained this month, excluding expansion. Formula: (StartMRR -\nCancellations - Downgrades) / StartMRR. Pre-FX. Typical values\n95-97%. Uses CEO-confirmed symmetric Prepay Expiry exclusion;\ndiverges from board-deck monthly GRR by ~4-6bp because the deck\nuses asymmetric methodology. For any number heading to the board,\nreconcile against the deck first.\n","original_file_path":"models/metrics/v_metric__monthly_grr.sql","refs":["v_metric__monthly_start_mrr","v_metric__monthly_cancellations_mrr","v_metric__monthly_downgrades_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Monthly GRR %\" (#382)\n-- Type: derived (cross-model)\n-- Formula: (StartMRR - Cancellations - Downgrades) / StartMRR\n\nSELECT\n  s.period,\n  SAFE_DIVIDE(s.value - c.value - d.value, s.value) AS value\nFROM {{ ref('v_metric__monthly_start_mrr') }} s\nJOIN {{ ref('v_metric__monthly_cancellations_mrr') }} c USING (period)\nJOIN {{ ref('v_metric__monthly_downgrades_mrr') }} d USING (period)\nORDER BY s.period\n","tests":[]},{"name":"v_metric__monthly_nrr","alias":"v_metric__monthly_nrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__monthly_nrr`","description":"Monthly Net Revenue Retention — fraction of last month's MRR retained\nthis month INCLUDING expansion from existing customers. Formula:\n(StartMRR - Cancellations - Downgrades + Expansions) / StartMRR.\nPre-FX. Typical values 97-99% — expansion mostly offsets churn at\nMethod's scale. Uses CEO-confirmed symmetric Prepay Expiry exclusion.\nFor board reporting, reconcile against the board deck first\n(~4-6bp methodology gap).\n","original_file_path":"models/metrics/v_metric__monthly_nrr.sql","refs":["v_metric__monthly_start_mrr","v_metric__monthly_cancellations_mrr","v_metric__monthly_downgrades_mrr","v_metric__monthly_expansions_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Monthly NRR %\" (#383)\n-- Type: derived (cross-model)\n-- Formula: (StartMRR - Cancellations - Downgrades + Expansions) / StartMRR\n\nSELECT\n  s.period,\n  SAFE_DIVIDE(s.value - c.value - d.value + e.value, s.value) AS value\nFROM {{ ref('v_metric__monthly_start_mrr') }} s\nJOIN {{ ref('v_metric__monthly_cancellations_mrr') }} c USING (period)\nJOIN {{ ref('v_metric__monthly_downgrades_mrr') }} d USING (period)\nJOIN {{ ref('v_metric__monthly_expansions_mrr') }} e USING (period)\nORDER BY s.period\n","tests":[]},{"name":"v_metric__monthly_start_mrr","alias":"v_metric__monthly_start_mrr","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__monthly_start_mrr`","description":"Total MRR at the start of each month, in dollars summed across all\ncustomers. Pre-FX — currencies (USD, CAD, UK) at face value, not\nUSD-converted. Excludes internal Method accounts. Uses CEO-confirmed\nmethodology that excludes one-time Prepay Expiry write-offs from\nboth StartMRR and Cancellations. Foundation for monthly GRR / NRR.\n","original_file_path":"models/metrics/v_metric__monthly_start_mrr.sql","refs":["int_customer_mrr"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Monthly Start MRR\" (#378)\n-- Type: simple SUM(StartMRR) from v_customer_mrr, rounded to 2 decimals\n-- Methodology: inherits CEO-confirmed symmetric Prepay Expiry Income\n--   exclusion from v_customer_mrr (see knowledge/verified-queries/v_customer_mrr.sql)\n-- Materialization: rolling 24 months ending at the current day\n\nSELECT\n  Month AS period,\n  ROUND(SUM(StartMRR), 2) AS value\nFROM {{ ref('int_customer_mrr') }}\nWHERE Month >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__sync_rate","alias":"v_metric__sync_rate","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__sync_rate`","description":"Monthly Sync Rate — sync milestone events divided by trial signups\nthat month, at account-grain. Today's typical range is 50-65%. Not\na clean \"% of customers who synced\" — both numerator and denominator\nare account/event counts. For exact \"fraction of unique trial cohort\nthat synced,\" a different metric would be needed.\n","original_file_path":"models/metrics/v_metric__sync_rate.sql","refs":["v_metric__syncs","v_metric__trials"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Sync Rate\" (#300)\n-- Type: ratio (cross-model: numerator from v_syncs, denominator from v_trials)\n-- Formula: SAFE_DIVIDE(syncs.value, trials.value) per period\n-- Materialization: rolling 24 months ending at the current day.\n-- Description and BQ labels come from v_metric__sync_rate.yml — at `dbt run`\n-- time, dbt-bigquery wraps this SELECT with CREATE OR REPLACE VIEW ...\n-- OPTIONS(description, labels) automatically.\n\nSELECT\n  COALESCE(s.period, t.period) AS period,\n  SAFE_DIVIDE(s.value, t.value) AS value\nFROM {{ ref('v_metric__syncs') }} s\nFULL OUTER JOIN {{ ref('v_metric__trials') }} t\n  ON s.period = t.period\nORDER BY 1\n","tests":[]},{"name":"v_metric__sync_rate_mtd","alias":"v_metric__sync_rate_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__sync_rate_mtd`","description":"Month-to-date sync rate as a percentage. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__sync_rate_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Month-to-date sync rate as a percentage: syncs_mtd over trials_mtd.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('sync_rate_mtd', \"SAFE_DIVIDE(syncs_mtd, trials_mtd) * 100\") }}\n","tests":[]},{"name":"v_metric__sync_to_conversion_rate","alias":"v_metric__sync_to_conversion_rate","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__sync_to_conversion_rate`","description":"Monthly Sync-to-Conversion Rate — conversions divided by syncs that\nmonth, at account-grain. Measures how well synced accounts progress\nto paying customers. Both numerator and denominator are account/event\ncounts (see Conversions #56 and Syncs #55). Use for funnel-stage\nanalysis, not as a clean \"% of unique sync cohort that converted.\"\n","original_file_path":"models/metrics/v_metric__sync_to_conversion_rate.sql","refs":["v_metric__conversions","v_metric__syncs"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Sync-to-Conversion Rate\" (#301)\n-- Type: ratio (cross-model)\n-- Formula: SAFE_DIVIDE(conversions, syncs) per period\n\nSELECT\n  COALESCE(c.period, s.period) AS period,\n  SAFE_DIVIDE(c.value, s.value) AS value\nFROM {{ ref('v_metric__conversions') }} c\nFULL OUTER JOIN {{ ref('v_metric__syncs') }} s\n  ON c.period = s.period\nORDER BY 1\n","tests":[]},{"name":"v_metric__syncs","alias":"v_metric__syncs","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__syncs`","description":"Monthly count of sync milestone events from Method's funnel pipeline.\nAccount-grain by intent — counts each time an account hits a sync\nmilestone. ~91% of accounts have one sync event; ~9% have 2+ from\nre-syncs after disconnect/reconnect, which are counted by design.\nFoundation for Sync Rate (#300).\n","original_file_path":"models/metrics/v_metric__syncs.sql","refs":["int_syncs"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Syncs\" (#55)\n-- Type: simple aggregation of v_syncs.SyncDate (COUNT(*) per month)\n-- Materialization: rolling 24 months ending at the current day.\n-- Description and BQ labels come from v_metric__syncs.yml — at `dbt run`\n-- time, dbt-bigquery wraps this SELECT with CREATE OR REPLACE VIEW ...\n-- OPTIONS(description, labels) automatically.\n\nSELECT\n  DATE_TRUNC(SyncDate, MONTH) AS period,\n  COUNT(*) AS value\nFROM {{ ref('int_syncs') }}\nWHERE SyncDate >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__syncs_mtd","alias":"v_metric__syncs_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__syncs_mtd`","description":"Syncs so far this month, excluding today. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__syncs_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Syncs so far this month, excluding today.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('syncs_mtd') }}\n","tests":[]},{"name":"v_metric__syncs_trajectory","alias":"v_metric__syncs_trajectory","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__syncs_trajectory`","description":"Calendar-day linear projection of full-month syncs. A projection of int_method_monday into (period, value); the definition lives upstream in that model. Returns NULL for the first 7 days of the month — see int_method_monday.\n","original_file_path":"models/metrics/v_metric__syncs_trajectory.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Calendar-day linear projection of full-month syncs.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('syncs_trajectory') }}\n","tests":[]},{"name":"v_metric__trial_to_conversion_rate","alias":"v_metric__trial_to_conversion_rate","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__trial_to_conversion_rate`","description":"Monthly Trial-to-Conversion Rate — conversions divided by trials\nthat month, at account-grain. Measures how well trial accounts\nprogress to paying customers. Both numerator and denominator are\naccount-grain counts (see Conversions #56 and Trials #54). Use for\nfunnel-stage analysis. Note: the numerator (conversions) and\ndenominator (trials) for the same month don't share a cohort —\nmost conversions in a given month come from trials in earlier\nmonths. For cohort-locked conversion rate, a different metric\nwould be needed.\n","original_file_path":"models/metrics/v_metric__trial_to_conversion_rate.sql","refs":["v_metric__conversions","v_metric__trials"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Trial-to-Conversion Rate\" (#302)\n-- Type: ratio (cross-model)\n-- Formula: SAFE_DIVIDE(conversions, trials) per period\n\nSELECT\n  COALESCE(c.period, t.period) AS period,\n  SAFE_DIVIDE(c.value, t.value) AS value\nFROM {{ ref('v_metric__conversions') }} c\nFULL OUTER JOIN {{ ref('v_metric__trials') }} t\n  ON c.period = t.period\nORDER BY 1\n","tests":[]},{"name":"v_metric__trials","alias":"v_metric__trials","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__trials`","description":"Monthly count of Method accounts that began a trial. Account-grain —\na customer with 2 trial accounts contributes 2 trials, by design.\nExcludes test accounts, internal Method Integration partner rows,\nand the '0001-01-01' sentinel value. For unique-customer counts,\nuse Customers (#373).\n","original_file_path":"models/metrics/v_metric__trials.sql","refs":["int_trials"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Canonical metric: \"Trials\" (#54)\n-- Type: simple aggregation of v_trials.SignupDate (COUNT(*) per month)\n-- Materialization: rolling 24 months ending at the current day.\n-- Description and BQ labels come from v_metric__trials.yml — at `dbt run`\n-- time, dbt-bigquery wraps this SELECT with CREATE OR REPLACE VIEW ...\n-- OPTIONS(description, labels) automatically.\n\nSELECT\n  DATE_TRUNC(SignupDate, MONTH) AS period,\n  COUNT(*) AS value\nFROM {{ ref('int_trials') }}\nWHERE SignupDate >= DATE_SUB(CURRENT_DATE(), INTERVAL 24 MONTH)\nGROUP BY 1\nORDER BY 1\n","tests":[]},{"name":"v_metric__trials_mtd","alias":"v_metric__trials_mtd","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__trials_mtd`","description":"Trials so far this month, excluding today. A projection of int_method_monday into (period, value); the definition lives upstream in that model.\n","original_file_path":"models/metrics/v_metric__trials_mtd.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Trials so far this month, excluding today.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('trials_mtd') }}\n","tests":[]},{"name":"v_metric__trials_trajectory","alias":"v_metric__trials_trajectory","relation_name":"`project-for-method-dw`.`revenue_metrics`.`v_metric__trials_trajectory`","description":"Calendar-day linear projection of full-month trials. A projection of int_method_monday into (period, value); the definition lives upstream in that model. Returns NULL for the first 7 days of the month — see int_method_monday.\n","original_file_path":"models/metrics/v_metric__trials_trajectory.sql","refs":["int_method_monday"],"sources":[],"columns":[],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Calendar-day linear projection of full-month trials.\n--\n-- A projection of int_method_monday, nothing more. The definition, the filters and\n-- the early-month trajectory suppression all live in that model; this view only\n-- reshapes one of its columns into the (period, value) contract the registry and\n-- the scorecard query planner expect.\n--\n-- Adopted into dbt 2026-09-08 from a hand-cut BigQuery view. SQL unchanged in\n-- substance; the CAST is now applied uniformly across all thirteen siblings.\n\n{{ method_monday_metric('trials_trajectory') }}\n","tests":[]},{"name":"v_motion_funnel","alias":"v_motion_funnel","relation_name":"`project-for-method-dw`.`revenue`.`v_motion_funnel`","description":"Motion + lifecycle acquisition funnel, aggregated to (signup_month, motion). Counts only: trials → synced → demo booked/attended → converted → customized → retained at 1/3/6/12 months (each with an eligibility denominator for maturity). The talked-to-us fork is only valid for 2024+ cohorts. Directional — inputs (Activity, V7) are partial; not a verified metric.\n","original_file_path":"models/marketing/v_motion_funnel.sql","refs":["int_motion_funnel"],"sources":[],"columns":[{"name":"signup_month","description":""},{"name":"motion","description":""}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- Aggregated motion + lifecycle funnel for the chart. One row per (signup_month, motion).\n-- Ships COUNTS only — the frontend computes conversion %, show rate, and retention rate\n-- (retained_Kmo / eligible_Kmo). status: directional (see labels); lives in `revenue`.\n-- The chart filters to motion_trackable cohorts (2024+) for the fork.\n\nSELECT\n  signup_month,\n  motion,\n  COUNT(*)                                          AS trials,\n  COUNTIF(synced)                                   AS synced,\n  COUNTIF(demo_booked)                              AS demo_booked,\n  COUNTIF(demo_attended)                            AS demo_attended,\n  COUNTIF(free_booked)                              AS free_booked,\n  COUNTIF(free_attended)                            AS free_attended,\n  COUNTIF(converted)                                AS converted,\n  COUNTIF(converted AND is_customized)              AS customized,\n  COUNTIF(converted AND eligible_1mo)               AS eligible_1mo,\n  COUNTIF(converted AND eligible_1mo AND retained_1mo)   AS retained_1mo,\n  COUNTIF(converted AND eligible_3mo)               AS eligible_3mo,\n  COUNTIF(converted AND eligible_3mo AND retained_3mo)   AS retained_3mo,\n  COUNTIF(converted AND eligible_6mo)               AS eligible_6mo,\n  COUNTIF(converted AND eligible_6mo AND retained_6mo)   AS retained_6mo,\n  COUNTIF(converted AND eligible_12mo)              AS eligible_12mo,\n  COUNTIF(converted AND eligible_12mo AND retained_12mo) AS retained_12mo\nFROM {{ ref('int_motion_funnel') }}\nGROUP BY 1, 2\n","tests":["not_null(motion)","not_null(signup_month)"]},{"name":"v_partner_scorecard","alias":"v_partner_scorecard","relation_name":"`project-for-method-dw`.`revenue`.`v_partner_scorecard`","description":"One row per partner: roll-up of int_partner_accounts. AccountsReferred is all-time; ActiveAccounts is a lifecycle count (not cancelled); TotalLicenses and TotalMRR are actual billing for the latest complete month. Active count and TotalMRR intentionally do not reconcile (hard-hold accounts are active but bill $0). Raw partner strings in v1, so a partner with name variants can appear as more than one row (3 partners affected; see spec follow-ups).\n","original_file_path":"models/partners/v_partner_scorecard.sql","refs":["int_partner_accounts"],"sources":[],"columns":[{"name":"Partner","description":"Referring partner, raw string (not normalized in v1)."},{"name":"AccountsReferred","description":"All-time count of accounts referred by this partner."},{"name":"ActiveAccounts","description":"Lifecycle-active accounts (SignupDate set, not cancelled)."},{"name":"TotalLicenses","description":"Sum of paid licenses billed across the partner's accounts in the latest complete month."},{"name":"TotalMRR","description":"Sum of SaaS billed across the partner's accounts in the latest complete month (financially correct run-rate)."}],"compiled_sql":"{{ config(materialized='view') }}\n\n-- One row per partner: roll-up of int_partner_accounts.\n--\n-- Raw partner strings (v1): variant names are not yet merged, so a partner with\n--   name variants appears as >1 row (only 3 partners affected; see spec).\n-- ActiveAccounts is a lifecycle count; TotalMRR / TotalLicenses are the actual\n--   billing for the latest complete month. The two intentionally do not\n--   reconcile (hard-hold accounts are active but bill $0). Both are correct for\n--   their own question. See memory project_hard_hold_billing_state.\n\nSELECT\n  Partner,\n  COUNT(*)                  AS AccountsReferred,   -- all-time\n  COUNTIF(IsActive)         AS ActiveAccounts,     -- lifecycle: not cancelled\n  SUM(Licenses)             AS TotalLicenses,      -- billed, latest complete month\n  CAST(SUM(MRR) AS NUMERIC) AS TotalMRR            -- billed, latest complete month\nFROM {{ ref('int_partner_accounts') }}\nGROUP BY Partner\nORDER BY ActiveAccounts DESC\n","tests":["not_null(AccountsReferred)","not_null(ActiveAccounts)","not_null(Partner)","unique(Partner)"]}]}