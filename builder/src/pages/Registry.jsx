import React from 'react';

export default function Registry() {
  return (
    <div style={{ padding: 48, maxWidth: 600 }}>
      <h1 style={{ fontSize: 24, fontWeight: 700, color: '#edf0f3', marginBottom: 8 }}>
        Metric Registry
      </h1>
      <p style={{ color: '#5a6370', fontSize: 14, lineHeight: 1.6 }}>
        The metric registry will be ported from tracker.html into this view.
        All metric management — types, statuses, SQL definitions — in one place.
      </p>
    </div>
  );
}
